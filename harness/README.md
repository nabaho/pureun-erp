# 급여 하네스 (harness)

급여 아웃소싱 자동화의 1차 단계. 완성 급여대장(골든)을 읽고 채점하기 위한 도구.
지시문: [`../docs/하네스1차_지시문_v2.2.md`](../docs/하네스1차_지시문_v2.2.md)

## ⚠️ 코드 / 자료 분리 원칙 (개인정보 보호)

- **코드는 여기(`pureunall`, git·GitHub)** — 개인정보 없음.
- **실제 급여 자료(주민번호 포함)는 여기 없음.** 별도 자료폴더에 둔다.
  - 기본 자료폴더: `C:\Users\fair0\OneDrive\바탕 화면\급여아웃소싱 서류들`
  - 담당자 폴더(주민정·박은비·김보람)와 아카이브(2.급여관리)가 그 안에 있음.
- **하네스 결과물(직원명+급여 포함)은 자료폴더의 `_harness_out/`** 에 생성된다(git 제외).
- 급여 엑셀/스캔/결과 JSON은 `.gitignore`로 커밋이 차단돼 있다. 절대 코드 repo에 넣지 말 것.

## 실행

```bash
# 기본 경로 사용
python harness/classify.py

# 자료폴더가 다른 위치면 환경변수로 지정
PAYROLL_DATA_ROOT="D:\경로\자료폴더" python harness/classify.py
```

## 현황 (목표 진행)

- [x] **목표 0 — 파일 자동 분류** (`classify.py`)
      담당자 3인 폴더 5,265개를 급여대장/근태/근로계약서/퇴직금/공단신고/기타로 분류.
      비급여(연차·명부·근무표·양식 등) 강제 제외 규칙 포함.
      결과: `_harness_out/file_classification.json`, `classify_summary.txt`
- [x] **목표 1 — 골든 파서** (`parser_v1.py`, `profile.py`)
      3종 파서: 가로 표형 + 세로 명세서형(병합셀 값읽기+상식검증, 탈락분은
      n_flagged로 사람확인) + 사업소득형(쪼개진 헤더 결합). 월탭 분해.
      성명 직급필터(NAME_BLOCK)로 오염 23%→0%. 주민번호 미저장.
      **전체 82%(747/908), 상용 84%, 일용 73%, 직원 27,671명, 월별시트 1,758개.**
      결과: `_harness_out/parser_output.json`, `parser_coverage.txt`
- [x] **목표 2 — 단수처리 판정** (`analyze.py`) 13곳 판정: 절사5·반올림4·올림4 혼재
      (설계 전제 "사업장마다 제각각" 실측 확인). 과세총액 확보 사업장만 가능.
- [x] **목표 3 — 산정기간 감지 (D1)** (`period.py`) 64곳 감지: 1일~말일 58곳,
      비표준 6곳(25일~24일×3, 11일~10일, 11일~말일, 24일~23일). 실패 6곳은
      대장에 표기 없음 → 설정 카드에서 사람 지정. `period_summary.txt`
- [x] **목표 4 — 동명이인 조사 (D2)** (`analyze.py`) 시트내 중복 기준 5곳 19건 후보.
      주민번호 미저장이라 확정은 불가(D2 매칭키 필요 확인). `analyze_summary.txt`
- [x] **목표 5 — 근태↔대장 매칭 키 조사** (`matching.py`) 근태 278파일·20사업장.
      판정: 이름단독 2곳 / 보조키필요(표기차이3+동명이인1) 4곳 / 불가·자료부족 2곳
      / 이름추출실패 9곳(사진·특수서식). 보조키 실재: 부서·직급·입사일·연락처·사번
      (사업장별 상이). 주민번호는 존재여부만 기록. `matching_summary.txt`
- [x] **목표 6 — 급여일 분포** (`analyze.py`) 폴더명 기준 20곳:
      5일 7·말일 7·25일 3·10일 2·26일 1.
- [x] **목표 7 — 커버리지 리포트** (`report.py`) → `_harness_out/coverage_report.html`
      (집계 전용, 개인정보 미포함)
