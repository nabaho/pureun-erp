# 급여 하네스 (harness)

급여 아웃소싱 자동화의 1차 단계. 완성 급여대장(골든)을 읽고 채점하기 위한 도구.
지시문: [`../docs/하네스1차_지시문_v2.2.md`](../docs/하네스1차_지시문_v2.2.md)

## ⚠️ 코드 / 자료 분리 원칙 (개인정보 보호)

- **코드는 여기(`pureunall`, git·GitHub)** — 개인정보 없음.
- **실제 급여 자료(주민번호 포함)는 여기 없음.** 별도 자료폴더에 둔다.
  - 기본 자료폴더: `C:\Users\fair0\OneDrive\바탕 화면\급여아웃소싱 서류들`
  - 담당자 폴더(주민정·박은비·김보람)와 아카이브(2.급여관리)가 그 안에 있음.
- **하네스 결과물(직원명+급여 포함)은 자료폴더의 `_harness_out/`** 에 생성된다(git 제외).
- 급여 엑셀/스캔/결과 JSON은 `.gitignore`로 커밋이 차단돼 있다. 절대 코드 repo에 넣지 말 것.

## 실행

```bash
# 기본 경로 사용
python harness/classify.py

# 자료폴더가 다른 위치면 환경변수로 지정
PAYROLL_DATA_ROOT="D:\경로\자료폴더" python harness/classify.py
```

## 현황 (목표 진행)

- [x] **목표 0 — 파일 자동 분류** (`classify.py`)
      담당자 3인 폴더 5,265개를 급여대장/근태/근로계약서/퇴직금/공단신고/기타로 분류.
      비급여(연차·명부·근무표·양식 등) 강제 제외 규칙 포함.
      결과: `_harness_out/file_classification.json`, `classify_summary.txt`
- [x] **목표 1 — 골든 파서 v1 (가로 표형)** (`parser_v1.py`, `profile.py`)
      급여대장 시트 자동선택 + 2~3줄 병합헤더 평탄화 + 주민번호/이름기준 행인식
      + 연간누적형 월탭 분해. 주민번호는 저장하지 않음(성명만).
      **상용 성공률 83% (621/748), 직원 28,005명, 월별시트 1,703개 추출.**
      결과: `_harness_out/parser_output.json`, `parser_coverage.txt`
  - [ ] (미완) 일용직 블록/세로 명세서형 — 인식은 되나 **병합셀 값 위치가 부정확**
        (`extract_vertical`, 기본 비활성 / HARNESS_VERTICAL=1 실험용). 전용 정밀작업 필요.
  - [ ] (미완) 사업소득지급대장(3.3%) 등 특수양식
- [ ] 목표 2 — 단수처리 자동 판정
- [ ] 목표 3 — 산정기간 감지 (D1)
- [ ] 목표 4 — 동명이인 조사 (D2)
- [ ] 목표 5 — 근태↔대장 매칭 키 조사
- [ ] 목표 6 — 급여일 분포 조사
- [ ] 목표 7 — 커버리지 리포트(HTML 1장)
