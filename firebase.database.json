{
  "_주의": "규칙 배포 전용 설정이다. 루트 firebase.json 에 database 를 넣지 않은 까닭은, 그러면 다른 세션이 `firebase deploy` 를 할 때 규칙까지 함께 나가기 때문이다. 규칙은 반드시 scripts/rules-deploy.js 로만 배포한다.",
  "database": {
    "rules": "docs/rules-paste.json"
  }
}
