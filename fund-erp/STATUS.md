# 기금 시스템 — 공용 상태 (STATUS)

> **모든 세션(어느 로그인·어느 PC든) 규칙**
> 1. **시작 시**: 이 파일을 읽고, `git log --oneline -20`으로 최근 변경 확인 후 이어간다.
> 2. **끝날 때**: 이 파일의 "진행률·다음 할 일"을 갱신하고 commit/push 한다.
> 3. AI 개인 메모리에 의존하지 말 것 — 계정마다 다르다. **이 파일이 유일한 공용 뇌.**

---

## 0. 한 줄 요약
푸른노무법인 근로복지기금 42개(충남·경기 지역공동 16 / 개별공동 / 사내) 통합 운영 시스템. **두 버전 병행, 현재 주력은 "인터넷판(정적+Firebase)".**

## 1. 두 버전
| | 로컬 파이썬판 | 인터넷판 (주력) |
|---|---|---|
| 위치 | (로컬 전용) `바탕화면/8. 공동기금/02_프로그램/fund-erp/` | 이 저장소 `fund.html` |
| 구조 | FastAPI + SQLite + 단일 HTML | 정적 HTML + Firebase RTDB (서버 없음) |
| 실행 | `실행.bat` (localhost:8777) | https://nabaho.github.io/pureunall/fund.html |
| 기능 | 전 기능 완성(M1~M17) | 이전 중(§3) |

- 포털 `enter.html` "기금관리" 타일 → fund.html (`?user=&role=` 전달).
- ⚠️ `fund-erp/` 이 폴더는 로컬 파이썬판을 저장소에 보관한 사본. 실행에는 fund.db·templates 별도 필요(비공개).

## 2. Firebase (인터넷판)
- 프로젝트 `pureun-erp`, Realtime DB `https://pureun-erp-default-rtdb.asia-southeast1.firebasedatabase.app`, SDK 10.13.2 compat + App Check reCAPTCHA v3.
- 로그인은 pu-erp와 공유(같은 도메인 auth).
- **네임스페이스 `fund_erp/*` 필수**(루트 `funds`는 pu-erp 계약용, 충돌 금지).
- RTDB 규칙: `fund_erp` `.read/.write:"auth != null"` 게시됨.
- 데이터: `fund_erp/funds/{fid}`(+`years/{yr}/subsidy`,`years/{yr}/opening`, +담당 `mgr_main{sid,name}`·`mgr_subs[]`·`manager`문자열), `/sites/{fid}/{sid}`, `/annual/{fid}/{yr}/{code}`, `/welfare/{fid}/{yr}/{id}`, `/events/{fid}/{eid}`, `/txns/{fid}/{yr}/{hkey}`, `/billings/{id}`.
- **담당자 명단은 pu-erp와 공유**: `data/user_accounts/v`(pu-erp 직원계정 배열) 읽어 재직(active)만 사용. ⚠️ RTDB 규칙이 `data` 경로 auth 읽기를 허용해야 함(pu-erp가 쓰므로 대개 허용됨). 담당 모달에서 "명단 로드 실패" 뜨면 규칙에 `data/user_accounts` 읽기 추가 필요.

## 3. fund.html 진행률
**완성 ✅**: 홈(**그룹 탭** 지역/공동/사내/지난, 계약관계 인라인 드롭다운, **정보 채우기 토글**=핵심6필드 인라인입력, 검색 — 기금 대장 흡수) · 기금대장(인라인) · 청구관리 · 기금정보 · 참여사업장 · 연간일정 · 지원금(공동) · 목적사업·대부 · 변경이벤트 · **회계·결산**(통장 SheetJS 파싱→자동분개→승인→결산관문→재무상태표·운영성과표·시산표→별지15호 인쇄) · **서식 자료실**(설립·지원신청서 원본 엑셀 데이터 채움, ExcelJS) · **담당자 지정**(주/부담당, pu-erp 재직자 선택)
**미완성 ⬜**: docgen HTML 서류(참고용 미리보기 — 인터넷판 미이전, 실제 제출은 서식 자료실 엑셀 사용)

- 구조: 전역 `S`(view/fundId/tab/year/*For 캐시)·`fbDb`·`funds`·`esc()`·`num()`·`showModal/closeM`. 탭: `infoForm/sitesTab/annualTab/subsidyTab/welfareTab/eventsTab/closingTab`. 회계: `parseBank/proposeAcct/computeFin/finPanel/openForm15`. 서식: `renderForms/pickTpl/fillForm/fillSetup/fillSubsidy`(ExcelJS+IndexedDB `fundErpTpl`). 상수: `ACCT_CHART/ACCT_RULES/ANNUAL_TMPL/EVENT_KINDS/WELF_CATS/TPL`. head: SheetJS(xlsx 0.18.5, 통장파싱)+ExcelJS(4.4.0, 서식채움) 로드.
- 코덱스(codex CLI) 검증 반영: XSS esc, saveInfo update, 비동기 레이스 가드(`_fid/_k` 캡처 후 `S.*For` 일치), 통장 재가져오기 보존, parseBank 건수열 제외, num 소수·부호, saveSubsidy 사내 차단. (미해결 낮음: 인라인 onclick 키 이스케이프 — Firebase 키가 안전문자만이라 무해)

## 4. 다음 할 일 (우선순위)
1. **서식 자료실 사용자 검증** — 원본 엑셀 등록 방식 확정(파일선택+IndexedDB, Firebase Storage 대신). 사용자가 로컬 `02_프로그램/fund-erp/templates/` 2개 파일을 서식 자료실에서 [원본 엑셀 등록] → 대상 기금 선택 → [데이터 채워 받기]로 실제 채움 확인 필요. node 검증은 통과(설립 21건·수식/병합 보존).
2. **미완비 기금 기본정보** — 03_과거자료 인가증·등기부 스캔 판독으로 **17개 기금 인가번호/인가일/설립등기일 확보→fund.db 반영**(2026-07-23). 잔여 15개는 스캔 자체가 없어 사용자 원본 필요: 충남 4·5·7호(인가일), 충남 9호·청신(인가번호), 충남 10·11·12호·경기 2호·플러스·디와이·나래산업·수공건설·다움(전부), 가치(설립등기일). ⚠청신 인가번호는 판독 불명확(5140-20??-7)→물리 인가증 확인. fund.db→`backups/fund_funds_full_export.json` 전체 export 갱신(task#12 Firebase 이전용). Firebase(fund.html)엔 아직 미반영 — 사용자가 기금 정보 탭 입력 또는 실데이터 이전 시 반영.
3. **docgen HTML 미리보기**(선택) — 참고용 화면. 실제 제출은 서식 자료실 엑셀이므로 우선순위 낮음.
4. **코덱스 2차 검증 잔여** — ⒁·⑿·⑷·⑻(00270a9), **⑼ 계정과목 확정 완료**(c5a3f60: 목적사업비 9계정 PURPOSE_ACCTS, WELF_CATS↔계정 통일, 모성보호비·근로자의날행사비·근로복지시설비 추가). **남은 것**: ㉕객체 키 inline-JS 삽입(키가 push()/FUND-NNNN이라 실위험 낮음).

## 5. 작업/배포 흐름
```
cd ~/Documents/pureunall
git pull --rebase origin main      # ⚠️ 원격에 급여·명함첩·달력 작업 자주 올라옴 — 반드시 먼저
# fund.html 편집
git add fund.html && git commit -m "..." && git push origin main
# github.io 재배포 1~2분. 확인 Ctrl+Shift+R
```
- JS 문법: `<script>` 추출 후 `node --check`.
- 브라우저 실검증은 로그인 필요 → 사용자가 fund.html 열어 확인.

## 6. 보안 주의 (중요)
- **저장소 금지**: `fund.db`, 사업장 실데이터, `templates/` 서식엑셀, `backups/`. (로컬 .gitignore·export FORBID로 차단.)
- 서식 원본 엑셀은 **각 사용자가 브라우저 IndexedDB(`fundErpTpl`)에 로컬 등록** — 저장소·Firebase에 안 올라감. 로컬 원본 위치 `02_프로그램/fund-erp/templates/` 2종.
- 시드용(로컬 gitignore): `02_프로그램/fund-erp/backups/fund_funds_export.json`(42기금), `fund_sites_export.json`(563사업장, **2026-07-21 contacts 담당자 포함 재생성** — 이전 export가 contacts 누락) → fund.html 참여사업장 [📥 가져오기]로 Firebase 업로드. ⚠️ 담당자 연락처 반영하려면 이 재생성본으로 **재가져오기 1회 필요**(sites/{fid} 통째 덮어씀, site_id 동일키라 안전).
- 실데이터 이전 현황: funds·sites는 Firebase에 올라감. annual/welfare/txns/subsidy는 사용 시 생성.
- PII(주민번호·인감·비번) 스키마 미저장. `Downloads/푸른노무법인(4).xlsx`에 평문 비번·주민번호 — 통째 공유 금지.

---
## 변경 로그 (세션 끝에 한 줄씩 추가)
- 2026-07-27: **[fund.html] 화면 설명문을 ⓘ 도움말 팝업으로 전환 — 전 화면 일괄** — 사용자 지적("셀이나 클릭해서 보는 내용은 팝업으로 보여줘도 된다. 화면에 모든 내용을 미리 넣어 이해하기 어렵게 만들지 말고 불필요한 공간 차지 안 하게. 기금 관련 모든 영역에 반영"). 서식 화면이 대표적이었음 — 머리말 1줄 + 주황 안내박스 + `hwpNote` 3줄이 단계마다 반복돼 정작 서식 목록이 접혀 있었다. **먼저 전수 조사**: `<p class="muted">`·`msg` 박스·안내 div를 한글 글자수로 재서 **25자 이상 37곳**을 뽑음(scan_help.py). ①공통 장치 `HELP` 등록부(31항목) + `hlp(key)`(ⓘ 버튼) + `showHelp(key)`(모달) 신설 — 없는 키면 아무것도 그리지 않고, `event.stopPropagation()`으로 행·탭 클릭과 충돌 방지, 제목은 `esc()` 처리. `.hbtn`(작은 알약 버튼)·`.helpbody`(팝업 본문 타이포) CSS 추가. ②**설명을 지운 게 아니라 접었다** — 문단을 팝업으로 옮기고 화면에는 핵심 한 줄만 남김(예: 정보 채우기는 '칸에 입력하면 자동 저장 · 빨간 테두리=빈 칸 ⓘ', 완비 5항목·등기소 추정 주의는 팝업). 서식 화면은 주황 안내박스를 **버튼 한 줄**로 바꾸고 `hwpNote`를 제거해 단계마다 반복되던 3줄을 없앰, 기금 상세 임베드 머리말도 한 줄로. ③적용 범위: 홈(정보 채우기·설립중·삭제보관·지난 기금), 기금 상태·담당자·새 기금 모달, 등기소/세무서 일괄 추정, 지원금 보드·신청·엑셀 채움, 청구 관리 ①②, 서식 6단계·미리보기·엑셀 미리보기·전체 초안·편집 패널, 설립 진행·설립중 목록, 서류 판독, 회계·결산, 연간 일정, 사업장 당겨오기 = **ⓘ 34곳**. ④**오류·상태 메시지는 화면에 그대로** — 지금 조치해야 하는 것이라 팝업으로 숨기면 안 됨(재직자 명단 실패·ExcelJS 로드 실패·Storage 미활성 3곳만 남김). 검증: **38항목 통과** — 등록부 무결성(제목·본문·태그 균형·따옴표), `hlp()` 호출 31종 전부 등록부에 존재·미사용 항목 0, 안전장치 8건, 남은 장문 3곳이 전부 msg 박스임을 확인, **지운 안내가 팝업에 살아 있는지 15개 키워드로 역검증**(iros.go.kr·hometax.go.kr·15MB·3개월·rhwp·전대 사용동의서·12단계 등). 회귀: 청구 48·메뉴드래그 34·모바일 51·A4 22·파서 14·node 문법 통과.
- 2026-07-27: **[취업규칙] 조 제목 괄호 【】·[] 인식 + DOCX/HWPX 추출 태그 오매칭 수정** — 사용자 지적('이 서식은 왜 분리되어 나오지 않나'). 세화전자 개정안(docx)에서 제34조 하나가 뒤 조문 전부를 삼켜 한 덩어리로 나왔음. **원인 1 — 조 헤딩 정규식**: `parseArticles`가 조 뒤에 `(`·`（`·공백·줄끝만 인정했는데 이 문서는 `제 5 조【성실의 의무】` 형식. 실측 집계: 줄머리 헤딩 103개 중 **【 96개 · [ 2개 · ( 4개** → 6개만 인식되고 97개 누락. `BR_OPEN/BR_CLOSE`로 괄호 쌍 12종(( （ [ 【 〔 〈 《 「 『 ｛ {)을 정의하고 `RE_HEAD·RE_TITLE·RE_HEAD_STRIP·RE_HEAD_SPLIT`로 공용화해 헤딩 인식·제목 추출·본문 앞머리 제거·조문 블록 분리 5곳에 일괄 적용. 결과: 세화전자 6→**102개**(미인식 1개는 부칙 제1조로 정상 제외), 맥스텍 **77개**(미인식 0), 내장 샘플 3종은 54/28/42개로 **회귀 없음**, 【】·[]·() 혼합 문서도 정상 분리. **원인 2 — 추출기 태그 오매칭**: `extractDocxText`의 `<w:t[^>]*>`가 `<w:top …/>`·`<w:tblPr>`처럼 t로 시작하는 다른 태그까지 잡아 서식 XML이 본문에 섞였음(세화전자 33,205자 중 8,412자가 XML 쓰레기). `<w:t(?:\s[^>]*)?>`로 태그 이름을 정확히 맞춰 24,793자로 정리. `extractHwpxText`의 `<hp:t[^>]*>`도 같은 결함(`<hp:tbl>`·`<hp:tc>`·`<hp:tr>`)이라 함께 수정. **추가**: RTF 추출에서 `fonttbl·colortbl·stylesheet·info` 그룹을 제거해 본문 앞 '맑은 고딕;' 잔여물 제거.
- 2026-07-27: **[fund.html] 사이드바 메뉴 순서 드래그** — 사용자 요청("대시보드 마우스로 드래그해서 이동"). 대상이 갈려(넓은 표 끌어 스크롤/메뉴 순서/패널 순서/모달 이동) 확인 후 **메뉴 순서 변경**으로 확정. 메뉴 4개를 `#navlist`로 묶고 각 항목에 `⋮⋮` 손잡이(푸른이알피 관례)+`data-nav`. **Pointer Events 사용**(`pointerdown/move/up/cancel`) — HTML5 `dragstart`는 터치에서 동작하지 않아 폰에서 못 씀. `_navPlace(list,el,y)`가 형제의 **중간선**을 기준으로 실제 DOM 위치를 즉시 옮겨(자리표시자 불필요) 끌리는 대로 보이게 함. **클릭 보존**: 5px 미만 움직임은 드래그로 보지 않고 평소대로 화면 이동, 드래그한 경우엔 뒤따르는 click을 캡처 단계에서 **1회만 삼켜** 화면이 바뀌지 않게 함. 저장은 `localStorage['fund_nav_order_v1']`(즉시) + `fund_erp/ui_prefs/{uid}/nav_order`(다른 PC에서도 유지, 실패해도 기기에는 남음), `start()`에서 `bindNavDrag()+loadNavOrder()`. `route()`는 `className`만 바꿔 순서를 다시 만들지 않음. `touch-action:pan-y`로 폰 세로 스크롤 유지. 검증: 실제 `_navPlace/applyNavOrder/_navIds`를 좌표 있는 가짜 DOM에 걸어 **34항목 통과** — 위/아래/맨위/맨아래 이동, 제자리 유지 4건, 40회 연속 드래그 후 개수·중복 무결성, 저장순서 복원(없는 id·일부만 저장된 경우 포함), 배선 18건. ⚠실제 마우스 드래그 감각은 사용자 확인 필요.
- 2026-07-27: **[취업규칙] 신구대조표 용지 방향 자동 판단** — 사용자 요청('상황에 따라 가로세로 자유롭게 조절, 내용 파악하고'). 고정 기본값 대신 **내용을 실제로 재서** 정한다. `orientMetrics(land)`가 열 비율을 실폭(HWPUNIT)으로 환산해 열마다 '한 줄에 들어가는 한글 글자 수'(`(폭-셀여백)/글자폭`, 라틴·숫자는 반각으로 계산)와 표 전체 줄 수·예상 페이지를 구하고, `autoOrient()`가 세로로도 가장 좁은 열이 12자(`CPL_OK`) 이상이면 세로, 아니면 가로를 고른다. 판단 이유를 문장으로 만들어 편집창 요약줄에 '📄 A4 가로 (세로는 한 줄 6자로 좁아 가로(한 줄 12자))'처럼 표시. 용지 버튼을 [자동][A4 세로][A4 가로] 3개로 바꾸고 기본을 **자동**(`PAGE_AUTO`)으로, 사람이 세로·가로를 누르면 그 선택을 고정한다(작업 세션에 `pauto` 저장). 다운로드 3곳(한글·엑셀·제출서류)에서 생성 직전 `applyAutoOrient()` 호출. 실측: 4열(변경이유 포함)은 세로 6자/가로 12자 → **가로** 선택, 3열([전체 미반영])은 세로 13자 → **세로** 선택, 수동 고정이 자동을 이김. 배경: 12pt·A4 세로·4열에서는 변경이유 열이 한 줄 6자라 근거 문장이 토막나 읽을 수 없었다.
- 2026-07-27: **[fund.html] 청구 관리 — 푸른이알피 기금 청구 자동 연결(읽기 전용)** — 사용자 요청("푸른이알피에서 기금과 관련되어 청구된 것들을 자동으로 연결해 별도 관리"). **푸른이알피 데이터 모델 실측**: 사무관리 4종(cases/consultings/**funds**/other_projects)이 `ProjectManagementShared` 한 컴포넌트를 공유하고, 기금은 `storageKey:'funds'`·`sourceKind:'fund'`·`incomeCategory:'inc-fund-fee'`. 저장 경로는 `data/{key}/v`(배열 또는 {id:item} 둘 다 나옴). **미입금**은 계약 항목의 `contractFee/contractPaid`·`balanceFee/balancePaid` 플래그로, **입금완료**는 `finance_income` 중 `sourceKind==='fund'`로 잡는다(푸른이알피 입금관리 `extractPending`과 같은 기준 → **이중계상 없음**). ①`loadPuerp()` — `data/funds/v`+`data/finance_income/v`를 **once로만 읽고**(쓰기 코드 없음: 원장 파손 방지), 권한 실패 시 같은 출처 `localStorage['pureun_v6_*']` 폴백. ②**자동 연결 `linkPuerp()`** — 수동지정 → **고유번호(tax_id_no)**/사업자/법인등록번호 → 기금명 완전일치 → 부분일치(후보 1개일 때만). ⚠기금은 사업자등록번호 필드가 없고 **고유번호로 계산서를 받으므로** `tax_id_no`가 1순위 번호. `_mkey()`가 ㈜·주식회사·'공동/사내근로복지기금'을 걷어내 **푸른이알피는 업체명("디와이"), 기금 시스템은 "디와이사내근로복지기금"** 인 사내기금도 붙는다. 후보가 둘 이상이면 **연결하지 않고** ⚠미연결 탭에 남겨 `[🔗 연결]`로 지정(보정값은 우리 네임스페이스 `fund_erp/puerp_link/{puerpId}`에만 저장, `__none__`=기금 아님). ③**화면 2단 재편** — ①푸른이알피 연동 청구(미입금/입금완료/⚠미연결 탭, 계약 건수·미입금·입금 합계, 읽기 전용 명시, [↻ 연동 새로고침]) + ②기금 시스템 자체 청구(기존 결산 보수 등 수동 편집 그대로). 담당은 사번(P-001)을 재직자 명단으로 이름 변환(명단이 늦게 와도 1회만 재렌더 — 캐시가 즉시 resolve돼 무한재귀 나던 것 플래그로 차단). 삭제 보관 기금 연결분은 다른 화면과 기준 맞춰 제외. 모바일 `.mo` 열 접기 적용. **검증: 실전 기금 42건에 실제 함수를 걸어 45항목 통과** — 고유번호 연결 6/6·정식명 42/42·약칭 42/42·사내기금 업체명만으로 5/5, 오연결 방지 5건("충남공동근로복지기금"처럼 호수 없는 이름은 자동연결 거부), 이중계상·정렬·빈데이터·고아매출, 푸른이알피 쓰기 코드 부재 확인. node 문법·모바일 51·A4 22·파서 14 회귀 통과. ⚠실제 푸른이알피 기금관리 데이터로 화면 확인은 사용자 필요(연결 결과·미연결 건수).
- 2026-07-27: **[취업규칙] 표 세로 병합(변경조항·변경이유 한 셀) + DOCX·ODT·RTF 읽기 + 줄간격 버그** — 사용자 지적 3건. **①표 세로 병합**: 항 단위로 행을 나눈 뒤 변경조항·변경이유까지 행마다 쪼개져 '어느 조문의 무슨 이유'인지 읽을 수 없었음. `hwpx_gen.js tablePara`가 셀 객체 {t,rowSpan}·null(덮인 자리)을 받도록 재작성 — 병합 셀은 `hp:cellSpan rowSpan=N`을 내고 덮인 자리는 `hp:tc`를 내지 않으며, 병합 구간 높이가 부족하면 마지막 행을 늘려 잘림을 막는다. `xlsx_gen.js`도 `mergeCells`로 동일 처리(병합 셀은 행 높이 계산에서 제외). `daejoBodyMerged()` 신설, 한글 2곳·엑셀 1곳 연결. 검증: 한글 COM 열기 OK → PDF에서 가로 구분선이 변경 전/후 열(x 171~421)에만 있고 변경조항·변경이유 열에는 없음 = 병합 확인. **②줄간격 버그**: 병합 셀만 줄간격 19.2pt(다른 셀 15.6pt)로 벌어졌음. 원인은 lineseg의 spacing을 CH*0.6(=720)로 두어 vertsize(1200)+spacing(720)=1920이 실제 줄높이 LH(1560)와 어긋난 것 — 한글이 재계산을 건너뛰는 병합 셀에서 내 값이 그대로 쓰였다. SPC=LH-VSZ로 고정. **③다른 파일 형식**: `hwp_extract.js`에 extractDocxText(word/document.xml, w:p 문단)·extractOdtText(content.xml)·extractRtfText(유니코드 이스케이프·바이트 이스케이프·par 처리) 추가, ZIP 안 항목명으로 HWPX/DOCX/ODT를 가르는 _zipKind, 그리고 **UTF-8 BOM 건너뛰기**(옛 내보내기가 만든 BOM 붙은 RTF가 CFB로 오판되던 문제). 업로드 accept·안내문구도 HWP·HWPX·DOCX·ODT·RTF·PDF·TXT로 확장하고, 텍스트가 거의 없으면 안내(옛 .doc 이진은 .docx로 저장 권고). 실측: 맥스텍 docx 23,020자·조문 87건 / 어린이집 hwpx 7,088자·50건 / BOM RTF(.doc) 15,217자·69건 모두 읽힘. **④열폭 조정**: 변경조항은 '제N조 / [처리]'만 들어가므로 좁히고 변경이유를 넓힘(세로 1.45/3.05/3.05/2.0, 가로 1.35/3.2/3.2/2.25).
- 2026-07-27: **[fund.html] 스마트폰 대응 — 서랍 메뉴 + 전체 컴팩트화** — 사용자 요청("기금관리 스마트폰에서 볼 수 있게 전체적으로 컴팩트하게"). 지금까지 `#side{width:216px;position:fixed}` + `#main{margin-left:216px}` 고정 2단이라 폰에서 본문이 216px 밀려 잘렸음. **①서랍 전환**(≤860px): `#side`를 `translateX(-100%)`로 밀어두고 상단바 **☰**(`#menubtn`)로 여닫음(`toggleSide/closeSide`, `body.sideopen`), 반투명 오버레이 `#sideveil` 클릭·Esc·화면이동(`_go`)·창 확대(resize>860)에서 자동 닫힘. z-index 사이드바 120 > 오버레이 110(가려서 못 누르는 문제 방지), `#side`에 `overflow-y:auto`·`100dvh`, `.sitem` 패딩 10px(터치 타깃). **②상단바**: 시계·연결표시 접기, 사용자명 30vw 말줄임, 로그아웃·크럼 축소. **③본문 컴팩트**: `#app` 여백 22→11px(≤600px 8px), `.panel` 16/18→11/12px, `.grid` **2열→1열**, `.search` `min-width:220px→0`(삐져나감 원인), 표 패딩 8→6→5px·폰트 13→12→11.5px, `.panel{overflow-x:auto}`로 넓은 내용은 패널 안에서 좌우 스크롤. **④탭바**: 홈 탭이 7개라 줄바꿈으로 2줄이 되던 것을 `flex-wrap:nowrap;overflow-x:auto`(스크롤바 숨김)로 **좌우 스와이프**. **⑤부차 열 접기**: `.mo` 클래스 신설 — ≤600px에서 기금목록 부담당·대표자, 삭제보관함 삭제자·상태 열을 숨김(th/td 짝 맞춤 검사 포함). **⑥서식 2단→위아래**: `#formLeft{width:100%!important}`·`#formGutter{display:none}`·`#formSide{position:static!important}`(인라인 스타일이라 !important 필요), 모달은 상단부터 꽉 차게. A4 미리보기는 `fitDocA4`가 그대로 동작(375px에서 37%, 320px에서 30% — 하한 20% 초과) + 핀치 확대 허용(`user-scalable` 미지정) + [⤢ 배율] 100% 전환으로 읽기 가능. **⑦iOS 입력 확대 차단**: iOS Safari는 16px 미만 입력칸에 포커스하면 화면을 확대하고 되돌리지 않아 매번 손으로 축소해야 함 → `.fld input/select/textarea`·`.search`를 16px로(인라인 폰트를 가진 '정보 채우기' 표 입력은 제외 — 12px 칸은 확대가 오히려 도움). 검증: 모바일 전용 검사 **51항목 통과**(미디어쿼리 캐스케이드 순서·특이도, 서랍 배선 9건, 인라인 스타일 덮어쓰기, th/td .mo 짝, 좁은폭 배율), A4 회귀 22/22, 파서 회귀 14/14, node 문법 통과. ⚠브라우저 자동화(내장 프리뷰·Chrome 모두)가 응답하지 않아 **실제 폰 화면 확인은 사용자 필요**.
- 2026-07-27: **[취업규칙] 시행일 색 강조(D-일수) + 다른 취업규칙으로 전환 안전장치** — 사용자 요청 2건. **①시행일 강조**: `effDays/effCls/effChip/effMini` 신설 — 검토 기준일 대비 남은 일수로 색을 나눔(여유=보라 #5b21b6, 90일 이내=주황 #9a3412 '시행 임박', 지난 것=빨강 #b91c1c '이미 시행됨'). 검토 카드 머리에 `📅 2026-11-27 시행 [D-129]` 칩, 지적사항 목록에는 작은 칩(`2025-02-23 시행 (D+513)`). **시행일이 있으면 상태와 무관하게 표시** — 실측에서 D10 난임치료휴가가 사업장에 따라 시행예정이 아니라 수동확인으로도 잡혀서, 시행예정 상태로만 한정하면 시행일이 안 보였음. 목록에서는 판정문의 '(날짜) 시행 예정' 중복 문구를 제거. 버그 1건 동시 수정: `renderFindings`가 `run()` 안에서 `LAST` 대입 **전에** 호출돼 기준일이 빈 값이었고 D-일수·색이 계산되지 않음 → `asofNow()`(LAST 우선, 없으면 입력칸)로 교체. **②다른 취업규칙으로 전환**: 임시저장(자동 저장) 슬롯은 `WORK_KEY`/`rules_mgmt/worksession/{uid}` **1건뿐**이라 새 파일을 올리면 조용히 덮어써졌음(보관함에는 사업장별로 남지만 배지에서 사라져 유실처럼 보임). `confirmSwitchDoc()` 신설 — `loadFile()` 진입 시 진행 중 작업이 있으면 편집 건수와 함께 알리고, [확인] 시 `doSaveArchive(true)`로 보관함에 확정 저장한 뒤 전환(데모 샘플은 저장 불가 안내). 임시저장 배지에 **[＋ 다른 취업규칙 시작]** 버튼 추가 — 보관함 저장 → 슬롯·편집모델 초기화 → 파일 선택창. 이어가기는 기존 대시보드/보관함(사업장별 `items`+원본 보존)에서 그대로 가능.
- 2026-07-27: **[취업규칙] 검토 카드 항 단위 좌우 비교 + 개선안 반영 시 현행 항 유실 차단** — 사용자 지적('조항의 좌우가 비교되어야 삭제·첨부를 할 수 있다'). 확인 과정에서 **더 심각한 유실 버그**를 함께 발견: `applyDecision('반영')`이 `it.after=it.sug`로 조문을 통째 교체해, 현행 6항 조문에 2항짜리 개선안을 반영하면 **③유산·사산휴가·④휴가급여 협조·⑥시간외근로 금지가 개정안에서 소멸**(제59조 임산부의 보호 실측). **①정렬을 번호→내용 기준으로 교체**: 마커로만 짝지어 현행②(유산 경험자 분할사용)↔개선②(유급 60일)처럼 무관한 항이 나란히 놓였음(개선②는 실제로 현행⑤에 대응). `simRatio`(한글 2-gram 자카드, 항번호·기호 제거) + `pairParas`(Needleman-Wunsch 순서보존 최적정렬, `PAIR_MIN=0.28` 미만은 짝으로 보지 않음) 도입 → 제59조에서 ①↔①(0.54)·⑤↔②(0.39)로 정확히 짝지어지고 ②③④⑥은 '유지 대상'으로 분리. `alignParas`가 이를 쓰므로 **신구대조표 정렬도 함께 개선**(제5조 회귀 확인: ①1.00·②0.68·③0.94·④신설). **②`mergeParas()` 신설 — 항 단위 병합**: 짝지어진 항만 개선 문안으로 바꾸고 짝 없는 현행 항은 살린다. 교체되는 항은 **현행 번호를 유지**(개선안 ②가 현행 ⑤ 자리에 들어가면 ⑤로) — 본문의 '제N항' 상호참조가 깨지지 않게. 개선안에만 있는 항은 현행 마지막 번호 다음으로 붙임. 실측: 반영 후 6항 유지·유산사산/급여협조/시간외근로 전부 보존·미숙아 100일 반영. **③검토 카드 UI 개편**(`paraCompare`): 좌우 두 덩어리 박스를 **항 단위 2열 그리드**로 바꿔 같은 항이 같은 줄에 오게 하고(`.dtpair`), 행마다 처리를 고른다 — 짝 있는 행 [개선안으로]/[현행 유지], 현행만 [유지]/[이 항 삭제](취소선 표시), 개선안만 [추가]/[빼기]. 유사도 0.45 미만 짝에는 '확인' 칩. 선택은 `it.paraPick`에 저장돼 [개선안 반영]이 이를 합쳐 최종 문안을 만든다. 개선 항 칸은 개별 편집 가능(입력 시 순서대로 `it.sug` 재조립). 실측: 반영→6항, ③ [이 항 삭제]→5항(유산사산만 제거, 나머지 보존), ⑤ [현행 유지]→그 항만 현행 복귀. 항별 선택이 서로 독립적으로 동작함을 확인.
- 2026-07-27: **[fund.html] 서식 쪽 나누기 + 쪽수 표기** — A4 적용 후 "긴 서식이 한 장으로 늘어나기만 한다"는 지적 반영. `.a4`를 **min-height→height 고정(297mm)** + flex 세로(본문 `.a4b` / 쪽수 `.a4f`)로 바꿔 쪽수가 장 하단에 붙게 함. `paginateDoc(html)`: 블록 요소를 담다가 본문높이(257mm−쪽수영역)를 넘치면 다음 장으로 넘겨 **실제로 여러 장 분할**, 장마다 `- n / 총쪽 -` 표기. 이미 `.a4`로 감싼 입력(전체 초안 6종)은 서식 경계를 유지한 채 각 서식 안에서만 재분할. **편집 중 자동 재분할은 안 함**(커서가 튀므로) — `markA4Overflow()`가 넘친 장에 빨간 테두리+배지를 띄우고 툴바 [⎘ 다시 나누기](`repaginateDoc`)로 정리. 저장본 재로드 시 쪽수 겹침 방지: `.a4f/.a4of` 제거 후 `.a4b` 껍데기를 벗겨 내용만 재분할. 인쇄 CSS도 `.a4{height:257mm;flex}`로 맞춰 쪽수가 인쇄물 하단 고정, 넘침배지는 인쇄 제외. HWPX 저장은 `h1,h2,h3,p,table`만 훑어 쪽수(div) 영향 없음 확인. 검증: A4·쪽나누기 규격 **22항목 통과**(생성 CSS를 node로 실행 검사), node 문법 통과. ⚠브라우저 자동화가 응답하지 않아 실제 분할 결과는 사용자 화면 확인 필요.
- 2026-07-27: **[취업규칙] 조문 오연결 구조 개선 + 교정 기억(공용) + 신설 조문 등급 분리·삭제 연동** — 사용자 지적 2건(①출산전후휴가 규칙이 배우자 출산휴가 조문에 연결 ②신설인데 문안 없음·삭제해도 가지번호가 당겨지지 않음). **원인 측정**: 「배우자출산휴가」가 「출산휴가」를 문자열로 포함해 제목일치 18점으로 가로챔 — 92개 규칙 키워드 전수 대조에서 이런 **포함관계 143쌍** 확인(휴가⊂생리휴가, 퇴직⊂퇴직급여, 임금⊂최저임금 등). 게다가 실제 문서(맥스텍)엔 「출산전후휴가」 제목 조문이 없고 내용은 「모성 보호」 본문에 있어 제목일치(18점)가 본문일치(4점)를 압도. **①매처**: 규칙 키워드를 서로 대조해 '내 키워드를 품은 더 구체적 용어'를 자동 산출(`specKw` — 수작업 예외표 없음) → 조문 제목이 그 용어를 담고 있으면 제목일치를 인정하지 않음. 동의어(성희롱↔직장내성희롱)는 `synPair`로 제외. 정식 법정용어(5자↑) 본문 일치는 +8로 강화, 제목 근거 없는 본문 매칭 문턱 6→8(`BODY_MIN`). 실측(맥스텍 75조×92규칙): **87건 동일·5건 변경·후퇴 0** — B13 출산전후휴가 제34조 배우자출산휴가→**제37조 모성보호**, B20 임신기단축 육아기→모성보호, B15 성희롱예방교육 조치→예방, D7 노동절 없음→제28조 휴일. **②교정 기억(전 사업장 공용)**: 검토 카드에 '조문 바꾸기' 드롭다운 신설 → (규칙ID↔조문제목) 쌍을 `rules_mgmt/matchfix`+localStorage(`pureun_rules_matchfix_v1`)에 저장, `bestHit`이 pin은 확정·ban은 후보 제외. 조문 제목은 사업장 무관하므로 한 번 고치면 전 사업장·전 노무사에 적용. `subscribeMfix` 실시간 동기화, [📌 교정 기억] 목록에서 되돌리기. 실측: ban→'관련 조문 없음', pin→지정 조문, 되돌리기→원복. **③신설 조문 등급 분리**(`insertGrade`): 샘플에서 신설 21건 중 17건(81%)이 문안 없는 `chkins_`. D=조문화 대상 아님(절차 C1·C2, 근로자대표 서면합의 E9·E10)은 신설 행을 만들지 않고 지적사항 목록에만 남김, C=선택 신설(타법령·조건부 8건)은 기본 제외 + [이 조문 신설함] 토글, B=문안 작성 필요(권장·최신개정 5건)는 대조표에 「문안 작성 필요」 배지. **문안 빈 신설은 제출 문서(한글·엑셀)에서 제외**하고 내려받기 전 경고. **④삭제 연동**(사용자 요청 '삭제하면 아래 조문이 삭제 전 조문으로 연결'): `liveAnchor()` 신설 — 삭제된 현행 조문을 앵커로 한 신설은 직전 생존 조문으로 자동 재연결(제31조 삭제 시 제31조의2→**제30조의2**), 신설하지 않는 것(삭제·선택 미포함)은 가지번호를 소비하지 않아 뒤 조문이 당겨짐(제34조의2를 신설 안 함 → 대지급금이 의3→**의2**). **⑤가지번호 게이트**: 제N조의n 신설마다 [위치·번호 확인] 체크 + 미확인 ⚠배지, 요약줄에 '⚠ 번호 확인 n · 문안 미작성 n · 선택 미포함 n · 조문화 대상 아님 n', 문서 내려받기 3곳(`docGateOk`)에서 확인 후 진행.
- 2026-07-27: **[fund.html] 서식 미리보기·인쇄를 A4 세로로 통일** — 사용자 요청("서식자료집은 A4 기준, 미리보기 형태로 관리"). 규격 상수 `A4_W/A4_H/A4_PAD`(210×297mm, 여백 20mm) 도입. ①**화면 미리보기가 A4 낱장**으로 보이게 — `#doced .a4{width:210mm;min-height:297mm;padding:20mm;box-sizing:border-box}`+그림자·테두리, 배경을 회색(#eef0f4)으로 바꿔 용지가 도드라지게. `_showDocHTML`이 내용을 `.a4`로 감싸고(이미 감싼 경우 중복 방지) `#docstage`에 담아 렌더. ②**배율 자동 맞춤** `fitDocA4()` — 패널 폭에 맞춰 transform scale(측정 시 transform을 먼저 걷어내 실폭 확보), `#docstage` 높이=스케일 높이·overflow로 가로 스크롤 방지, 툴바에 배율 표시 + [⤢ 배율]로 맞춤↔실제크기(100%) 전환(`S.docZoom`), 패널 드래그·창 리사이즈 시 재계산(디바운스). ③**인쇄·PDF도 A4 고정** — `@page{size:A4 portrait;margin:20mm}`, 인쇄 시 `.a4`는 본문폭 170mm·여백 0(용지여백은 @page가 담당)·그림자 제거, 새 창 화면보기엔 용지 모양 유지. ④**전체 초안(인가 6종)은 서식마다 A4 한 장**(`page-break-after:always`, 마지막 장 auto, 장마다 'n/6 · 서식명' 머리표기). 옛 `max-width:760px` 제거(A4와 충돌). HWPX 저장은 이미 A4 세로(`HWPX.setPage({landscape:false})`, 59528×84188 HWPUNIT) 확인, 엑셀은 원본 법정서식(A4) 그대로라 안내문만 보강. 검증: A4 규격·CSS 문법 15항목 통과(실제 생성 CSS를 node로 실행 검사), 파서 회귀 14/14, node 문법 통과. ⚠브라우저 도구가 응답하지 않아 시각 확인은 사용자 화면에서 필요.
- 2026-07-27: **[취업규칙] 신구대조표 항(①②③) 줄맞춤 + 한글 12pt 통일 + 제목 가운데** — 사용자 지적("변경 전 1항과 변경 후 1항의 줄 위치가 뒤죽박죽"). ①`splitParas`/`alignParas`로 조문을 **항 단위로 쪼개 같은 항끼리 같은 행**에 배치, 한쪽에만 있는 항은 반대편에 〈신설〉·〈삭제〉 표기. 화면 편집표는 한 `<td>` 안 **2열 CSS 그리드**(`.djgrid`)로 묶어 좌우 높이·폭이 자동 일치(측정: 모든 항 쌍 top/height/width 차 0px), 각 항 칸을 개별 편집 → 입력 시 순서대로 다시 합쳐 저장. 문서는 `daejoRowsAligned()`로 같은 행 구조를 그대로 출력. ②열폭 `daejoCols()`를 **변경 전/후 동일 비율**로(세로 1.4/3.1/3.1/1.9). ③**[전체 미반영]이면 변경이유 열 자체를 제거**(화면 헤더·셀·문서 열 모두 3열로, 남는 폭은 본문에 배분). ④**한글 글자크기 전부 12pt**(header.xml charPr height 전부 1200, `CH=1200`·`LH=1560`·lineseg vertsize/baseline/spacing을 글자크기에서 파생, `CP` 프리셋을 12pt로 통일) — hwpx_gen.js?v=8. ⑤신구대조표 제목·부제를 `docTitle/docSub`로 바꿔 **가운데 정렬**. 검증: 한글 COM으로 실제 열기 3종 OK → PDF 렌더 확인(A4 595×841 세로/841×595 가로, 전체 글자 12.0pt, 제목 중심 297.5=지면중앙, ①y=485·②y=594·③y=733 좌우 동일), Excel COM → PDF에서도 ①y=313·②y=392·③y=485 좌우 동일·B/C 열폭 동일(29.4/29.4)·A4 세로 1페이지.
- 2026-07-27: **[fund.html] 코덱스 3차 검증 — 21건 지적 중 10건 수정** — 두 계정 동시편집으로 생긴 중복·충돌·경합을 codex(read-only)로 점검. **수정**: ①`imgCanvas` 대형 사진 축소식 오류(`sc<1?1:1.6`이라 4000px 사진이 안 줄어듦) → 폭 기준 재계산 ②`ocrCanvases` 인식 실패 시 `terminate()` 누락 → WASM worker 누수, 성공·실패 양쪽에서 종료 ③`pdfText`/`pdfPageCanvases` `doc.destroy()` 누락 → 자원 반납 추가 ④**엑셀 업로드 기금 귀속 경합** — `_sitePend`에 읽기 시점 기금 고정(`fid`), 커밋 시 그 기금에만 등록 ⑤중복 검사를 캐시가 아닌 **서버 최신값**으로(동시 등록 시 중복 방지) ⑥**OCR 결과 기금 귀속** — `_docFid` 도입, 판독 중 기금 이동 시 결과 폐기·반영 차단 ⑦`_deleteSetupFund` 청구 삭제 실패를 삼키고 성공 표시 → '일부 실패' 경고로 분리 ⑧`renderBilling`에 삭제보관 기금 제외(다른 화면과 기준 통일, 제외 건수 표기) ⑨`saveInfo` 응답 콜백의 `S.fundId` 재참조 경합 → 저장 대상 고정 ⑩`showSetupFunds`의 완전삭제 경로가 소프트삭제 정책 우회 → `trashFund`로 교체, 죽은 `backToFund` 제거. **미수정(판단)**: saveInfo/saveSite/saveSubsidy 전체 `set/update` 덮어쓰기(동시편집 병합)는 단일 사용자 환경이라 보류, 청구 고아 레코드는 확인 필요해 남김, `subsidyDocsPanel` url 스킴 검증은 Storage 경유라 위험 낮음. 검증: node 문법 + 파서 회귀 14/14 통과.
- 2026-07-27: **[fund.html] 스캔 문서 OCR 추가 (tesseract.js) — 실측 10/10** — 앞 단계에서 텍스트PDF만 읽던 것을 스캔본까지 확장. **엔진 선택 근거**: 배포 페이지가 공개(github.io)라 API 키를 심으면 그대로 노출 → 키 없는 **클라이언트 OCR(tesseract.js kor+eng)** 채택, 최초 1회만 CDN 지연 로드(~15MB). **전처리가 정확도를 가름** — pdf.js로 3배(≈216dpi) 렌더 후 `_binarize()` 흑백 2치화(등기부 노란 보안무늬 제거). 실측(node에서 동일 엔진 + fund.html 실제 파서로 측정): 고유번호증 2건(고유번호·법인등록번호·대표자·소재지)·인가증 스캔(인가번호·대표자)·등기부 2건(법인성립연월일) = **10/10 일치**. 2치화 전 등기부 raw는 0/2였음. 파서 보강 3건: ①`_cleanName`을 parseInka에도 적용(스캔 이름 뒤 잡음 "…기금3호겨이|저쁘무호Arp1…" 제거) ②'대표자'가 '내표자'로 오독돼 `[대내]표자`로 완화 ③주소 꼬리 '발급사유 신규' 제거(OCR이 '발 = 사 유'로 깨뜨림). 텍스트PDF 경로 회귀 14/14 유지. UI: OCR 사용 시 주황 배지 **"OCR — 오독 가능, 꼭 확인"** + 진행률, 반영 전 사용자 확인 필수. ⚠등기부 법인등록번호는 작은 숫자+무늬라 실패율 높음(고유번호증에서 대신 취득 가능).
- 2026-07-27: **[취업규칙] 작성기+규정관리 통합 — 사업장 대시보드 신설** — 포털 타일 2개(규정관리·취업규칙 작성)를 **「취업규칙 관리」 1개로 통합**. rules.html 상단에 **사업장 규정 현황 대시보드** 신설: 보관함(rules_mgmt/archive)+작성기 문서(chwieop/docs)를 **사업자번호 우선 키**로 묶어 사업장별 4단계 진행(작성중→검토완료→개정중→신고완료)을 진행바·다음 할 일과 함께 표시, 검색·접기/펼치기·상태별 집계 제공. 행 클릭 시 단계에 맞게 이동(보관함 기록은 대조표로, 미작성/작성중은 작성기로 사업장 정보 인계 pureun_chwieop_open_v1). [＋ 신규 사업장 규정 만들기]로 표준취업규칙 작성 바로 시작, [📄 기존 규칙 업로드]도 대시보드에서. 표준 템플릿(16장 52조)을 **rules_std.js로 분리해 두 화면이 공유**(chwieop는 RULES_STD 참조). 검증: 4개 사업장 상태 판정·정렬·검색·보관함 연결·인계 데이터 모두 정상.
- 2026-07-27: **[fund.html] 서류 끌어놓기 자동입력 + 참여사업장 엑셀 업로드(드래그 기본)** — 사용자 요청("인가증·등기·고유번호증 OCR, 참여사업장 엑셀, 모든 업로드는 드래그"). **먼저 실물 확인**: 고용노동부 **인가증은 텍스트 PDF**(전자문서)라 OCR 없이 100% 읽힘 / **등기부·고유번호증은 스캔 이미지**라 텍스트 없음 → 하이브리드로 설계. ①`dropZone()` 공용 드래그앤드롭(드롭+클릭선택, `_dzHandler` 레지스트리) ②`pdfText()` pdf.js로 **좌표 정렬**(y↓,x→) 추출 — 기본 순서로 뽑으면 표 레이아웃에서 글자가 섞여 파싱 실패(경기1호 실증) ③파서 3종(`parseInka/parseCorpReg/parseTaxId`) — 인가증은 라벨과 값이 엇갈려 나오는 구조("명칭: {이름} {주소앞} 2. 소재지: {주소뒤}")여서 **구간 절단→라벨 제거→시·도+시/군/구 위치로 이름/주소 분리**, 이름에 '충남' 등이 있어 **마지막 매치**를 주소 시작으로, "기금호1"→"기금1호" 보정 ④기금 정보 탭에 3개 드롭존+판독결과 칩+[아래 칸에 반영](저장은 별도) ⑤참여사업장 탭에 엑셀 드롭존 — 헤더 자동 인식(상호/대표자/사업자번호/업종/상시근로자/소재지, 합계행 제외), 미리보기 8행, 사업자번호 중복 건너뛰기 후 일괄 등록. 검증: **실제 인가증 3건 × 5필드 = 14건 전부 정확**(fund.html 파서 함수를 추출해 node 실행). ⚠스캔 문서는 자동판독 불가 안내만(OCR 엔진은 사용자 결정 대기 — Tesseract 무료·정확도낮음 vs API 유료·키노출 이슈).
- 2026-07-27: **[fund.html] 설립중 삭제 버튼 + 🗑 삭제 보관함(소프트 삭제)** — 사용자 요청. 기존엔 완전삭제만 있고 기금 상세 깊숙이 있었음. ①**설립중 목록 행에 🗑 버튼** 신설(`fundRow(f,no,mode)`·`fundTable(list,edit,mode)`에 mode 추가) → `trashFund()`가 `funds/{fid}`에 `deleted/deleted_at/deleted_by`만 표시(**데이터 미삭제**). ②**홈에 '🗑 삭제 보관' 탭** 신설(보관분 있을 때만 표시) — 삭제일·삭제자·삭제 전 상태 열, [↩ 복원]·[완전 삭제]. `restoreFund()`는 플래그 제거로 원상복구. ③**완전 삭제는 보관함에서만** — `cancelSetup()`도 보관 방식으로 변경(기존 하드삭제 문구·동작 교체), `_deleteSetupFund`는 서버에서 `deleted||설립준비` 재확인 후에만 진행하고 6단 중첩 프라미스를 **평탄한 체인으로 재작성**(중첩 오류로 문법 깨졌던 것 정리). ④**보관분 전역 격리** — 열거 8곳에 `isTrashed()` 가드(createFund 중복명·bulkRegistry·bulkTax·subBoardPanel·addBilling·renderDocsBoard·showSetupFunds·renderHome). `nextFundId`만 의도적으로 포함(ID 재사용 방지). ⑤기금 상세에 삭제보관 배너+복원 링크. node 통과.
- 2026-07-26: **[fund.html] 서식 모드를 '상태값'이 아니라 '렌더 위치'로 결정 — 자료실 오염 차단** — 사용자 지적("기금 현황에서 서식 클릭 시 자료실로 이동해 혼란")은 직전 세션의 임베드 구조(`_formsHostEl`/`#fundDocsHost`/`goTab('docs')`)로 이미 해결된 상태였음(라이브 확인). 다만 남은 허점: `fundMode`를 `S.formsCtx==='fund'`로 판정해 **뒤로가기로 자료실(단독 렌더)에 들어오면 기금 모드가 남아** 자료실에 특정 기금 서식+[← 기금 상세로]가 뜰 수 있었음. → `fundMode = EMB && !!funds[S.formFund]`로 바꿔 **임베드=기금 실데이터 / 단독=전사 표준서식**이 위치로 확정되게 하고 `S.formsCtx`는 파생값으로 강제. 자료실 헤더의 기금 모드 분기(fsel)·크럼 분기 제거. 역할 분리 최종: **기금 상세 📑서식 탭**=그 기금 데이터 채움·개별 저장 / **사이드바 📚서식 자료실**=견본·공통 기본서식·원본 등록. node 통과.
- 2026-07-27: **[fund.html] 변경 이벤트 단계별 서식 연결**(a979c39) — 사용자 질문: "이미 설립된 기금에서 변경등기·인가신청이 생기면?" → 답: **설립 탭이 아니라 [🔁 변경 이벤트]에서 처리**(정관변경/임원변경/주소이전/사업장탈퇴/해산 유형별 체크리스트 자동생성, 기존 구현). 부족했던 점(각 단계에서 서식으로 갈 수 없음)을 보강: `EVT_LINKS` 규칙표+`_evtBtn()`으로 단계 라벨 키워드 매칭 → 바로가기 버튼. 별지11호→④운영 정관변경 서식, 별지10호→재산변동상황보고서, 변경·해산·청산종결등기→②등기 서식, 취임승낙서/협의회명부/회의록/정관 편집, 소재지·등기완료·세무서→기금정보 탭, 탈퇴→참여사업장, 잔여재산→회계결산, 스캔보관→서식 탭. 별지14호는 공식 원본 HWP 안내(생성서식 없음). 규칙 우선순위 조정("변경 정관·등기부 스캔 보관"이 정관 편집으로 가던 것 수정). 설립 진행 EST_LINK와 동일 패턴, 체크박스 충돌 방지(stopPropagation). node 검증 9건 통과(전 유형 34단계 매칭). 미연결 7건은 "사유 확인·인가증 수령" 등 단순 확인 단계로 버튼 불필요.
- 2026-07-26: **[fund.html] 서식 구조 3단계 분리**(0c7536b) — 사용자 지적: 기금에서 📑서식 누르면 화면이 자료실로 넘어가 "새 기금 등록"과 헷갈림. ①**기금 안 서식 = 기금 상세 탭 내장**: renderForms에 임베드 모드 추가(`S.formsHost`→`_formsHostEl/_formsEmbedded/_formsRerender`), 기금 상세 `tab='docs'`에서 `#fundDocsHost`에 렌더 → 화면 이동 없이 기금 헤더·탭바 유지, 사이드바도 "기금 현황" 활성. 기금 밖으로 나가면 임베드 해제. ②**서식 자료실 = 전사 표준서식 전용**: 견본 편집·원본 .hwp/엑셀 등록·유형 공통본 저장만. 진입 시 임베드/선택 초기화, 안내문 명확화. ③**🗂 서식 현황 대시보드 신설**(`renderDocsBoard`): 기금×서식 매트릭스(🟢개별저장본 📎원본HWP ⚪미작성 ·해당없음), 단계 탭 6종·유형 필터·"미완성만"·기금별 N/M, 칸 클릭→그 기금 📑서식 탭 해당 서식으로 이동(`openFundDoc`, 단계키 매핑 kinds→inka/sub→subsidy). `doc_edits` 1회 캐시(`S._dbEdits`)·저장/삭제 시 무효화. node 검증 12건 통과.
- 2026-07-26: **[fund.html] 코덱스 전체 검증 → 17건 수정**. 1차(22건 지적 중 12건 수정, ad135db): **저장형 XSS 차단**(`sanitizeDocHTML` — script/iframe/on*/javascript: 제거, _showDocHTML·printDoc 적용), **createFund 동시등록 덮어쓰기**→transaction 선점, `_deleteSetupFund` 서버 setup_stage 재확인+삭제범위 확대(welfare/events/txns/billings), `toggleSetup` 유령기금·null 가드, `saveSubsidy` 저장시점 기금/연도 고정, popstate 미저장 확인(무경고 폐기 제거), `_loadDocInto` 토큰으로 늦은응답 차단, 서식편집 markDirty, `resetDoc` 로딩중 오삭제 방지, 일괄채움 적용직전 재확인(`_applyBulkOffice`), `finishSetup` 서버 재검증, subBoard 설립중 제외, reg_license 지자체=시·군·구청(`_localGovFromAddr`), patchSub/saveSubsidy 0 저장, 중복명 공백정규화·nextFundId 9999+·mgr_subs 빈배열 폴백(`_hasMgrStruct`). 2차(5건, ea167ab): **축약 시·도 주소 인식**(`_addrParts` 공용 파서 — "충남 천안시" 등이 등기소·세무서 매핑 안 되던 문제), renderForms `_hwpChk` 캐시 무효화, loadStaff `data/user_dir` 우선+빈결과 캐시 안 함, Storage 고아파일 삭제, pickHwpBulk 실패 집계. node 단위검증 16건 통과. 미수정(정보): check-then-write는 Firebase 규칙/트랜잭션 없이는 완전 원자화 불가 — 단일 사용자 환경이라 현 가드로 충분.
- 2026-07-26: **[fund.html] 기부금영수증 서식 제외** — ④운영의 `ops_donation_receipt`(기부금영수증)·`ops_donation_list`(발급명세서) 제거. 원본이 **타 법인(고려인삼연구 사내근로복지기금) 서류**로 우리 서식이 아니었음(대표자 노옥희 등 제3자 정보도 있었음 → 앞서 마스킹했으나 서식 자체가 부적합). DOC_OPS 5종→3종(재산변동상황보고서·협의회 회의록 2종), build_forms.py·pickHwpBulk 규칙에서도 제거. 결과 31종 · 151KB. 검증: 전 그룹 100% 커버·PII 0건·'고려인삼' 잔존 0·node 통과.
- 2026-07-26: **[fund.html] 사업계획서 중복 통합** — ①인가 `bizplan`과 ⑤지원금 `sub_bizplan`을 원본 대조한 결과 **같은 서식**(문단 일치율 78.8%, 차이는 빈양식 "20 년/00 은행" ↔ 이비공동기금 2020년 작성예시 "2020년/신한은행/실제금액"). **빈 양식인 `bizplan`을 정본**으로 삼고 DOC_SUB에서도 같은 키를 참조(`['bizplan','사업계획서 (①인가와 같은 서식)','공동']`) → 한 곳에서 고치면 양쪽에 반영, 저장본도 공유. build_forms.py에서 sub_bizplan 변환 제외, pickHwpBulk 매칭규칙에서도 제거. 결과 33종(−1) · fund_forms.js 176KB→157KB. 검증: ⑤지원금 9/9 유지·전 그룹 100% 커버·PII 0건·node 통과.
- 2026-07-26: **[fund.html] 나머지 서식 전부 원본 HWP 변환 — 33종 100% 커버** — 앞서 7종만 변환했던 것을 `C:/Users/fair0/Documents/공동기금_서식정리/`(이전 세션이 이비공동기금 zip에서 추출)의 원본으로 확장. ②등기 8 + ③고유번호증 4 + ④운영 5 + ⑤지원금 9 + ⑥인센티브 1 = **27종 추가**, 총 34키(charter_sane 포함). 커버리지 검증: 앱 DOC_* 33종 전부 원본 변환 적용, 부실(<100자) 0. **⚠중대 — 개인정보 대량 혼입 발견·제거**: 원본이 '작성 예시'라 13종에 이비공동기금 실데이터가 박혀 있었음(임원 실명 박준희·전석정·노옥희, **개인 자택주소 2건**, 전화 02-6959-8865, 법인등록번호 250171-0037919, 사업자번호 2건, 기금 인가번호, 고려인삼연구). build_forms.py에 SCRUB 단계 추가 — 정규식으로 상세주소·전화·휴대폰·법인번호·사업자번호·인가번호·주민번호 마스킹 + 실명/상호 치환. **함정 2개**: ⓐ `\b0\d…` 전화 패턴은 "전화02-6959-8865"처럼 한글에 붙으면 단어경계 불성립 → `(?<!\d)`로 교체, ⓑ 시·군·구 없이 "경기도"만 든 칸은 주소 규칙에 안 걸림 → `>시도명</` 전용 규칙 추가. 전수 PII 스캔(전화/휴대폰/주민번호/상세주소/사업자번호/법인번호/계좌) **0건** 확인 후 커밋. node 검증 통과.
- 2026-07-26: **[rules.html] 한글·엑셀 출력 근본 해결 + A4 세로 기준 통일** — 이 PC에 한글 오피스 2022가 설치된 것을 발견해 COM(HWPFrame.HwpObject)으로 열기 성공/실패를 판정 → **생성 hwpx를 한글이 아예 열지 못하던 것**이 원인으로 확인(정상 문서는 OK, 생성물은 FAIL). 바이트 비교로 특정한 4건 수정: ① **zip UTF-8 파일명 플래그(0x0800) 제거**(결정적 원인 — 한글이 거부), ② 전부 STORE → 실제 한글 저장본과 동일하게 mimetype·version·PrvImage만 STORE·나머지 DEFLATE, ③ Preview/PrvText.txt·PrvImage.png 추가 + 엔트리 순서 일치, ④ charPr 삽입 위치를 목록 끝으로(id 순서 0,7,1,2… → 0..7). 엑셀은 별건: **행 높이 미지정으로 wrapText라도 한 줄로 표시돼 조문이 잘리던 것**을 글자폭 기반 줄 수 계산으로 행 높이 명시. 사용자 요청에 따라 **모든 산출물 A4 세로 기준**(대조표도 가로→세로, 열비율 1.4:3.0:3.2:1.9). 검증: 한글 PDF 변환·렌더 이미지 확인(표 폭 146.7mm/본문 149.9mm, 4열·머리행 서식 정상), 엑셀 PDF 변환 확인(A4 세로 1폭·조문 전문 표시). 캐시 hwpx_gen v6·xlsx_gen v2.
- 2026-07-26: **[fund.html] 기타 양식.zip 검토 → 기금출연확인서 엑셀 서식 추가** — `8. 공동기금\기타 양식.zip`(5개) 점검 결과: 설립인가신청서·지원신청서는 이미 등록됨(TPL.setup/subsidy), 급여위임계약서는 기금 무관, **심사위원회 평가표는 외부통합문서 참조(`[1]기금법인정보!`) 의존이라 단독 사용 불가**(공단 심사용이라 우선순위 낮음 → 보류), **기금출연확인서.xlsm만 유효**해서 채택. 원본에 충남8호 참여사 38곳의 대표자·근로자 **성명+생년월일(PII)**이 있어 그대로 못 씀 → `tools/make_blank_contrib.py`로 실데이터 343셀 제거 + 기금명을 `{{기금명}}` 토큰화한 **빈 양식**을 로컬 templates/에 생성(잔존 실데이터 0건 검사 통과, 병합 23개·VLOOKUP 수식 보존). 앱: `TPL.contribx` 추가(①인가 탭 카드, hint 표기), `fillContribXls()` — [기업정보] 시트를 참여사업장(회사명·대표자·상시근로자수)으로 채우고 출연금=근로자수×단가(`fund.contrib_per_worker`, 기본 400,000), 확인서 시트의 {{기금명}} 치환. 생년월일 칸은 PII 미저장 방침대로 공란(엑셀에서 수기). 검증: fund.html에서 실제 함수 추출해 node 실행 — 단가 반영·수식 보존·토큰 잔존 0 전부 통과. ⚠원본 .xlsm/빈양식 모두 저장소 밖(로컬 templates/, gitignore).
- 2026-07-26: **[fund.html] 원본 변환 서식 정밀도 개선(칸 너비·중첩표 순서)** — 1차 변환본이 "뒤죽박죽"이라는 지적 원인 2가지 해결. ① **칸 너비 미복원** — 모든 열이 균등폭이라 글자가 세로로 깨짐 → 셀 레코드 offset 16의 width(HWPUNIT)를 읽어 `<colgroup>` 백분율로 복원(별지7호 19열이 원본 비율대로, 총 177mm=A4 여백폭 일치 확인). ② **표 순서 역전** — 처리절차표가 맨 위로 올라옴. 원인은 별지7호가 *표 안에 표*(본문 lvl2 표 안에 처리절차 lvl4 표)인데 중첩을 형제로 뽑아 스택 역순 출력한 것 → 표를 닫을 때 부모 셀(`pcell`)로 되돌려 넣고 렌더러를 재귀화(cell_html/render_table). 검증: 최상위표 1·중첩표 1, 등장순서 별지7호→접수번호→기금법인→대표자→설립준비위원회→분사무소→첨부서류→수수료→처리절차(원본과 동일). 변환기를 `fund-erp/tools/`(hwp2html.py·build_forms.py·README)에 보관 — 재생성 `python fund-erp/tools/build_forms.py`, 원본 .hwp는 로컬 전용. node 문법 통과.
- 2026-07-26: **[fund.html] ★근본원인 해결 — 앱 서식을 원본 .hwp에서 실제 변환** — 사용자 반복 지적("폴더 한글 서식과 왜 다른가")의 진짜 원인 규명: **앱 서식(docBody)은 내가 손으로 쓴 근사본이고, 폴더 .hwp는 한 번도 읽힌 적 없음**(등록한 .hwp도 rhwp 편집기 열람용일 뿐 미리보기와 무관). 증거대조 — 설립합의서 실제=「○○주식회사…임시 노사협의회」+서명란4개 vs 앱=제1~4조 창작 / 회의록 실제=회순·경과보고·추진일정표13행·의안1~4호 vs 앱=안건1~5 창작. **한글 인식 문제 아님을 실증**: olefile+zlib으로 HWP5 OLE2 파싱, 레코드 트리(PARA_TEXT 67 / LIST_HEADER 72 / TABLE 77)에서 문단·표 격자 복원 성공(인가신청서 35×19·19열 별지7호 격자, 체크박스, 처리절차 흐름표까지). 해결: `scratchpad/hwp2html.py`(변환기)+`build_forms.py`로 **핵심 7종**(인가신청서·설립합의서·정관(공동)·정관(사내)·회의록·출연확인서·사업계획서)을 `fund_forms.js`(62KB, window.HWP_FORMS)로 생성, fund.html이 로드 후 `hwpFormHTML()`로 {{FUND}} 바인딩+라벨 옆 빈칸 자동채움(FORM_FILL)+사내/공동 체크박스 전환, docBody가 이를 우선 사용(없는 서식은 기존 초안 폴백). 실데이터 혼입 0건 검사 통과(빈 양식 ○○/0000만). dgDocCssIn 조밀표 대응(table-layout:fixed·padding 축소). node 검증 9/9. ⚠️정밀 인쇄·제출은 여전히 엑셀 채움/HWP 편집 권장.
- 2026-07-26: **[fund.html] 서식 자료실=샘플 / 기금별 서식=기금 상세로 분리** — 사용자 요청. `S.formsCtx`('library'|'fund')로 renderForms 2모드화. **사이드바 서식 자료실**=신규 설립 샘플 영역: 기금 드롭다운 제거→[공동기금 샘플|사내기금 샘플] 토글(`S.sampleType`, `curForm()`이 가상 샘플 객체 반환→빈 값은 ＿＿ 표시), 저장은 [💾 공동/사내 공통]만(개별 저장·엑셀 채움·바로가기 숨김, `_docCtx.fundRef=null`·`_fillWbBuffer` 가드). **기금 상세 탭바에 [📑 서식]** 추가(`openFundDocs`→formsCtx='fund'로 forms 뷰 진입, [← 기금 상세로] 복귀) — 그 기금 데이터 채움·개별 저장·엑셀 채움·회계/변경/지원금 바로가기 전부 여기서. sidePreviewAll도 유형별 docsFor 적용. node 문법 통과.
- 2026-07-26: **[fund.html] 서식 저장 3계층 — 공동/사내 공통본 분리 저장·출력** — 사용자 요청 "공동기금과 사내기금 별개로 수정·저장·출력". 저장 계층: **개별 기금(doc_edits/{fid}/{kind}) > 유형 공통(doc_edits_type/{공동|사내}/{kind}) > 자동생성(docBody)** — `_docCtx/_loadDocInto/_setDocLayer` 신설, 우선순위 역순으로 렌더 후 덮어씀. 사이드패널·팝업 헤더에 [💾 이 기금](개별 저장)+[💾 공동|사내 공통](confirm 후 유형 전체 기본으로)+계층 배지(개별 저장본/공통 수정본/자동생성). ↺ 되돌리기는 현재 계층 기준(개별→공통→자동생성 순 단계 삭제, 공통 삭제는 danger confirm). 서식 내용 자체의 공동/사내 분기(charterGong/Sane·docsFor 33/22종)는 기존 구현 활용 — 대상 기금을 사내기금으로 바꾸면 사내 서식·사내 공통본이 뜸. node 문법 통과.
- 2026-07-26: **[fund.html] 서식 자료실 6단계·공동/사내 분리(과거자료 zip 기준 재정비)** — 2020년 이비공동기금 zip(HWP 39종)을 단계별로 대조해 누락 서식 19종 추가: ③고유번호증(법인설립신고·사업자등록신청서/임대차계약서/전대 사용동의서/홈택스 이용신청서), ④운영(재산변동상황보고서·협의회 회의록 2종·기부금영수증·발급명세서), ⑤지원금 9종(필요서류·지원신청서·자율점검체크리스트·출연확인서·서약서·복지사업계획서·사업계획서·재산목록표·지급신청서), ⑥공단 인센티브(별지 제11호의2 비용청구서). 서식 정의에 3번째 값=적용 기금유형 추가(docsFor) → **공동 33종 / 사내 22종**으로 갈라 표시(설립합의서·지원금·인센티브는 공동 전용, 단계 탭도 사내에선 ⑤⑥ 숨김), 패널마다 '○○기금 기본서식' 칩 표기. pickHwpBulk 파일명 매칭 규칙 확장 — 추출 파일 31개 100% 자동 매칭(미매칭 0) 검증. 원본 HWP는 실데이터 포함이라 저장소 밖 `C:/Users/fair0/Documents/공동기금_서식정리/`에 단계별로 추출(회사명 흔적은 파일명에서 정리). node 문법 통과.
- 2026-07-26: **[fund.html] 엑셀 화면 미리보기 + HWPX 저장 안내 가드** — ① fillForm 채움 로직을 `_fillWbBuffer(key)`로 분리, `previewExcel(key)`: 채워진 엑셀을 SheetJS(sheet_to_html)로 **오른쪽 패널에서 시트 탭별 읽기전용 미리보기**(값·병합 확인용, [⬇ 이 파일 받기] 동반). 인가 배너·설립6종/지원신청서 카드에 [👁 엑셀 화면에서 보기] 추가. ② 사용자 질문 "한글에 원본이 왜 안 나오나" 대응: docToHwpx에 confirmM 가드 — HWPX는 문단·표 재조립이라 별지 격자·도장란 재현 불가, 원본 그대로는 엑셀채움/HWP편집 안내 후 진행. node 문법 통과.
- 2026-07-26: **[fund.html] 서식 CSS 전역 누출 수정(화면 깨짐 근본 원인)** — dgDocCss()의 `body{max-width:760px;margin:0 auto}`·`h1{letter-spacing}` 등 전역 선택자가 사이드패널 <style>로 상시 삽입되며 **앱 전체가 760px로 압축·자간 벌어짐**(사용자 "또 이상하다" 스크린샷 증상). 해결: `dgDocCssIn()`(#doced 스코프판) 신설, `_showDocHTML`이 들어오는 html의 <style> 전부 제거(저장된 옛 서식에 박힌 전역 CSS도 정화) 후 스코프판 주입, sidePreviewAll도 스코프판. printDoc(새창)은 기존 전역 dgDocCss 유지(정상). node 문법 통과.
- 2026-07-26: **[fund.html] 서식 이름 클릭=오른쪽 미리보기(근본 재설계)** — [👁 미리보기·편집] 버튼 제거, **서식 행(이름) 전체 클릭** → `selectForm(kind)`(S._sideKind 저장→renderForms→끝에서 sidePreview 자동 호출). 선택 행 하이라이트(warn-bg+좌측바+"▶ 오른쪽에 표시 중"), 재렌더·탭전환·기금변경 후에도 선택 서식 오른쪽 유지. HWP 버튼들은 stopPropagation으로 행클릭과 분리. ops정관·지원신청서도 selectForm. node 문법 통과.
- 2026-07-26: **[fund.html] 서식 사이드패널 보강 + 좌우 드래그 리사이저** — sidePreview: 기금 미선택 시 패널에 안내 표시, **생성서식(docBody) 먼저 렌더 후 저장본 덮어쓰기**(doc_edits 조회 실패해도 서식은 보이게), 폴백 순서 정리. 좌우 2단 사이 `#formGutter` 추가 → `startFormDrag/onFormDrag/endFormDrag`로 좌측폭(`S.formSplitW`) 마우스 드래그 조절(재렌더에도 유지). 사용자 보고 "미리보기 클릭 시 우측 미표시" 대응 — 코드는 배포본에 정상 존재 확인(라이브 grep), 런타임 견고화로 처리. node 문법 통과. 사용자 하드리프레시 후 재확인 필요.
- 2026-07-26: **[fund.html] 서식 옆에서 보기(사이드 편집)+새기금 일괄+엑셀 전면화** — 서식 자료실 2단 레이아웃(좌 목록/우 sticky `#formSide`). 서식 [👁]→팝업 대신 `sidePreview(kind)`가 우측 패널에 렌더(contenteditable 인라인 편집), `#docwrap/#doced` 재사용해 저장(saveDocEdit)·인쇄(printDoc)·HWPX(docToHwpx)·원본(resetDoc) 그대로 동작. `sidePreviewAll()`=인가 6종 전체 초안 쌓아 미리보기·일괄 인쇄. 인가탭 상단 엑셀 전면화 배너([⬇ 인가 6종 엑셀 한번에 채워받기]=fillForm('setup'), [🖨 전체 초안 미리보기], 설립중 기금 배지). 방침 확정: **공식 제출=엑셀 채움(형태 100%)**, HTML=빠른 편집, HWP(rhwp)=등기 등 엑셀원본 없는 서식. HWPX 자동생성은 별지 격자 깨짐이라 참고용. ops/subsidy 미리보기도 sidePreview로. node 문법 통과.
- 2026-07-26: **[fund.html] 서식 자료실 생애주기 4탭 재편** — 서식 자료실을 `S.formPhase` 탭(①인가 ②법인설립 ③운영 ④지원금)으로 재구성. 서식마다 [👁 미리보기·편집(HTML)]+[📄 HWP 편집(원본 .hwp)]을 **한 줄(formRow)** 로 통합. ①인가=DOC_KINDS 6종+설립6종 엑셀(tplCard setup), ②법인설립=DOC_REG 8종, ③운영=현행정관 편집 + 회계·결산/변경이벤트/연간일정 **바로가기**(`gotoFundTab` → S.view='fund'+S.tab+route()), ④지원금=지원신청서 편집+지원금탭 바로가기+지원 엑셀(공동만, 사내는 안내). node 문법 통과. 운영상황보고서·결산서는 재무데이터 원천이 회계탭이라 중복생성 없이 링크만.
- 2026-07-26: **[fund.html] 서식 자료실 실제 HWP 편집(rhwp 에디터) 추가** — 사용자 제공 rhwp(오픈소스 Rust+WASM HWP 에디터) 검토. 공개 SDK는 뷰어/에디터(iframe)+export(.hwp/.hwpx)만, **필드 자동채움 API 없음** → "원본 .hwp 등록→브라우저 편집→저장" 방식 채택(사용자 선택). 새 '📄 실제 HWP 서식' 패널: 서식별 원본 .hwp를 IndexedDB(`hwp:<kind>`)에 등록(개별+파일명 자동매칭 일괄 `pickHwpBulk`), [📄 편집]→모달에서 `@rhwp/editor`(esm.sh @0.8.0) 동적 import→`createEditor`(iframe studio)→`loadFile`→`exportHwp/Hwpx` 다운로드, 우측 기금데이터 참고패널(수동 입력). 의존성 실재 확인(studio 로드 OK·esm.sh 정상). **이 방식은 위 2026-07-25 docToHwpx(HTML→HWPX 자동채움)와 상호보완** — 자동채움은 격자·도장란 재현 불가, 이쪽은 원본 서식 100% 그대로. ⚠외부 iframe 편집기 의존(자체호스팅 여지), 실템플릿 load/export E2E는 로그인 세션에서 사용자 검증 필요(샌드박스 localhost 제약). node 문법 통과.
- 2026-07-25: **[fund.html] 생성 서식 → 한글(HWPX) 저장 추가** — 서식 자료실 미리보기 팝업에 [📄 한글(HWPX)] 버튼 신설(`docToHwpx`). 화면에서 contenteditable로 편집한 내용 그대로 문단·표를 HWPX로 조립해 내려받는다(한글 프로그램 설치 불필요). 저장소 공용 `hwpx_gen.js` 로드(rules.html과 동일 모듈). h1~h3=제목(굵게), p=문단, table=표(열 균등분할, 병합셀은 열 수 정규화로 채움), 표 안 문단은 표에서 함께 처리. 파일명 `{기금약칭}_{서식명}_{날짜}.hwpx`. 검증: node 문법 통과 + 브라우저 실검증 — 유효 ZIP(PK, 필수 8엔트리), 한글 텍스트 5/5 포함, **rhwp 0.7.19 엔진 파싱 성공(A4 1쪽, <text> 29개·표 괘선 렌더 확인)**. ⚠️ 한계: 문단+표 조립이라 관공서 별지서식의 정밀 격자·도장란은 재현 못 함 → 양식 그대로 제출해야 하면 기존 [🖨 인쇄/PDF] 또는 원본 엑셀 채움(설립6종·지원신청서) 사용. 인쇄·엑셀 경로는 그대로 유지(선택지만 추가).
- 2026-07-23: **[fund.html] 서식 자료실 폴더 번호순 재정렬** — `03_과거자료\설립관련과거자료` 실폴더 서식 번호를 확인해 생성서식을 그 순서로 재배열. ①노동부 인가서류(1.설립인가신청서 2.설립합의서 3.정관 4.설립준비위원회 회의록 5.기금출연확인서 6.사업계획서), ②설립등기서류(1.특수법인설립등기신청서 2.취임승낙서 3.협의회명부 4.인감·개인신고서 5.인감대지 6.인감카드(재)발급신청서 7.위임장 +등록면허세). docBtns 자동 번호(numbered 인자), 섹션 헤더에 폴더 출처 표기. 14개 서식 전부 previewDoc(contenteditable) 미리보기·수정 기능 기존 구현 확인. node 문법 통과. (원본 엑셀 등록칸=설립6종/지원신청서 별도 유지.)
- 2026-07-23: **[fund.html] 홈 '설립중' 별도 탭 분리** — setup_stage='설립준비' 기금을 지역/공동/사내 운영 목록에서 빼내 **🏗 설립중 탭**으로 분리(유형별 하위그룹). 상단 '운영 N'은 설립완료(live)만 집계, 미완비(incN)에서도 설립중 제외(신규라 당연히 0/5). 상단 🏗설립중 배지 클릭=설립중 탭 이동. renderHome: active→setup/live 분리, catSecs 공용화(past·setup 공유), 탭 가드에 setup 추가. node 문법 통과. (App Check 403은 콘솔 도메인 미등록 이슈로 코드 정상·모니터모드라 무해, ㉕키 인라인은 push/FUND-NNNN이라 위험 없어 미변경으로 종결.)
- 2026-07-23: **[데이터] 미완비 기금 인가정보 판독 반영** — 03_과거자료 인가증·법인등기부 스캔을 PyMuPDF 렌더→비전 판독하여 17개 기금의 인가번호·인가일·설립등기일(일부 법인등록번호)을 fund.db에 채움(빈 항목만, 기존값 보존). 충남 1·2·3·6·8·9호, 경기 1·3·4호, 참살이·이비·안전공사·청신·배경·일원·가치·현재기업. 인가일은 인가증 발급일 또는 등기 '설립인허가연월일', 설립등기일은 등기 '법인성립연월일' 기준. `backups/fund_funds_full_export.json`(42건 전체 컬럼) 신규 생성. 잔여 미완비 15개는 스캔 부재(사용자 원본 필요), ⚠청신 인가번호 판독 불명확. Firebase 미반영(로컬 fund.db만) — task#12 이전 또는 사용자 수기 입력 필요.
- 2026-07-22: **[fund.html] 계정과목 확정**(c5a3f60, 코덱스 ⑼) — 목적사업비 계정 3종 추가(모성보호비·근로자의날행사비·근로복지시설비), PURPOSE_ACCTS 단일정의(목적사업비 9계정)로 재무제표 목적사업비 집계 일치, WELF_CATS를 계정명과 통일(대부사업은 자산=근로자대부금 유지), ACCT_RULES에 모성보호/근로자의날/복지시설 자동분개 규칙. 다음: 서식 자료실 실검증 → 미완비 14개.
- 2026-07-22: **[fund.html] 코덱스 2차 잔여 4건 수정**(commit 00270a9) — ⒁ 입력 중(#app input 포커스) funds 리스너 재렌더 보류·focusout 시 반영(값 소실 방지, appHasFocus/_pendingRender), ⑿ 상단바 ↻ 새로고침(refreshAll: .once 캐시 초기화), ⑷ 회계 탭 통장잔액 대사 칩(마지막 통장잔액 vs 전기이월현금+입금-출금), ⑻ 시산표 상단 전기이월 행 표시. 남은 코덱스: ⑼계정과목 확장(사용자 결정), ㉕키 검증(위험 낮음).
- 2026-07-22: **[명함첩 pu-cards] ERP 연동 대폭 강화** — ①메인 목록 상단 ERP 유형 탭(자문/급여/노조/기금, 업체관리 실시간 매칭 건수, 활성탭 재클릭 해제). ②ERP 매칭 명함 → '업체관리' 단일 폴더 자동 분류(구 '자문사' 폴더 이름변경, 급여/기금/노조 자동폴더 통합·정리). ③업체관리 status가 종료/해지/비활성이면 '🚪 퇴사' 배지 + '업체퇴사' 폴더로 자동 이동(상태 변화 시 업체관리↔업체퇴사 재배치, 사용자 수동배정·타폴더 이동은 존중, erpAutoFoldered 멱등). ErpMatch.autoFolder/type/left. 커밋 a672f13·8ec5d4a·d392ca2·a84f7fa·ad4433e.
- 2026-07-22: **[포털 enter.html 건의함] 관리 3종** — ①목록 상태 필터(미처리/완료/전체)+검색창(SG._all 재조회 없이 필터, 기본 미처리). ②새 건의 작성 시 제목·내용 키워드로 유사 지난 건의 최대 3건 표시(중복 확인, 클릭 이동). ③대표 답변에 해결 캡처(이미지 최대3장, 클릭/드래그/Ctrl+V) 첨부 → 상세·완료 알림 팝업(sg_resolved)에 노출(요청자 로그인 시 확인). 건의 상세 모달 헤더 드래그 이동. sgRenderList/sgRenderSimilar/sgAddReplyImgs. 커밋 52c19d6·372dc16. ※무인 자동 답변은 Firebase 인증(자격증명) 필요라 미도입 — 대표 승인형(초안+1클릭 게시) 유지.
- 2026-07-22: **[gov-consulting.html 컨설팅 일정]** — ①현장 사진 파일명에서 담당 노무사 이름 제거(개별/수정창/ZIP/갤러리ZIP 5곳): 일터혁신_목동_5회_시작사진. ②유형별 '사진 1장' 모드(유형설정 체크박스 onePhoto, 또는 이름/기관에 '비즈니스지원단' 포함 시 자동) — 스탬프 모달 활동(끝) 슬롯·간격·시각 숨김, 단일 컬럼. 커밋 bb6f828·8311386.
- 2026-07-22: **[pu-erp.html] UI·데이터 다수** — ①법인대시 진행항목 정렬·헤더 틀고정·항목 번호. ②계약 금액 반올림 제거(만·천 정확표기+원 툴팁, fmtManExact). ③업체관리 모바일 카드 '⏸ 중단' 배지. ④**데이터 급감 경고 반복 근본수정(코덱스 교차검증)**: 정규식 id 근사 → _fbLocalCountForShrink 실제파싱, 급감모달 큐잉, '현재유지' 서버 통째덮어쓰기 제거(수동 erpHealServerKey), dbSet 서버확인 실패 fail-closed, 자가진단 스냅샷 sid별 분리. ⑤사무관리 종료↔재무 입금 가시화: 미입금 대기에 '진행상태' 열+3단계 필터(전체/종료·마감/진행중), 사건 종료모달 미입금 잔액 경고. ⑥출금관리 사업주부담분 근로자별 펼침(payroll 재계산), 귀속월≠지급월 배지+기준 토글. ⑦**비관리자 담당자 드롭다운 빈 버그 수정**(getActiveUsers user_dir 폴백). ⑧사이드바 즐겨찾기: 상단 탭전환(전체⇄즐겨찾기)+최대10개+6개이상 그룹 자동전환. 커밋 26c5fb5·906edad·f07ab34·26fa9c1·2f7b7ec·b86eb86·359752d·f64651f·211342f·3e6d2f6.
- 2026-07-22: **생성 서식 편집 팝업 완성**(파이썬 docgen 이식). 서식 자료실 [👁 미리보기·편집]: 설립6종(정관·회의록·합의서·인가신청서·출연확인서·사업계획서)+등기8종(설립등기신청·취임승낙서·협의회명부·인감신고·인감대지·인감카드·등록면허세·위임장)+지원신청서(공동). 팝업 contenteditable 직접 수정→`fund_erp/doc_edits/{fid}/{kind}` 저장(저장본 우선)·[↺원본]·[🖨인쇄]. **정관은 공동/사내 표준양식 분리**(공동 6장42조=배경공동 인가본, 사내 5장37조=참살이 인가본, docBody charter→charterGong/charterSane, fund_type 분기). docgen 헬퍼 dgV/dgWon/dgToday/dgDocCss. 운영상황보고서·결산서는 실재무 필요라 회계·결산 탭에서 생성(중복 안 함). 설립 진행 단계 버튼(openFundForms)→서식 허브 연결. 커밋 29841a6·95100d4·0204031·fb47fd2.
- 2026-07-22: **새 기금 등록 + 설립 진행 대시보드 이식**(파이썬판에만 있던 기능, 인터넷판 미이전분). 사이드바 [＋ 새 기금] → 모달(기금명·약칭·유형·지역·주담당 재직자선택·신규/기존). nextFundId(FUND-000N). 신규(설립부터)=setup_stage='설립준비'→기금 상세 [🏗 설립 진행] 탭(EST_TMPL 12단계 체크리스트·진행률·완료일, 사내는 참여사업장 단계 제외, 모두완료 시 [운영 전환]). 헤더 [🏗 설립 진행] 배지. 커밋 477e122·706ab10. **관할등기소·세무서 자동채움**(소재지 기반, 국세청/대법원 관할 검증): 기금 정보 칸 [🔍 소재지로]+홈 정보채우기 [🏛 등기소 일괄]·[🏢 세무서 일괄](미리보기 후 적용). REGISTRY_MAP/TAX_MAP·guessRegistry/guessTax. 로컬 fund.db는 apply_offices.py로 채움(등기28·세무14 신규). ⚠️인터넷판은 사용자가 일괄 버튼 클릭 필요. 확인필요: 누리공동(칠곡) 세무서 기존값 구미 vs 검증 북대구.
- 2026-07-21: **지원금 하위 탭 통합 + 키 내비게이션** — 사이드바 '지원금 관리' 독립 메뉴 제거, 기금 지원금 탭 안 하위 탭 4종(S.subTab): [📋 신청·정산] [📁 서류함] [📥 신청서 엑셀](공단 원본 IndexedDB 등록+데이터채움 다운로드, fillSubsidyExcelFor) [🗂 전체 보드](subBoardPanel, 연도별 공동기금 전체 인라인 입력·현재 기금 하이라이트·기금명 클릭 전환). patchSub는 S.year 기준. 담당컬럼 주담당/부담당 분리(mgrMainName/mgrSubNames). '← 뒤로' 버튼 제거 → **Esc**(모달닫기→입력해제→뒤로)·**Backspace**(입력 중 아닐 때 뒤로) 키. 홈 '오늘 할 일' 제거. 기금 대장 흡수(정보 채우기 토글). Storage 규칙 fund_erp 게시 완료(버킷 pureun-erp.firebasestorage.app). 커밋 92fd0bc.
- 2026-07-20: 인터넷판 회계 결산(재무제표+별지15호)까지 완성. 코덱스 1차 검증 반영. STATUS 공용화.
- 2026-07-21: 코덱스 2차 전체 재점검(25건 발견) → 회계 정합성 재작성(computeFin 유형별 전 계정 집계·부채/자본계정 반영·대차일치 칩·전기이월에 준비금 추가·별지15호 준비금/기타비용 행·불일치 시 출력 경고), 통장 파싱 6건(합계행 제외·잔액열 오인·다단헤더·날짜 정규화·입출 동시행 분리·같은날 동일거래 키 충돌), import 레이스 2건(기금/연도 경로 고정·서버 최신본 중복검사), 동일 차/대 승인 차단, 로그아웃 시 Firebase signOut, 쓰기실패 catch+복원 일괄, num() 음수/지수 표기. 로컬 파이썬판(accounting.py·docgen.py 결산서 준비금=순이익 임의대입 버그)도 동일 수정. node 단위테스트 23건 통과. 보류 6건은 §4-4. 사용자 실화면 검증 후 홈·대장 호수순 정렬 수정(localeCompare numeric — 충남 10호가 1호보다 앞에 오던 문제). 참고: 홈 '미완비 42개'는 버그 아님 — 완비 기준 5항목 중 인가일(inka_date)이 지원신청서 원본에 없어 충남1~7도 4/5, 대장에서 수기 입력 필요.
- 2026-07-21: **서식 자료실 이전 완료** — 파이썬 setup-excel/subsidy-excel 로직을 브라우저로 이식. 원본 법정서식 엑셀(빈 양식)을 IndexedDB에 1회 등록 후 ExcelJS(CDN 4.4.0)로 채움 — 테두리·병합셀 1110개·VLOOKUP 수식(설립165·지원721) 전부 보존(SheetJS 커뮤니티판은 저장 시 스타일 소실하므로 ExcelJS 선택). 사이드바 '서식 자료실' 활성화. node 검증 21건 통과. 사용자 실화면 검증(원본 등록→채워받기) 대기.
- 2026-07-21: **담당자 주/부담당 지정 + 홈 그룹 탭** — (담당) 기금 상세 헤더 [👤 담당 ✎]→모달에서 주담당1·부담당다수를 pu-erp 재직자에서 선택, `mgr_main/mgr_subs/manager` 저장, `mgrText()`로 표시. (홈) 세로 3그룹 나열→상단 탭 지역/공동/사내 전환(S.homeTab)으로 스크롤 축소, 검색 시 전체결과. node 검증. 커밋 adcf742 push.
- 2026-07-21: **담당자 명단 로드 안정화** — 재직자 명단을 동일출처 localStorage(`pureun_v6_user_accounts`, pu-erp가 채움) 우선 로드 → Firebase 규칙 의존 제거, 실패 시 `data/user_accounts/v` 폴백. `_normStaff`(배열/id객체형·퇴사·중복·휴직 필터). **기금 정보 탭에 주담당 드롭다운+부담당 체크박스 셀 분리**(자유입력 제거, 재직자 선택). 커밋 9ae4a18. (전제: 그 브라우저에서 pu-erp 1회 실행돼 localStorage에 명단 존재)
- 2026-07-21: **지원금 3종(일괄보드·전년복사·서류함)** — ①사이드바 '🎁 지원금 관리' 신설: 연도 선택→공동기금(운영중) 전체 한 표에서 신청액·결정액·교부일·집행액 인라인 입력(patchSub 자동저장)·정산대사·합계, 기금명 클릭→해당 기금 지원금탭(openFundSubsidy). ②각 기금 지원금탭 [↩ 작년 값 불러오기](copyPrevSubsidy, 전년 subsidy 프리필·교부일만 비움). ③지원금 서류함(subsidyDocsPanel): 연도별 파일 업로드(공고/신청서/결정통지/정산보고 등 7종), **Firebase Storage 필요** — SDK(storage-compat)·`storageBucket:'pureun-erp.appspot.com'` 추가, fbStore. RTDB 메타 `fund_erp/subsidy_docs/{fid}/{yr}/{id}`{kind,name,url,path,uploaded}, 파일 `fund_erp/subsidy_docs/{fid}/{yr}/`. 커밋 77b58ed. **⚠️ 사용자 설정 필요: Firebase 콘솔에서 Storage 활성화 + 규칙(fund_erp 경로 auth 읽기/쓰기) 게시. storageBucket 이름이 다르면(.firebasestorage.app) 수정. 미설정 시 업로드만 실패(나머지 기능 정상).**
- 2026-07-21: **참여사업장 담당자 연락처 채움(원본 재추출)** — 원본 `Downloads/푸른노무법인 (4).xlsx`의 `2026/2025지역공근사업장` 시트(상호·사업자번호·담당자·직무·전화·휴대폰·이메일·상시근로자수·업종)를 `import_site_contacts.py`(사업자번호 매칭, 2026 우선)로 fund.db sites.contacts에 채움 + 빈 상시근로자수/업종/대표자/소재지 보완. **563/563 전량 매칭·연락처 채움(523 이메일)**. `fund_sites_export.json` 재생성. ⚠️ **사용자 작업: 인터넷판 참여사업장 [📥 사업장 데이터 가져오기]로 재가져오기 1회 → 담당자·이메일 표시**(Firebase는 auth 필요라 코드가 직접 못 씀). 참고: (5) 파일 없어 최신 (4) 사용.
- 2026-07-21: **기금 대장 → 기금 현황 흡수 + 지난기금 상태별 분류** — (통합)사이드바 '기금 대장' 제거, 기금 현황에 [✏️ 정보 채우기] 토글(켜면 목록이 핵심6필드 인라인 입력표=옛 대장 기능, 자동저장·미완비필터·완성도). fundTable(list,edit)/fundEditRow/LED_COLS 재사용, renderLedger 삭제. (지난기금)📄계약종료·🗄기록보관 상태별 섹션+각 안에서 동종(지역/공동/사내). 커밋 76ed5e0·6cdcc2b.
- 2026-07-21: **공통 편의 4종 추가** — ①`confirmM(msg,opts)` 통일 확인 모달(모든 confirm 대체, 삭제=danger 빨강, Esc취소/Enter확인)+남은 alert 전부 `toast`화(openForm15 대차확인 confirm만 팝업 제스처 위해 native 유지). ②저장 안 한 변경 이탈 경고: infoForm 입력 `markDirty`, 저장 `clearDirty`, go/goTab/goBack `_leaveGuard`, 새로고침·닫기 `beforeunload`. ③`loadingHTML()` 회전 스피너(불러오는중 8곳 통일, @keyframes spin). ④단축키 Esc 닫기·Ctrl/Cmd+S 저장(모달 primary or 기금정보). 커밋 30f24bd. 이 유틸들은 향후 전 기능 재사용.
- 2026-07-21: **공통 편의 기능(전 화면 공유)** — ①`toast(msg,type)` 알림(#app 밖 body, 재렌더에도 유지) → 저장 클릭 시 '저장되었습니다' 표시(기존 saveInfo가 #msg 세팅 직후 renderFund로 지워 안 보이던 문제 해결). 주요 저장/삭제/가져오기 전반 연결. ②브라우저 히스토리 통합(go/goTab pushState, popstate 복원) → **브라우저 뒤로가기 동작**, `goBack()`+기금상세 [← 뒤로]. 향후 기능도 toast/goTab/goBack 재사용. 커밋 494a7a9. **방침(사용자): 추가 기능보다 전 시스템 공통 편의기능 우선.**
- 2026-07-21: **기금 생애주기 관리(계약종료·기록보관)** — 파이썬 mgmt_type 개념 이식. funds에 `lifecycle`(운영/계약종료/기록보관)+`end_date`·`end_reason`. 기금 상세 [🔖 상태]→모달(상태·종료일·사유), 상단 안내 배너. 홈: 지난 기금을 3그룹 탭에서 분리→'🗂 지난 기금' 탭, 운영/지난 개수 표시, 오늘할일(미완비) 집계 제외, 검색은 포함. 계약종료=재개 가능(운영으로 되돌림). `isPast()`. 커밋 f491ab8. node 검증. (미적용: 대장·서식 기금선택은 지난 기금도 노출 — 의도, 필요 시 필터 추가)
- 2026-07-21: **참여사업장 푸른이알피 업체 연결(모기업 당겨오기)** — 참여사업장 [🔗 푸른이알피 업체 연결]→업체 검색 모달(동일출처 localStorage `pureun_v6_companies` 우선→Firebase `data/companies/v` 폴백)→선택 시 사업장 편집기에 업체정보(상호·대표자·사업자번호·업종·소재지·담당자연락처) 프리필→검토 후 저장. `companyToSite`(contacts 배열/primaryContact 필드 모두 매핑), `puerp_co_id` 연결키 보존. 사내기금 모기업이 pu-erp에 이미 있으면 재입력 불필요. 커밋 cd487bb. node 검증.
- 2026-07-21: **참여사업장 담당자(연락처) 필드 반영** — 이전 export가 `contacts` 누락→Firebase sites에 담당자 없음이 원인. ①`fund_sites_export.json` contacts(담당자명·직위·휴대폰·이메일·isPrimary)+corp_no·note 포함 재생성(526/563건 담당자). ②편집 모달에 담당자명/직위/휴대폰/이메일+연번/법인번호/비고 필드, 목록에 담당자·이메일 컬럼. saveSite는 contacts 대표항목만 갱신(부담당 보존). ③[📥 사업장 데이터 가져오기] 버튼 항상 노출. 커밋 a742c9e·b338d58. **사용자 작업 필요: 참여사업장 탭에서 재생성 export 재가져오기 1회→담당자 채워짐.** node 검증(연락처 파싱·병합).
- 2026-07-21: **[pu-erp] Firebase 원격적용 3경로 단일화 + 초기동기화 급감차단 게이트** — (fund 아님, pu-erp.html) 코덱스 교차검증 P1. `_applyRemote`(실시간 child_changed)·`child_added`·`fbInitialSync`(초기 서버→로컬)에 복붙돼 있던 원격 레코드 적용 본문을 공용 `_fbApplyRecord(k,v,opts)`로 단일화. **초기 동기화도 급감차단 게이트 통과**(로그인 직후 서버 급감이 로컬을 무방비로 덮던 데이터 유실 차단). 대량 급감은 보류큐(deferGate)로 모아 동기화 완료 후 1건씩 확인(모달폭탄 방지). 급감 보류큐를 **버전(레코드 u) 인지**로 재설계: 무정지 펌프(정체 차단)·활성키 중복 차단(이중모달 방지)·버전 가드(meta 되돌림 방지)·완화된 최신항목은 적용(최신값 유실 방지)·모달 거부 시 상태복구. 코덱스 5라운드 교차검증으로 결함 A/B/C/D+minor 전부 해결(단일탭 정확성 확보). PR #1 병합. **잔여(범위 밖): 다중 탭 localStorage 원자성 레이스 — dbSet 등 앱 전역 기존 특성이라 별도 과제(근본해결 Web Locks).**
