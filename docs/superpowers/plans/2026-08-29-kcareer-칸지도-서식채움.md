# 경력관리 「칸 지도」 서식 채움 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 받은 한글 서식의 채울 자리를 **하나도 안 놓치고** 찾아 목록으로 보여 주고, 못 알아본 자리는 한 번 골라 주면 그 서식에 기억해서 다음부터는 바로 채운다. 도장도 눌러서 찍는다.

**Architecture:** 순수 모듈 두 개(`kcareer-formmap.js` 훑기·짝맞추기·넣기, `kcareer-hwpstamp.js` 도장)를 새로 만들고, 기존 `kcareer-hwpxfill.js`는 **채워 넣는 일꾼으로 그대로 재사용**한다(검사 154개가 지키고 있다). 화면은 이미 있는 전체화면(`#rcEditCard.rh-wide`)을 쓰고 오른쪽 패널만 서랍으로 바꾼다 — 7MB 엔진을 다시 올리지 않기 위해서다.

**Tech Stack:** 순수 JS(ES5 문법, `var`/함수 선언 — 기존 모듈과 같게), Node 내장 `node:test`, JSZip(도장만), 빌드 없음.

설계서: `docs/superpowers/specs/2026-08-29-kcareer-칸지도-서식채움-design.md`

## Global Constraints

- **주석은 한국어.** 노무 도메인 용어를 지킨다(급여→임금, 직원→근로자).
- **검사에 「지금 값」을 박지 않는다.** 규칙을 못 박는다 (`CLAUDE.md` 참조). 값 자체가 규칙일 때만 `검사고정-허용` 과 까닭을 같은 줄에 적는다.
- **소스를 글자로 보는 검사는 주석을 먼저 걷는다** (`stripComments`).
- **검사에 이빨을 넣는다** — 쓴 뒤 고친 자리를 도로 막아 실제로 걸리는지 확인한다.
- 모듈은 **브라우저/Node 겸용**: `if (typeof module !== 'undefined' && module.exports) module.exports = api; else root.X = api;`
- **`.js` 를 고치면 `kcareer.html` 의 `?v=` 를 올린다.** 안 올리면 배포에 반영되지 않는다.
- **공용 작업트리다.** `git add` 는 반드시 **경로를 찍어서** 한다. `git checkout --` 금지(내 편집이 통째로 날아간다).
- **`git stash` 금지** — 스태시 목록이 저장소 공용이라 남의 것을 건드린다.
- 전체 검사: `node --test tests/*.test.js` — **종료코드로 판정한다**(출력을 골라 읽지 않는다).

## 파일 구조

| 파일 | 맡은 일 |
|---|---|
| `js/kcareer-formmap.js` (새로) | 칸 지도 훑기(`scan`) · 짝 맞추기(`guess`) · 되돌려 넣기(`apply`) · 서식 지문(`fingerprint`) |
| `js/kcareer-hwpstamp.js` (새로) | hwpx 묶음에 도장 그림 넣기 (`BinData` + `hpf` 목록 + `<hp:pic>`) |
| `js/kcareer-hwpxfill.js` (고침) | 조각 함수 내보내기 + 「끝에 있는 라벨」 결함 고치기 |
| `kcareer.html` (고침) | 서랍 · 칸 지도 화면 · 「내 정보」 칸 · 서식별 기억 · 「🖋 도장 찍기」 |
| `tests/kcareer-formmap.test.js` (새로) | 서식 여섯으로 자리 100% · 짝 맞추기 · 넣기 · 지문 |
| `tests/kcareer-hwpstamp.test.js` (새로) | 그림 조립 세 조각 · 잘림 방지 규칙 |
| `tests/kcareer-incell-tail.test.js` (새로) | 「직위」처럼 끝에 있는 라벨 |

---

### Task 1: 「끝에 있는 라벨」 결함 고치기 + 조각 함수 내보내기

「기관명 : 부서명 : **직위 :**」에서 직위가 늘 빠졌다. 라벨 뒤에 밑줄도 넉넉한 공백도 없이
**문자열이 끝나기 때문**이다(실측 2026-08-29). 공백이 좁으면 셋 다 빠졌다.
칸 지도가 이 함수를 일꾼으로 쓰므로 먼저 고친다.

**Files:**
- Modify: `js/kcareer-hwpxfill.js` (`fillInCell` 의 정규식, api 내보내기)
- Test: `tests/kcareer-incell-tail.test.js` (새로)

**Interfaces:**
- Produces: `KcareerHwpxFill.splitRows(tbl)` → `string[]`, `KcareerHwpxFill.splitCells(tr)` → `string[]`, `KcareerHwpxFill.eachTable(xml, fn)` → `string`, `KcareerHwpxFill.normLabel(s)` → `string` — Task 2 의 `scan()` 이 그대로 쓴다.

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/kcareer-incell-tail.test.js`:

```js
/* 「기관명 : 부서명 : 직위 :」 — 직위가 늘 빠지던 것 (2026-08-29 실측)
   까닭: 라벨 뒤에 밑줄도 넉넉한 공백도 없이 «문자열이 끝난다». */
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const F = require('../js/kcareer-hwpxfill.js');
const H = require('../hwpx_gen.js');

const tbl = (rows) => H.tablePara(rows, H.cols(rows[0].map(() => 1 / rows[0].length)));
const WHO = { org: '푸른노무법인', dept: '대표', title: '대표노무사',
              phoneHome: '041-556-0035', phoneWork: '041-556-0035' };

test('끝에 있는 라벨에도 값이 들어간다 — 「직위 :」가 문장 끝이어도', () => {
  const r = F.autoFill(tbl([['현 근무처', '기관명 :        부서명 :        직위 :']]), { fields: WHO });
  assert.match(r.xml, />[^<]*대표노무사/, '직위가 빠지면 안 됩니다');
});

test('사이 공백이 좁아도 셋 다 들어간다 — 서식마다 공백 수가 다르다', () => {
  const r = F.autoFill(tbl([['현 근무처', '기관명 : 부서명 : 직위 :']]), { fields: WHO });
  ['푸른노무법인', '대표', '대표노무사'].forEach((v) =>
    assert.ok(r.xml.indexOf(v) > 0, v + ' 이(가) 들어가야 합니다'));
});

test('넣은 값과 다음 라벨이 붙지 않는다 — 「푸른노무법인부서명」이 되면 안 된다', () => {
  const r = F.autoFill(tbl([['현 근무처', '기관명 : 부서명 : 직위 :']]), { fields: WHO });
  assert.doesNotMatch(r.xml, /푸른노무법인부서명/);
});

test('이미 값이 있는 라벨은 덮지 않는다 — 끝 허용이 문을 열어서는 안 된다', () => {
  const r = F.autoFill(tbl([['현 근무처', '기관명 : 한국공인노무사회   직위 :']]), { fields: WHO });
  assert.ok(r.xml.indexOf('한국공인노무사회') > 0, '원래 값이 남아야 합니다');
  assert.equal(r.xml.indexOf('푸른노무법인'), -1, '이미 찬 칸을 덮으면 안 됩니다');
});

test('밑줄 자리는 지금처럼 그대로 채운다 — 고치다 부수지 않았는지', () => {
  const r = F.autoFill(tbl([['전화번호', '자택:_________  직장:_________']]), { fields: WHO });
  assert.ok(r.xml.indexOf('041-556-0035') > 0);
});

test('조각 함수를 내보낸다 — 칸 지도가 같은 자를 쓴다(따로 만들면 어긋난다)', () => {
  ['splitRows', 'splitCells', 'eachTable', 'normLabel'].forEach((n) =>
    assert.equal(typeof F[n], 'function', n + ' 을(를) 내보내야 합니다'));
  const t = tbl([['가', '나']]);
  assert.equal(F.splitRows(t).length, 1);
  assert.equal(F.splitCells(F.splitRows(t)[0]).length, 2);
  assert.equal(F.normLabel('성 명 *'), '성명');
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-incell-tail.test.js`
Expected: FAIL — 「직위가 빠지면 안 됩니다」와 `splitRows 을(를) 내보내야 합니다`

- [ ] **Step 3: `fillInCell` 의 정규식을 고친다**

`js/kcareer-hwpxfill.js` 의 `fillInCell` 안, `var re = new RegExp(...)` 줄을 바꾼다.
**앞뒤 주석도 함께 고친다** — 옛 설명이 남으면 다음 사람이 되돌린다.

```js
      /* 라벨 + 콜론 + «값 자리»
         ⚠ 값 자리는 밑줄만이 아니다. 실측(2026-08-29) 「기관명 : 부서명 : 직위 :」에서
           끝에 붙은 「직위」가 늘 빠졌다 — 뒤에 밑줄도 넉넉한 공백도 없이 문장이 끝난다.
           그래서 «끝» 과 «바로 다음 라벨» 도 값 자리로 본다.
         ⚠ 그렇다고 아무 데나 넣으면 안 된다 — 뒤에 이미 «글자»가 오면 건드리지 않는다.
           앞을 내다보기(?=…)로만 판단해 바꾸는 자리는 라벨+콜론까지다.
         ⚠ L.re.source 를 (?:…) 로 감싼다 — 「직장|사무실」처럼 교대가 들어 있으면
           괄호 없이는 뒤가 통째로 갈라진다. */
      var ANY = INCELL_LABELS.map(function (x) { return x.re.source; }).join('|');
      var re = new RegExp('((?:' + L.re.source + ')\\s*[:：]\\s*)'
        + '(?=$|_{2,}|\\u3000{2,}|\\s{4,}|\\s*(?:' + ANY + ')\\s*[:：])');
      if (!re.test(inner)) return m;
      var next = inner.replace(re, function (mm, head, off, whole) {
        /* 뒤에 다른 라벨이 이어지면 사이를 벌린다 — 「푸른노무법인부서명」이 되지 않게 */
        var rest = whole.slice(off + mm.length);
        return head + esc(fields[L.key]) + (rest.replace(/^[\s_　]+/, '') ? '  ' : '');
      });
```

- [ ] **Step 4: api 에 조각 함수를 더한다**

같은 파일 맨 아래 `var api = {` 에 네 개를 더한다.

```js
  var api = {
    autoFill: autoFill, summarize: summarize,
    fieldKeyOf: fieldKeyOf, colKeyOf: colKeyOf,
    cellText: cellText, isEmptyCell: isEmptyCell, fillCell: fillCell,
    /* 칸 지도(kcareer-formmap.js)가 «같은 자»를 쓰도록 내보낸다 —
       따로 만들면 두 곳의 셈이 어긋나 「지도에는 있는데 안 채워지는 칸」이 생긴다 */
    splitRows: splitRows, splitCells: splitCells, eachTable: eachTable, normLabel: normLabel
  };
```

- [ ] **Step 5: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-incell-tail.test.js tests/kcareer-hwpxfill.test.js`
Expected: PASS — 새 검사 6개 + 기존 154개가 모두 통과(기존이 깨지면 고친 것이 지나쳤다는 뜻)

- [ ] **Step 6: 이빨 확인 — 고친 자리를 도로 막아 본다**

정규식에서 `$|` 를 잠깐 지우고 검사를 돌려 「직위」 검사가 **실패하는지** 본다. 확인했으면 되돌린다.
⚠ 되돌릴 때 `git checkout` 을 쓰지 말 것 — 이 파일의 다른 편집까지 날아간다. 손으로 되돌린다.

- [ ] **Step 7: 커밋**

```bash
git add js/kcareer-hwpxfill.js tests/kcareer-incell-tail.test.js
git commit -m "fix(경력관리): 「직위 :」처럼 끝에 붙은 라벨이 늘 빠지던 것"
```

---

### Task 2: 칸 지도 훑기 — 자리를 하나도 안 놓친다

**Files:**
- Create: `js/kcareer-formmap.js`
- Test: `tests/kcareer-formmap.test.js`

**Interfaces:**
- Consumes: `KcareerHwpxFill.splitRows/splitCells/eachTable/cellText/colKeyOf` (Task 1)
- Produces:
  - `KcareerFormMap.scan(sectionXml)` → `{ slots, lists, warn }`
  - `slot` = `{ id:'t0r1c2', tbl, row, col, kind:'빈칸'|'안내글뒤'|'칸안라벨', text, left, guess:'' }`
  - `list` = `{ id:'L0', tbl, kind:'edu'|'career', cols:[…], head, blank }`
  - `warn` = `{ textBoxes, nested }`
  - Task 3·4·5 와 화면이 이 모양을 그대로 쓴다.

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/kcareer-formmap.test.js`:

```js
/* 칸 지도 — 「이름은 짐작이지만 자리는 사실이다」
   묻는 것: 모양이 전혀 다른 서식이 와도 채울 자리를 «하나도 안 놓치는가».
   설계서: docs/superpowers/specs/2026-08-29-kcareer-칸지도-서식채움-design.md */
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const M = require('../js/kcareer-formmap.js');
const H = require('../hwpx_gen.js');

const tbl = (rows) => H.tablePara(rows, H.cols(rows[0].map(() => 1 / rows[0].length)));

/* 모양이 전혀 다른 서식들 — 여기에 하나를 더해도 검사가 그대로 돌아야 한다 */
const FORMS = {
  지원서: [['성  명', '(한글)', '생년월일', ''],
           ['현 주 소', '', '', ''],
           ['전화번호', '자택:____  직장:____', '휴대폰', '']],
  세로라벨: [['성명', ''], ['생년월일', ''], ['주소', ''], ['연락처', ''], ['이메일', '']],
  두쌍: [['신청인', '', '주민등록번호', ''], ['소속기관', '', '직  위', '']],
  처음보는말: [['위촉대상자', '', '위촉희망분야', ''], ['비상연락망', '', '', '']],
  도장자리: [['작성일', ''], ['성명', '(인)']]
};

test('빈 칸은 빠짐없이 자리로 잡는다 — 이름을 몰라도', () => {
  const m = M.scan(tbl(FORMS.처음보는말));
  /* 사전에 없는 말이라 짐작은 하나도 못 하지만, 자리는 다 나와야 한다 */
  assert.equal(m.slots.filter((s) => s.kind === '빈칸').length, 4);
  assert.ok(m.slots.every((s) => s.guess === ''), 'scan 단계에서는 짐작하지 않는다');
});

test('왼쪽 칸 글자를 함께 담는다 — 짝 맞추기의 유일한 실마리다', () => {
  const m = M.scan(tbl(FORMS.세로라벨));
  const first = m.slots.find((s) => s.row === 0 && s.col === 1);
  assert.equal(first.left, '성명');
});

test('괄호 안내글은 「안내글뒤」로 — 덮지 않고 뒤에 이어 쓸 자리다', () => {
  const m = M.scan(tbl(FORMS.지원서));
  const hint = m.slots.find((s) => s.text === '(한글)');
  assert.ok(hint, '「(한글)」을 자리로 잡아야 합니다 — 여기서 이름이 통째로 빠졌었다');
  assert.equal(hint.kind, '안내글뒤');
  assert.equal(hint.left, '성  명');
});

test('칸 안에 라벨과 빈자리가 함께면 「칸안라벨」', () => {
  const m = M.scan(tbl(FORMS.지원서));
  const c = m.slots.find((s) => /자택/.test(s.text));
  assert.equal(c.kind, '칸안라벨');
});

test('그냥 본문은 자리로 보지 않는다 — 절대 덮지 않는다', () => {
  const m = M.scan(tbl([['제출서류', '1. 이력서(사진부착) 1부']]));
  assert.equal(m.slots.length, 0);
});

test('자리 이름표는 표·행·열로 만든다 — 기억의 열쇠가 된다', () => {
  const m = M.scan(tbl(FORMS.세로라벨));
  assert.equal(m.slots[0].id, 't0r0c1');
  const ids = m.slots.map((s) => s.id);
  assert.equal(new Set(ids).size, ids.length, '이름표가 겹치면 기억이 엉킨다');
});

test('서식 다섯을 다 훑는다 — 어느 것에서도 자리가 0이 되지 않는다', () => {
  Object.keys(FORMS).forEach((name) => {
    const m = M.scan(tbl(FORMS[name]));
    assert.ok(m.slots.length > 0, name + ' 에서 자리를 하나도 못 찾았습니다');
  });
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js`
Expected: FAIL — `Cannot find module '../js/kcareer-formmap.js'`

- [ ] **Step 3: 모듈을 만든다**

`js/kcareer-formmap.js`:

```js
'use strict';
/* 푸른노무법인 경력관리 — 서식 「칸 지도」
   (브라우저 window.KcareerFormMap / Node module.exports 겸용, DOM 미사용)
   설계서: docs/superpowers/specs/2026-08-29-kcareer-칸지도-서식채움-design.md

   왜 만드나: 지금 자동채움은 «라벨을 알아본 자리만» 채우고 모르는 자리는 조용히 지나간다.
   그래서 서식이 바뀔 때마다 몇 칸씩 빈다(실측: 서식 여섯에서 자리 37군데 중 이름을
   알아본 것은 12군데뿐).

   여기서 뒤집는다 — «이름은 짐작이지만 자리는 사실이다».
   먼저 채울 자리를 빠짐없이 찾아 두고, 이름은 나중에 짐작하거나 사람에게 묻는다. */
(function (root) {

  var X = (typeof require === 'function' && typeof module !== 'undefined')
    ? require('./kcareer-hwpxfill.js')
    : root.KcareerHwpxFill;

  /* ── 칸 하나가 «어떤 자리»인가 ──
     빈칸     : 글자가 하나도 없다 → 그 칸에 넣는다
     안내글뒤 : 괄호만 있는 짧은 안내 「(한글)」「(한자)」「(인)」 → 글자 «뒤에 이어» 쓴다
     칸안라벨 : 「자택:____」「기관명 : 부서명 :」 → 라벨 뒤 빈자리에 넣는다
     ''       : 그냥 본문 → 자리가 아니다. 절대 덮지 않는다. */
  function classify(text) {
    var s = String(text == null ? '' : text).trim();
    if (s === '') return '빈칸';
    if (/^[（(][^)）]{1,6}[)）]$/.test(s)) return '안내글뒤';
    if (/_{2,}/.test(s) || /[:：]\s*$/.test(s) || /[:：]\s{2,}/.test(s)) return '칸안라벨';
    return '';
  }

  function scan(sectionXml) {
    var slots = [], lists = [], warn = { textBoxes: 0, nested: 0 };
    var ti = -1;
    X.eachTable(String(sectionXml || ''), function (tbl) {
      ti++;
      var rows = X.splitRows(tbl);
      var grid = rows.map(function (tr) { return X.splitCells(tr).map(X.cellText); });
      grid.forEach(function (cells, ri) {
        cells.forEach(function (txt, ci) {
          var kind = classify(txt);
          if (!kind) return;
          slots.push({ id: 't' + ti + 'r' + ri + 'c' + ci, tbl: ti, row: ri, col: ci,
                       kind: kind, text: String(txt || '').trim(),
                       left: ci > 0 ? String(cells[ci - 1] || '').trim() : '', guess: '' });
        });
      });
      return tbl;   /* 훑기만 한다 — 여기서는 아무것도 안 바꾼다 */
    });
    return { slots: slots, lists: lists, warn: warn };
  }

  var api = { scan: scan, classify: classify };
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else root.KcareerFormMap = api;
})(typeof globalThis !== 'undefined' ? globalThis : window);
```

- [ ] **Step 4: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js`
Expected: PASS — 7개 통과

- [ ] **Step 5: 커밋**

```bash
git add js/kcareer-formmap.js tests/kcareer-formmap.test.js
git commit -m "feat(경력관리): 칸 지도 훑기 — 이름을 몰라도 «자리»는 빠짐없이 찾는다"
```

---

### Task 3: 목록 표는 묶고, 못 읽는 것은 세어서 알린다

낱개로 두면 학력·경력 표 하나가 칸 지도 아홉 줄이 되어 못 쓴다.
글상자·중첩 표는 조용히 빠지면 「채웠다는데 비어 있다」가 된다 — **세어서 알린다.**

**Files:**
- Modify: `js/kcareer-formmap.js` (`scan` 에 `lists`·`warn` 채우기)
- Test: `tests/kcareer-formmap.test.js` (검사 추가)

**Interfaces:**
- Produces: `list` = `{ id:'L0', tbl, kind:'edu'|'career', cols:['period','org','role'], head, blank }`, `warn` = `{ textBoxes, nested }`

- [ ] **Step 1: 실패하는 검사를 더한다**

`tests/kcareer-formmap.test.js` 아래에 붙인다:

```js
test('목록 표는 낱개 칸이 아니라 «한 줄»로 묶는다 — 아홉 줄이 되면 못 쓴다', () => {
  const m = M.scan(tbl([['기간', '기관명', '직위'], ['', '', ''], ['', '', ''], ['', '', '']]));
  assert.equal(m.lists.length, 1);
  assert.equal(m.lists[0].kind, 'career');
  assert.equal(m.lists[0].blank, 3, '빈 줄이 몇 개인지 알아야 「3줄까지」를 말해 준다');
  assert.equal(m.slots.length, 0, '목록 표의 칸은 낱개로 세지 않는다');
});

test('학교 열이 있으면 학력 표로 본다', () => {
  const m = M.scan(tbl([['기간', '학교명', '전공'], ['', '', '']]));
  assert.equal(m.lists[0].kind, 'edu');
});

test('머리행 열쇠가 하나뿐이면 목록 표로 보지 않는다 — 보통 표를 잘못 삼키면 안 된다', () => {
  const m = M.scan(tbl([['기간', '비고'], ['', '']]));
  assert.equal(m.lists.length, 0);
  assert.ok(m.slots.length > 0, '보통 표로서 자리는 나와야 합니다');
});

test('글상자를 세어서 알린다 — 조용히 빠지면 「채웠다는데 비어 있다」가 된다', () => {
  const xml = tbl([['성명', '']]) + '<hp:drawText><hp:p><hp:run><hp:t>글상자 속 글</hp:t></hp:run></hp:p></hp:drawText>';
  assert.equal(M.scan(xml).warn.textBoxes, 1);
});

test('중첩 표(칸 안의 표)를 세어서 알린다', () => {
  const inner = tbl([['가', '']]);
  const outer = '<hp:tbl><hp:tr><hp:tc><hp:p><hp:run>' + inner + '</hp:run></hp:p></hp:tc></hp:tr></hp:tbl>';
  assert.equal(M.scan(outer).warn.nested, 1);
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js`
Expected: FAIL — `m.lists.length` 가 0

- [ ] **Step 3: `scan` 을 고친다**

`js/kcareer-formmap.js` 의 `classify` 아래에 더한다:

```js
  /* ── 목록 표 알아보기 ──
     머리행에서 목록 열쇠가 둘 이상 맞으면 목록 표다.
     하나만 맞으면 보통 표로 둔다 — 「기간 | 비고」 같은 표를 삼키면 안 된다. */
  function detectList(grid, ti) {
    for (var r = 0; r < grid.length; r++) {
      var keys = grid[r].map(function (t) { return X.colKeyOf(t); });
      var hit = keys.filter(Boolean).length;
      if (hit < 2) continue;
      var kind = keys.indexOf('school') >= 0 ? 'edu'
        : (keys.indexOf('org') >= 0 && (keys.indexOf('role') >= 0 || keys.indexOf('period') >= 0)) ? 'career' : '';
      if (!kind) continue;
      var blank = 0;
      for (var q = r + 1; q < grid.length; q++) {
        var empty = grid[q].every(function (t) { return !String(t || '').trim(); });
        if (empty) blank++;
      }
      return { id: 'L' + ti, tbl: ti, kind: kind, cols: keys, head: r, blank: blank };
    }
    return null;
  }
```

그리고 `scan` 안의 `X.eachTable(...)` 콜백 첫머리에 목록 표 갈림길을 넣는다:

```js
      var rows = X.splitRows(tbl);
      var grid = rows.map(function (tr) { return X.splitCells(tr).map(X.cellText); });
      /* 칸 안에 표가 또 있으면 정규식이 경계를 잘못 짚는다 — 세어만 두고 건드리지 않는다 */
      if (tbl.indexOf('<hp:tbl', 7) >= 0) { warn.nested++; return tbl; }
      var L = detectList(grid, ti);
      if (L) { lists.push(L); return tbl; }   /* 목록 표는 낱개로 세지 않는다 */
```

`return { slots: ... }` 바로 앞에 글상자 세기를 넣는다:

```js
    /* 글상자(도형 안의 글)는 아직 못 읽는다 — 세어서 화면에 알린다 */
    warn.textBoxes = (String(sectionXml || '').match(/<hp:drawText\b/g) || []).length;
```

⚠ `eachTable` 은 중첩 표를 만나면 콜백을 부르지 않고 통째로 지나간다. 그래서 위 `nested++`
줄은 안전망이다 — 나중에 `eachTable` 이 바뀌어도 세는 자리가 남는다.

- [ ] **Step 4: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js`
Expected: PASS — 12개 통과

⚠ `warn.nested` 검사가 0으로 실패하면 `eachTable` 이 중첩 표에서 콜백을 아예 안 부르는 것이다.
그때는 `scan` 에서 `sectionXml` 전체를 훑어 중첩 표를 따로 센다:
`warn.nested = (sectionXml.match(/<hp:tbl(?=[\s\S]*?<hp:tbl)/g) || []).length` 가 아니라,
`X.eachTable` 을 쓰지 않고 `<hp:tbl` 짝을 직접 세는 작은 함수를 둔다.

- [ ] **Step 5: 커밋**

```bash
git add js/kcareer-formmap.js tests/kcareer-formmap.test.js
git commit -m "feat(경력관리): 목록 표는 묶고, 못 읽는 글상자·중첩표는 세어서 알린다"
```

---

### Task 4: 짝 맞추기 — 실측에서 드러난 다섯 가지

**Files:**
- Modify: `js/kcareer-formmap.js` (`guess` 추가)
- Modify: `js/kcareer-hwpxfill.js` (`FIELD_LABELS` 에 흔한 말 더하기)
- Test: `tests/kcareer-formmap.test.js`

**Interfaces:**
- Produces: `KcareerFormMap.guess(map, data)` → 같은 `map`(각 `slot.guess`, `list.guess` 채워짐). 열쇠는 `kcareer-hwpxfill` 의 것과 같다(`name`·`nameHanja`·`birth`·`phone`·`email`·`addr`·`org`·`dept`·`title`) + `'__stamp'`(도장 자리) + `'__incell'`(칸 안 라벨 통째).

- [ ] **Step 1: 실패하는 검사를 더한다**

```js
test('★ 왼쪽이 성명이면 그 행의 「(한글)」은 이름 — 여기서 이름이 통째로 빠졌었다', () => {
  const m = M.guess(M.scan(tbl([['성  명', '(한글)', '생년월일', '']])), {});
  const hint = m.slots.find((s) => s.text === '(한글)');
  assert.equal(hint.guess, 'name');
});

test('「(한자)」는 한자 이름', () => {
  const m = M.guess(M.scan(tbl([['성  명', '(한자)']])), {});
  assert.equal(m.slots.find((s) => s.text === '(한자)').guess, 'nameHanja');
});

test('「(인)」은 도장 자리로 따로 표시한다 — 글자를 넣으면 안 된다', () => {
  const m = M.guess(M.scan(tbl([['성명', '(인)']])), {});
  assert.equal(m.slots.find((s) => s.text === '(인)').guess, '__stamp');
});

test('흔한 말을 알아본다 — 신청인·위촉대상자·추천인', () => {
  const m = M.guess(M.scan(tbl([['신청인', ''], ['위촉대상자', ''], ['추천인', '']])), {});
  m.slots.forEach((s) => assert.equal(s.guess, 'name', s.left + ' 을(를) 이름으로 봐야 합니다'));
});

test('주민등록번호는 알아보되 기본은 «비워 둠» — 자동으로 나가면 안 되는 정보다', () => {
  const m = M.guess(M.scan(tbl([['주민등록번호', '']])), {});
  const s = m.slots[0];
  assert.equal(s.guess, '', '짐작으로 채우지 않는다');
  assert.equal(s.hint, 'rrn', '무슨 칸인지는 알려 주되 값은 사람이 넣는다');
});

test('목록 표에도 짐작을 붙인다 — 경력 표면 경력을 넣는다', () => {
  const m = M.guess(M.scan(tbl([['기간', '기관명', '직위'], ['', '', '']])), {});
  assert.equal(m.lists[0].guess, 'career');
});

test('사전에 없는 말은 «모름»으로 남긴다 — 지어내지 않는다', () => {
  const m = M.guess(M.scan(tbl([['추천사유', ''], ['비상연락망', '']])), {});
  assert.ok(m.slots.every((s) => s.guess === ''), '모르면 모른다고 해야 사람이 고른다');
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js`
Expected: FAIL — `M.guess is not a function`

- [ ] **Step 3: 사전에 흔한 말을 더한다**

`js/kcareer-hwpxfill.js` 의 `FIELD_LABELS` 에서 name 줄과 새 줄:

```js
    { re: /^(성명|이름|신청자명?|신청인|성함|성명한글|한글성명|위촉대상자|추천인|대상자)$/, key: 'name' },
```

같은 배열 끝에 더한다:

```js
    /* 주민등록번호는 «알아보되 채우지 않는다» — 자동으로 나가면 안 되는 정보다.
       열쇠를 rrn 으로 따로 두어, 칸 지도가 「무슨 칸인지는 알려 주고 값은 비워」 둘 수 있게 한다. */
    { re: /^(주민등록번호|주민번호|생년월일주민등록번호)$/, key: 'rrn' }
```

- [ ] **Step 4: `guess` 를 만든다**

`js/kcareer-formmap.js` 의 `scan` 아래에 더한다:

```js
  /* 안내글 「(한글)」「(한자)」「(인)」이 무엇을 뜻하는지 —
     ⚠ 「(한글)」은 그 자체로는 아무 뜻이 없다. «왼쪽 칸이 성명일 때만» 이름이다.
       (성명 행이 아닌 곳의 「(한글)」에 이름을 넣으면 엉뚱한 자리에 이름이 박힌다.) */
  function hintKey(slot) {
    var t = X.normLabel(slot.text);
    if (/^한자$/.test(t)) return 'nameHanja';
    if (/^한글$/.test(t)) return X.fieldKeyOf(slot.left) === 'name' ? 'name' : '';
    if (/^(인|서명|서명또는인|印)$/.test(t)) return '__stamp';
    return X.fieldKeyOf(slot.text);
  }

  /* ── 짝 맞추기 ──
     모르면 «모른다»고 남긴다. 지어내면 엉뚱한 자리에 값이 박히고, 그건 안 채운 것보다 나쁘다.
     rrn(주민등록번호)은 알아보되 채우지 않는다 — hint 로만 알린다. */
  function guess(map, data) {
    map.slots.forEach(function (s) {
      var k = s.kind === '안내글뒤' ? hintKey(s)
            : s.kind === '칸안라벨' ? '__incell'
            : X.fieldKeyOf(s.left);
      if (k === 'rrn') { s.guess = ''; s.hint = 'rrn'; return; }
      s.guess = k || '';
    });
    map.lists.forEach(function (l) { l.guess = l.kind; });
    return map;
  }
```

`api` 에 `guess: guess, hintKey: hintKey` 를 더한다.

- [ ] **Step 5: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js tests/kcareer-hwpxfill.test.js tests/kcareer-incell-tail.test.js`
Expected: PASS — 전부 통과

- [ ] **Step 6: 이빨 확인**

`hintKey` 의 `한글` 줄에서 `X.fieldKeyOf(slot.left) === 'name' ?` 조건을 지우고 늘 `'name'`을
돌려주게 바꾼 뒤, 「(한글)」이 성명 행이 아닐 때도 이름으로 잡히는지 본다. 확인 후 손으로 되돌린다.

- [ ] **Step 7: 커밋**

```bash
git add js/kcareer-formmap.js js/kcareer-hwpxfill.js tests/kcareer-formmap.test.js
git commit -m "feat(경력관리): 짝 맞추기 — 「(한글)」을 이름으로 잇고, 모르면 모른다고 남긴다"
```

---

### Task 5: 되돌려 넣기와 서식 지문

**Files:**
- Modify: `js/kcareer-formmap.js` (`apply`, `fingerprint`)
- Test: `tests/kcareer-formmap.test.js`

**Interfaces:**
- Produces:
  - `KcareerFormMap.apply(sectionXml, plan)` → `{ xml, changed, filled:[{id,key,value}], failed:[{id,why}] }`
  - `plan` = `{ picks:{ 't0r1c2':'name' }, lists:{ 'L0':'career' }, data:{ fields:{…}, edu:[…], career:[…] } }`
  - `KcareerFormMap.fingerprint(sectionXml)` → `string`

- [ ] **Step 1: 실패하는 검사를 더한다**

```js
const WHO = { fields: { name: '권형하', nameHanja: '權炯河', birth: '1975.01.07',
  phone: '010-1234-5678', addr: '충남 천안시', org: '푸른노무법인', title: '대표노무사' },
  career: [{ period: '2025', org: '충청남도', role: '노동권익보호관' }] };

test('빈 칸에 고른 값이 들어간다', () => {
  const xml = tbl([['성명', ''], ['주소', '']]);
  const m = M.guess(M.scan(xml), WHO);
  const r = M.apply(xml, { picks: { t0r0c1: 'name', t0r1c1: 'addr' }, data: WHO });
  assert.match(r.xml, />[^<]*권형하/);
  assert.match(r.xml, />[^<]*충남 천안시/);
  assert.equal(r.filled.length, 2);
});

test('★ 안내글 뒤에 «이어» 쓴다 — 「(한글)」을 지우지 않는다', () => {
  const xml = tbl([['성  명', '(한글)']]);
  const r = M.apply(xml, { picks: { t0r0c1: 'name' }, data: WHO });
  assert.ok(r.xml.indexOf('(한글)') > 0, '안내글은 남아야 합니다');
  assert.ok(r.xml.indexOf('권형하') > 0, '이름이 들어가야 합니다');
});

test('«비워 둠»(빈 값)을 고르면 아무것도 안 넣는다', () => {
  const xml = tbl([['성명', '']]);
  const r = M.apply(xml, { picks: { t0r0c1: '' }, data: WHO });
  assert.equal(r.changed, false);
  assert.equal(r.filled.length, 0);
});

test('목록 표는 고른 만큼 줄로 넣는다', () => {
  const xml = tbl([['기간', '기관명', '직위'], ['', '', '']]);
  const r = M.apply(xml, { picks: {}, lists: { L0: 'career' }, data: WHO });
  assert.ok(r.xml.indexOf('충청남도') > 0);
});

test('넣지 못한 자리는 «세어서» 돌려준다 — 조용히 넘기면 「저장했는데 비어 있다」가 된다', () => {
  const xml = tbl([['성명', '']]);
  const r = M.apply(xml, { picks: { t9r9c9: 'name' }, data: WHO });
  assert.equal(r.failed.length, 1);
  assert.equal(r.failed[0].id, 't9r9c9');
});

test('서식 지문 — 같은 서식은 같고, 칸이 달라지면 달라진다', () => {
  const a = tbl([['성명', ''], ['주소', '']]);
  const b = tbl([['성명', ''], ['주소', '']]);
  const c = tbl([['성명', ''], ['연락처', '']]);
  assert.equal(M.fingerprint(a), M.fingerprint(b));
  assert.notEqual(M.fingerprint(a), M.fingerprint(c),
    '칸 이름이 달라지면 옛 기억을 쓰면 안 됩니다 — 엉뚱한 칸에 넣는 것이 더 나쁘다');
});

test('서식 지문은 «채운 값»에는 흔들리지 않는다 — 한 번 채운 서식도 같은 서식이다', () => {
  const before = tbl([['성명', ''], ['주소', '']]);
  const after = M.apply(before, { picks: { t0r0c1: 'name' }, data: WHO }).xml;
  assert.equal(M.fingerprint(before), M.fingerprint(after));
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js`
Expected: FAIL — `M.apply is not a function`

- [ ] **Step 3: `apply` 와 `fingerprint` 를 만든다**

`js/kcareer-formmap.js` 에 더한다:

```js
  /* 한 칸의 XML 을 찾아 바꾼다 — 표·행·열 자리로 찾는다 */
  function eachCellAt(xml, tbl, row, col, fn) {
    var ti = -1, done = false;
    var out = X.eachTable(xml, function (t) {
      ti++;
      if (ti !== tbl || done) return t;
      var rows = X.splitRows(t);
      if (!rows[row]) return t;
      var cells = X.splitCells(rows[row]);
      if (!cells[col]) return t;
      var next = fn(cells[col]);
      if (next == null || next === cells[col]) return t;
      var newTr = rows[row].replace(cells[col], next);
      done = true;
      return t.replace(rows[row], newTr);
    });
    return { xml: out, ok: done };
  }

  /* 안내글 「(한글)」 뒤에 이어 쓴다 — 지우지 않는다.
     ⚠ 안내글을 지우면 서식이 뜻하는 「여기에 한글로 쓰세요」가 사라진다. */
  function appendAfter(tc, value) {
    var v = String(value == null ? '' : value)
      .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    var m = tc.match(/(<hp:t[^>]*>)([\s\S]*?)(<\/hp:t>)/);
    if (!m) return null;
    return tc.replace(m[0], m[1] + m[2] + ' ' + v + m[3]);
  }

  /* ── 되돌려 넣기 ──
     화면이 만든 plan 만 보고 넣는다. 화면은 XML 을 모르고, 여기는 화면을 모른다. */
  function apply(sectionXml, plan) {
    plan = plan || {};
    var picks = plan.picks || {}, data = plan.data || {}, fields = data.fields || {};
    var xml = String(sectionXml || ''), filled = [], failed = [];
    var map = scan(xml);

    Object.keys(picks).forEach(function (id) {
      var key = picks[id];
      if (!key || key === '__stamp') return;                 /* 비워 둠 · 도장은 여기서 안 다룬다 */
      var s = map.slots.filter(function (x) { return x.id === id; })[0];
      if (!s) { failed.push({ id: id, why: '그런 자리가 없습니다' }); return; }
      var r;
      if (key === '__incell') {
        /* 칸 안에 라벨이 여럿인 자리는 통째로 기존 일꾼에게 맡긴다 */
        r = eachCellAt(xml, s.tbl, s.row, s.col, function (tc) {
          var rep = { fields: [], usedKeys: {} };
          var one = X.autoFill(tc, { fields: fields });
          return one.changed ? one.xml : null;
        });
        if (r.ok) { xml = r.xml; filled.push({ id: id, key: key, value: '(칸 안 라벨)' }); }
        else failed.push({ id: id, why: '칸 안 라벨을 못 채웠습니다' });
        return;
      }
      var val = fields[key];
      if (val == null || val === '') { failed.push({ id: id, why: '넣을 값이 없습니다' }); return; }
      r = eachCellAt(xml, s.tbl, s.row, s.col, function (tc) {
        return s.kind === '안내글뒤' ? appendAfter(tc, val) : X.fillCell(tc, val);
      });
      if (r.ok) { xml = r.xml; filled.push({ id: id, key: key, value: val }); }
      else failed.push({ id: id, why: '이 칸에는 넣을 수 없습니다' });
    });

    /* 목록 표는 기존 fillList(autoFill 안)가 이미 잘 한다 — 그대로 쓴다 */
    var wantLists = plan.lists || {};
    if (Object.keys(wantLists).length) {
      var before = xml;
      var r2 = X.autoFill(xml, { fields: {}, edu: data.edu || [], career: data.career || [] });
      if (r2.changed) { xml = r2.xml; (r2.report.lists || []).forEach(function (l) {
        filled.push({ id: 'L', key: l.kind, value: l.put + '줄' }); }); }
      if (xml === before) failed.push({ id: 'L', why: '목록 표에 빈 줄이 없습니다' });
    }

    return { xml: xml, changed: xml !== String(sectionXml || ''), filled: filled, failed: failed };
  }

  /* ── 서식 지문 ──
     «칸의 짜임»만 본다 — 표 개수·행열 크기·라벨 글자. 채운 값은 안 본다.
     한 번 채운 서식도 같은 서식이어야 기억이 이어진다. */
  function fingerprint(sectionXml) {
    var parts = [], ti = -1;
    X.eachTable(String(sectionXml || ''), function (t) {
      ti++;
      var rows = X.splitRows(t);
      parts.push('T' + ti + ':' + rows.length);
      rows.forEach(function (tr) {
        var cells = X.splitCells(tr).map(X.cellText);
        parts.push(cells.length + '|' + cells.map(function (c) {
          /* 자리(빈칸·안내글·칸안라벨)는 «자리»라고만 적는다 — 무엇이 채워졌는지는 안 본다 */
          return classify(c) ? '·' : X.normLabel(c);
        }).join(','));
      });
      return t;
    });
    var s = parts.join(';'), h = 5381;
    for (var i = 0; i < s.length; i++) { h = ((h << 5) + h + s.charCodeAt(i)) | 0; }
    return 'F' + (h >>> 0).toString(36) + '.' + s.length;
  }
```

`api` 에 `apply: apply, fingerprint: fingerprint` 를 더한다.

- [ ] **Step 4: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-formmap.test.js`
Expected: PASS

⚠ 「안내글 뒤에 이어 쓴다」가 실패하면 `appendAfter` 가 첫 `<hp:t>` 를 못 찾은 것이다.
`hwpx_gen.tablePara` 가 만드는 빈 칸 모양(`<hp:t></hp:t>` · `<hp:t/>`)을 콘솔에 찍어 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add js/kcareer-formmap.js tests/kcareer-formmap.test.js
git commit -m "feat(경력관리): 칸 지도대로 되돌려 넣기 + 서식 지문(채운 값에 안 흔들린다)"
```

---

### Task 6: 화면 — 서랍 · 칸 지도 · 「내 정보」 칸

지금 「내 정보로 채우기」는 결과를 **다른 창**에 보여 준다. 편집기는 그대로라 「내가 쳐야 하나」가 된다.
채운 결과를 **보고 있는 편집기에 그대로** 올린다.

**Files:**
- Modify: `kcareer.html` (CSS, 오른쪽 패널 마크업, 함수 `rhBuildMap`·`rhRenderMap`·`rhFillByMap`·`rhDrawerTab`)
- Test: `tests/kcareer-formmap-ui.test.js` (새로)

**Interfaces:**
- Consumes: `KcareerFormMap.scan/guess/apply/fingerprint` (Task 2~5)
- Produces: `window.rhBuildMap()`(서식 훑어 지도 그리기) · `window.rhFillByMap()`(고른 대로 채우기) — Task 7·8 이 부른다.

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/kcareer-formmap-ui.test.js`:

```js
/* 칸 지도 화면 — 「모르는 자리를 물어보는 길」이 실제로 화면에 있는가 */
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const source = fs.readFileSync(path.join(__dirname, '..', 'kcareer.html'), 'utf8');
const bare = source.replace(/\/\*[\s\S]*?\*\//g, ' ').replace(/^\s*\/\/.*$/gm, ' ');

test('칸 지도 모듈을 읽어 들인다 — 캐시 번호를 붙여서', () => {
  assert.match(source, /js\/kcareer-formmap\.js\?v=\d+/);
});

test('오른쪽 패널이 세 칸 서랍이다 — 채울 곳 · 내 정보 · 경력·자격', () => {
  assert.match(bare, /rhDrawerTab\(/);
  ['채울 곳', '내 정보', '경력·자격'].forEach((t) =>
    assert.ok(source.indexOf(t) > 0, '「' + t + '」 칸이 있어야 합니다'));
});

test('★ 「내 정보」 목록이 있다 — 성명·연락처를 클릭으로 넣을 길이 없어서 손으로 쳤다', () => {
  assert.match(bare, /_rhMyInfoList|rhRenderMyInfo/);
});

test('서랍을 접을 수 있다 — 접으면 문서가 화면을 다 쓴다', () => {
  assert.match(bare, /rh-drawer-off|rhDrawerToggle/);
});

test('채운 결과를 «보고 있는 편집기»에 다시 올린다 — 다른 창에만 띄우면 손으로 친다', () => {
  const fn = bare.slice(bare.indexOf('function rhFillByMap'));
  assert.match(fn.slice(0, 2500), /mountEditor\(/,
    '편집기를 다시 올려야 이어서 손볼 수 있습니다');
});

test('못 읽은 글상자·중첩표를 화면에 적는다 — 조용히 빠지면 「비어 있다」가 된다', () => {
  assert.match(bare, /warn\.textBoxes/);
  assert.match(bare, /warn\.nested/);
});

test('넣지 못한 자리를 화면에 적는다', () => {
  assert.match(bare, /failed\.length/);
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-formmap-ui.test.js`
Expected: FAIL — `js/kcareer-formmap.js?v=` 없음

- [ ] **Step 3: 모듈을 읽어 들인다**

`kcareer.html` 에서 `<script src="js/kcareer-hwpxfill.js?v=1"></script>` 다음 줄에 더한다.
같은 줄의 `hwpxfill` 캐시 번호도 **올린다**(Task 1·4 에서 고쳤다).

```html
<script src="js/kcareer-hwpxfill.js?v=2"></script>
<script src="js/kcareer-formmap.js?v=1"></script>
```

- [ ] **Step 4: 서랍 CSS 를 더한다**

`#rhwpEditor{min-height:520px}` 아래에 더한다:

```css
/* ── 서식 작업 서랍 ──
   오른쪽 패널을 접으면 문서가 화면을 다 쓴다. 서식은 넓게 봐야 어느 칸이 비었는지 보인다. */
#rcEditCard.rh-wide>div:last-child{grid-template-columns:1fr 360px}
#rcEditCard.rh-wide.rh-drawer-off>div:last-child{grid-template-columns:1fr}
#rcEditCard.rh-drawer-off #rcDragPanel{display:none}
.rh-dtabs{display:flex;gap:4px;margin-bottom:8px}
.rh-dtab{font-size:11.5px;padding:5px 10px;border:1px solid var(--border);border-radius:7px;
  background:var(--soft);color:var(--text-mute);cursor:pointer;font-family:inherit}
.rh-dtab.on{background:#fff;color:var(--navy);font-weight:700;border-color:var(--teal)}
.rh-slot{border:1px solid var(--border);border-radius:9px;padding:8px 10px;margin-bottom:7px}
.rh-slot.ok{border-color:#BEDFD4;background:#F4FBF8}
.rh-slot.miss{border-color:#EBD9A8;background:#FFFBF0}
.rh-slot .r1{display:flex;align-items:center;gap:6px;margin-bottom:5px}
.rh-slot .nm{font-size:12px;font-weight:700;color:var(--navy)}
.rh-slot .loc{font-size:10.5px;color:var(--text-mute);margin-left:auto}
.rh-slot select{width:100%;font-family:inherit;font-size:12.5px;padding:5px 7px;
  border:1px solid var(--border);border-radius:6px;background:#fff}
.rh-warn{font-size:11px;color:var(--amber);background:#FFFBF0;border:1px solid #EBD9A8;
  border-radius:7px;padding:7px 9px;margin-bottom:8px;line-height:1.5}
```

- [ ] **Step 5: 오른쪽 패널을 세 칸 서랍으로 바꾼다**

`kcareer.html` 의 `<div id="rcDragPanel">` 안, 맨 위(「경력·자격 목록 — 드래그 또는 클릭」 줄 자리)를 바꾼다:

```html
              <div class="rh-dtabs">
                <button class="rh-dtab on" id="rhDT-map" onclick="rhDrawerTab('map')">채울 곳</button>
                <button class="rh-dtab" id="rhDT-me" onclick="rhDrawerTab('me')">내 정보</button>
                <button class="rh-dtab" id="rhDT-career" onclick="rhDrawerTab('career')">경력·자격</button>
                <button class="btn sm" style="margin-left:auto;font-size:11px" onclick="rhDrawerToggle()" title="서랍을 접으면 문서가 화면을 다 씁니다">⇥</button>
              </div>
              <div id="rhPane-map"><div id="rhMapWarn"></div><div id="rhMapList" class="rc-mute" style="font-size:12px">서식을 올리면 채울 곳이 여기 나옵니다</div>
                <button class="btn primary" style="width:100%;margin-top:8px" onclick="rhFillByMap()">✅ 이대로 채우기</button></div>
              <div id="rhPane-me" style="display:none"><div id="rhMyInfo"></div></div>
              <div id="rhPane-career" style="display:none">
```

⚠ 기존 「경력·자격 목록」 묶음(`rcCatTabs`·`rcDragSearch`·`rcDragList`)을 `#rhPane-career` 안으로
옮기고, 그 묶음이 끝나는 자리에 `</div>` 를 하나 더한다. **id 는 바꾸지 않는다** —
`_initDragPanel`·`_renderDragList` 가 그 id 로 찾는다.

- [ ] **Step 6: 화면 함수를 더한다**

`kcareer.html` 의 `function rhToggleWide(){…}` 아래에 더한다:

```js
/* ===== 칸 지도 화면 =====
   지금까지는 「알아본 자리만」 채우고 모르는 자리는 조용히 지나갔다.
   여기서는 모르는 자리도 목록에 올려 «물어본다» — 그래야 처음 보는 서식도 다 채운다. */
var _rhMap = null;        /* {slots,lists,warn} + fp */
var _rhPicks = {};        /* {slotId: 열쇠} — 사람이 고른 것 */

var RH_KEYS = [
  ['name','성명'],['nameHanja','한자 성명'],['birth','생년월일'],['gender','성별'],
  ['phone','휴대폰'],['phoneHome','자택 전화'],['phoneWork','사무실 전화'],
  ['email','이메일'],['addr','주소'],['org','소속'],['dept','부서'],
  ['title','직위'],['license','자격증']
];

function rhDrawerTab(which){
  ['map','me','career'].forEach(function(k){
    var p=document.getElementById('rhPane-'+k); if(p) p.style.display=(k===which?'':'none');
    var t=document.getElementById('rhDT-'+k); if(t) t.classList.toggle('on', k===which);
  });
  if(which==='me') rhRenderMyInfo();
}
function rhDrawerToggle(){
  var c=document.getElementById('rcEditCard'); if(c) c.classList.toggle('rh-drawer-off');
}

/* 「내 정보」 — 클릭하면 편집기 커서 자리에 들어간다.
   이 목록이 없어서 성명·연락처를 손으로 쳐야 했다(대표 제보 2026-08-29). */
function rhRenderMyInfo(){
  var box=document.getElementById('rhMyInfo'); if(!box) return;
  var f=(_cvFillData().fields)||{};
  box.innerHTML=RH_KEYS.map(function(p){
    var v=f[p[0]]; if(!v) return '';
    return '<div class="rh-slot" style="cursor:pointer;margin-bottom:5px" onclick="insertToEditor('+_jsAttr2(v)+')">'
      +'<div style="font-size:10.5px;color:var(--text-mute)">'+escapeHtml(p[1])+'</div>'
      +'<div style="font-size:12.5px;font-weight:600;color:var(--ink)">'+escapeHtml(v)+'</div></div>';
  }).join('')||'<div class="rc-mute" style="font-size:12px">프로필에 적은 내용이 없습니다</div>';
}
function _jsAttr2(s){ return "'"+_jsAttr(s)+"'"; }

/* 올린 서식을 훑어 칸 지도를 그린다 */
async function rhBuildMap(){
  if(!_rhDoc || !_rhDoc.bytes) return;
  if(!window.KcareerFormMap){ toast('칸 지도 모듈을 못 불러왔습니다'); return; }
  try{ await _ensureJSZip(); }catch(e){ toast('모듈 로드 실패(인터넷 필요)'); return; }
  var src; try{ src=await _rhToHwpx(_rhDoc.bytes,_rhDoc.name); }catch(e){ src=null; }
  if(!src){ document.getElementById('rhMapList').textContent='이 형식은 칸 지도를 만들 수 없습니다 — 한글에서 hwpx로 저장해 올려주세요'; return; }
  var zip=await JSZip.loadAsync(src);
  var names=Object.keys(zip.files).filter(function(n){ return /Contents\/section\d+\.xml$/i.test(n); }).sort();
  var slots=[],lists=[],warn={textBoxes:0,nested:0},fps=[];
  for(const n of names){
    var xml=await zip.file(n).async('string');
    var m=KcareerFormMap.guess(KcareerFormMap.scan(xml), _cvFillData());
    m.slots.forEach(function(s){ s.sec=n; }); m.lists.forEach(function(l){ l.sec=n; });
    slots=slots.concat(m.slots); lists=lists.concat(m.lists);
    warn.textBoxes+=m.warn.textBoxes; warn.nested+=m.warn.nested;
    fps.push(KcareerFormMap.fingerprint(xml));
  }
  _rhMap={slots:slots,lists:lists,warn:warn,fp:fps.join('/')};
  _rhPicks=rhMemoryLoad(_rhMap.fp) || {};
  rhRenderMap();
}

function rhRenderMap(){
  var box=document.getElementById('rhMapList'); if(!box||!_rhMap) return;
  var w=document.getElementById('rhMapWarn');
  /* 못 읽은 것은 «반드시» 적는다 — 조용히 빠지면 「채웠다는데 비어 있다」가 된다 */
  var msgs=[];
  if(_rhMap.warn.textBoxes) msgs.push('글상자 '+_rhMap.warn.textBoxes+'개는 못 읽습니다(한글에서 직접 쓰셔야 합니다)');
  if(_rhMap.warn.nested) msgs.push('칸 안에 표가 또 있는 자리 '+_rhMap.warn.nested+'곳은 건드리지 않습니다');
  if(w) w.innerHTML=msgs.length?'<div class="rh-warn">⚠ '+msgs.join('<br>⚠ ')+'</div>':'';

  var f=(_cvFillData().fields)||{};
  var shown=_rhMap.slots.filter(function(s){ return s.guess || s.left || s.kind!=='빈칸'; });
  var hidden=_rhMap.slots.length-shown.length;
  var known=shown.filter(function(s){ return (_rhPicks[s.id]||s.guess); }).length;
  var head='<div style="font-size:11.5px;color:var(--text-mute);margin-bottom:8px">채울 곳 '
    +shown.length+'군데 — 알아본 것 '+known+' · 골라 줄 것 '+(shown.length-known)+'</div>';

  box.innerHTML=head+_rhMap.lists.map(function(l){
    return '<div class="rh-slot ok"><div class="r1"><span class="nm">'
      +(l.kind==='edu'?'학력 표':'경력 표')+'</span><span class="loc">빈 '+l.blank+'줄</span></div>'
      +'<div style="font-size:11px;color:var(--text-mute)">아래 빈 줄에 '+(l.kind==='edu'?'학력':'경력')+'을 채웁니다</div></div>';
  }).join('')+shown.map(function(s){
    var pick=(_rhPicks[s.id]!=null)?_rhPicks[s.id]:s.guess;
    var ok=!!pick;
    var opts='<option value="">— 비워 둠 —</option>'+RH_KEYS.map(function(p){
      return '<option value="'+p[0]+'"'+(pick===p[0]?' selected':'')+'>'+p[1]+(f[p[0]]?' · '+escapeHtml(f[p[0]]).slice(0,18):'')+'</option>';
    }).join('')+'<option value="__incell"'+(pick==='__incell'?' selected':'')+'>칸 안 라벨 자동</option>';
    return '<div class="rh-slot '+(ok?'ok':'miss')+'">'
      +'<div class="r1"><span class="nm">'+escapeHtml(s.left||s.text||'빈 칸')+'</span>'
      +'<span class="loc">'+(s.tbl+1)+'표 '+(s.row+1)+'행 '+(s.col+1)+'열</span></div>'
      +(s.text?'<div style="font-size:10.5px;color:var(--text-mute);margin-bottom:4px">지금 「'+escapeHtml(s.text)+'」'+(s.kind==='안내글뒤'?' — 뒤에 이어 씁니다':'')+'</div>':'')
      +(s.hint==='rrn'?'<div style="font-size:10.5px;color:var(--red);margin-bottom:4px">주민등록번호는 자동으로 넣지 않습니다</div>':'')
      +'<select onchange="rhPick(\''+s.id+'\',this.value)">'+opts+'</select></div>';
  }).join('');
  if(hidden) box.innerHTML+='<div style="font-size:11px;color:var(--text-mute);margin-top:6px">'
    +'이름도 왼쪽 라벨도 없는 빈 칸 '+hidden+'군데는 감췄습니다</div>';
}
function rhPick(id,v){ _rhPicks[id]=v; rhRenderMap(); }
```

- [ ] **Step 7: 「이대로 채우기」를 만든다**

같은 자리에 이어서 더한다:

```js
/* 고른 대로 채운 뒤 «보고 있는 편집기»에 다시 올린다.
   ⚠ 다른 창에만 띄우면 편집기는 그대로라 「내가 쳐야 하나」가 된다(대표 제보 2026-08-29). */
async function rhFillByMap(){
  if(!_rhDoc||!_rhDoc.bytes||!_rhMap){ toast('먼저 서식을 올려주세요'); return; }
  toast('⏳ 채우는 중…');
  var src=await _rhToHwpx(_rhDoc.bytes,_rhDoc.name);
  if(!src){ toast('이 형식은 채울 수 없습니다'); return; }
  var zip=await JSZip.loadAsync(src), data=_cvFillData();
  var filled=[], failed=[];
  var names=Object.keys(zip.files).filter(function(n){ return /Contents\/section\d+\.xml$/i.test(n); }).sort();
  for(const n of names){
    var picks={};
    _rhMap.slots.forEach(function(s){ if(s.sec!==n) return;
      var p=(_rhPicks[s.id]!=null)?_rhPicks[s.id]:s.guess; if(p) picks[s.id]=p; });
    var wantLists={}; _rhMap.lists.forEach(function(l){ if(l.sec===n) wantLists[l.id]=l.guess; });
    var xml=await zip.file(n).async('string');
    var r=KcareerFormMap.apply(xml,{picks:picks,lists:wantLists,data:data});
    if(r.changed) zip.file(n,r.xml);
    filled=filled.concat(r.filled); failed=failed.concat(r.failed);
  }
  if(!filled.length){ toast('채운 칸이 없습니다 — 오른쪽에서 넣을 값을 골라 주세요'); return; }
  var bytes=new Uint8Array(await zip.generateAsync({type:'arraybuffer'}));
  _rhDoc={ name:String(_rhDoc.name).replace(/\.(hwp|hwpx)$/i,'')+'_채움.hwpx', ext:'hwpx', bytes:bytes };
  rhMemorySave(_rhMap.fp, _rhPicks, _rhDoc.name);
  rhDraftSave();
  await mountEditor(bytes.buffer.slice(0), _rhDoc.name);   /* 보고 있는 편집기에 그대로 */
  toast('✅ '+filled.length+'칸 채웠습니다'+(failed.length?' · '+failed.length+'칸은 못 넣었습니다':''));
  if(failed.length) console.warn('[칸지도] 못 넣은 자리', failed);
}
```

- [ ] **Step 8: 서식을 올릴 때 지도를 만들게 잇는다**

`importTemplateFile` 의 마지막(`if(/^(hwp|hwpx)$/.test(ext)) openHwpViewer(buf, fname);`) 바로 앞에 더한다:

```js
  /* 올리자마자 칸 지도를 그린다 — 「어디를 채울지」를 먼저 보여 준다 */
  if(/^(hwp|hwpx)$/.test(ext)) { try{ await rhBuildMap(); }catch(e){ console.warn('칸지도',e); } }
```

- [ ] **Step 9: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-formmap-ui.test.js`
Expected: PASS — 7개

- [ ] **Step 10: 커밋**

```bash
git add kcareer.html tests/kcareer-formmap-ui.test.js
git commit -m "feat(경력관리): 칸 지도 서랍 — 모르는 자리를 «물어보고», 채운 결과를 편집기에 그대로"
```

---

### Task 7: 서식별 기억 — 두 번째부터 진짜 한 번에

**Files:**
- Modify: `kcareer.html` (`rhMemorySave`·`rhMemoryLoad`)
- Test: `tests/kcareer-formmap-ui.test.js` (검사 추가)

**Interfaces:**
- Consumes: `KcareerFormMap.fingerprint` (Task 5)
- Produces: `rhMemorySave(fp, picks, name)` · `rhMemoryLoad(fp)` → `{}|null` (Task 6 의 `rhBuildMap` 이 이미 부른다)

- [ ] **Step 1: 실패하는 검사를 더한다**

```js
test('고른 짝을 «서식 지문»으로 기억한다 — 서식이 바뀌면 옛 기억을 쓰지 않는다', () => {
  assert.match(bare, /function rhMemorySave/);
  assert.match(bare, /function rhMemoryLoad/);
  const save = bare.slice(bare.indexOf('function rhMemorySave'));
  assert.match(save.slice(0, 800), /fp/, '이름이 아니라 «지문»으로 기억해야 합니다');
});

test('기억은 서식마다 따로 담는다 — 하나로 뭉치면 서식을 바꿀 때 엉킨다', () => {
  assert.match(bare, /form_maps/);
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-formmap-ui.test.js`
Expected: FAIL — `rhMemorySave` 없음

- [ ] **Step 3: 기억 함수를 만든다**

`kcareer.html` 의 `rhFillByMap` 아래에 더한다:

```js
/* ── 서식별 기억 ──
   한 번 골라 준 짝을 «서식 지문»으로 담아 둔다. 같은 서식을 다시 올리면 그대로 얹는다.
   ⚠ 이름이 아니라 지문으로 담는다 — 이름은 사람이 바꾸고, 짜임은 안 바뀐다.
   ⚠ 지문이 다르면 옛 기억을 쓰지 않는다 — 엉뚱한 칸에 넣는 것이 안 넣는 것보다 나쁘다. */
var RH_MEM='form_maps';
function _rhMemAll(){ try{ return JSON.parse(LS.get(NS+RH_MEM)||'{}')||{}; }catch(e){ return {}; } }
function rhMemorySave(fp, picks, name){
  if(!fp) return;
  try{
    var all=_rhMemAll();
    all[fp]={ name:String(name||''), at:Date.now(), picks:picks||{} };
    /* 오래된 것부터 버린다 — 서식이 쌓이면 저장공간을 먹는다 */
    var keys=Object.keys(all);
    if(keys.length>60){ keys.sort(function(a,b){ return (all[a].at||0)-(all[b].at||0); })
      .slice(0, keys.length-60).forEach(function(k){ delete all[k]; }); }
    LS.set(NS+RH_MEM, JSON.stringify(all));
  }catch(e){ console.warn('칸지도 기억',e); }
}
function rhMemoryLoad(fp){
  if(!fp) return null;
  var m=_rhMemAll()[fp];
  if(!m) return null;
  toast('전에 정한 대로 채울 곳을 맞춰 두었습니다 — 확인만 하시면 됩니다');
  return Object.assign({}, m.picks||{});
}
```

- [ ] **Step 4: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-formmap-ui.test.js`
Expected: PASS — 9개

- [ ] **Step 5: 커밋**

```bash
git add kcareer.html tests/kcareer-formmap-ui.test.js
git commit -m "feat(경력관리): 서식별 기억 — 지문이 같으면 고른 짝을 그대로 얹는다"
```

---

### Task 8: 도장 — 눌렀을 때만

**Files:**
- Create: `js/kcareer-hwpstamp.js`
- Modify: `kcareer.html` (모듈 읽기, 「🖋 도장 찍기」 단추, `rhStampDoc`)
- Test: `tests/kcareer-hwpstamp.test.js`

**Interfaces:**
- Produces:
  - `KcareerHwpStamp.picXml({ id:'image1', orgPx:300, showHU:3400, offX, offY })` → `string`
  - `KcareerHwpStamp.findSpot(sectionXml)` → `{ index:number }|null` (「(인)」·「(서명)」 자리)
  - `KcareerHwpStamp.insertPic(sectionXml, picXml, at)` → `string`
  - `KcareerHwpStamp.PX_TO_HU(px)` → `number`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/kcareer-hwpstamp.test.js`:

```js
/* 한글 서식에 도장 그림 넣기 — 2026-08-29 브라우저에서 실제로 찍어 확인한 값을 규칙으로 못 박는다.
   세 번 만에 맞췄다: ①너무 커서 종이 밖 ②잘림 ③제자리.
   ①②의 까닭이 아래 두 검사다. */
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const S = require('../js/kcareer-hwpstamp.js');

test('그림 본래 크기는 px 를 HWPUNIT 으로 — 96dpi 기준', () => {
  /* 검사고정-허용: 1인치 = 7200 HWPUNIT 은 한글 파일 형식의 규칙이다(값이 곧 규칙) */
  assert.equal(S.PX_TO_HU(96), 7200);
  assert.equal(S.PX_TO_HU(300), 22500);
});

test('★ 본래 크기(orgSz)와 찍을 크기(curSz)가 달라야 한다 — 같으면 그림이 잘린다', () => {
  const xml = S.picXml({ id: 'image1', orgPx: 300, showHU: 3400 });
  const org = /<hp:orgSz width="(\d+)"/.exec(xml)[1];
  const cur = /<hp:curSz width="(\d+)"/.exec(xml)[1];
  assert.notEqual(org, cur, '둘을 같게 두면 조각만 그려진다(2026-08-29 실측)');
  assert.equal(org, String(S.PX_TO_HU(300)));
  assert.equal(cur, '3400');
});

test('★ imgRect·imgClip 은 «본래 크기» 기준이다 — 찍을 크기로 적으면 잘린다', () => {
  const xml = S.picXml({ id: 'image1', orgPx: 300, showHU: 3400 });
  const org = String(S.PX_TO_HU(300));
  assert.ok(xml.indexOf('<hp:imgClip left="0" right="' + org + '"') > 0);
  assert.ok(xml.indexOf('<hc:pt1 x="' + org + '"') > 0);
});

test('★ 글자처럼 붙이지 않는다(treatAsChar=0) — 1이면 뒤로 밀려 종이 밖으로 나간다', () => {
  const xml = S.picXml({ id: 'image1', orgPx: 300, showHU: 3400 });
  assert.match(xml, /treatAsChar="0"/);
  assert.match(xml, /allowOverlap="1"/);
  assert.match(xml, /textWrap="IN_FRONT_OF_TEXT"/);
});

test('그림 이름표를 가리킨다 — BinData 의 파일과 이어져야 그려진다', () => {
  assert.match(S.picXml({ id: 'image7', orgPx: 300, showHU: 3400 }), /binaryItemIDRef="image7"/);
});

test('도장 자리를 찾는다 — (인)·(서명)·서명 또는 인·印', () => {
  ['(인)', '（인）', '(서명)', '서명 또는 인', '印'].forEach((mark) => {
    const xml = '<hp:p><hp:run><hp:t>성명 : 권형하   ' + mark + '</hp:t></hp:run></hp:p>';
    assert.ok(S.findSpot(xml), mark + ' 을(를) 도장 자리로 알아봐야 합니다');
  });
});

test('도장 자리가 없으면 «없다»고 한다 — 아무 데나 찍지 않는다', () => {
  assert.equal(S.findSpot('<hp:p><hp:run><hp:t>제출서류 1부</hp:t></hp:run></hp:p>'), null);
});

test('그림을 그 자리 문단 안에 넣는다 — 문서가 깨지지 않게 run 으로 감싼다', () => {
  const xml = '<hp:p><hp:run><hp:t>성명 : 권형하   (인)</hp:t></hp:run></hp:p>';
  const out = S.insertPic(xml, '<hp:pic/>', S.findSpot(xml));
  assert.match(out, /<hp:run[^>]*><hp:pic\/><\/hp:run>/);
  assert.ok(out.indexOf('(인)') > 0, '원래 글자는 남아야 합니다');
});
```

- [ ] **Step 2: 검사를 돌려 실패를 확인한다**

Run: `node --test tests/kcareer-hwpstamp.test.js`
Expected: FAIL — `Cannot find module '../js/kcareer-hwpstamp.js'`

- [ ] **Step 3: 모듈을 만든다**

`js/kcareer-hwpstamp.js`:

```js
'use strict';
/* 푸른노무법인 경력관리 — 한글 서식(HWPX)에 도장 찍기
   (브라우저 window.KcareerHwpStamp / Node module.exports 겸용, DOM·JSZip 미사용 — XML만 만든다)

   2026-08-29 브라우저에서 실제로 찍어 세 번 만에 맞췄다. 틀렸던 두 가지를 여기 적어 둔다:
   ⚠ orgSz(본래 크기)와 curSz(찍을 크기)를 같게 두면 «그림이 잘려 조각만» 그려진다.
     orgSz 는 그림의 px 를 HWPUNIT 으로 바꾼 값이고, curSz 는 종이에 찍을 크기다.
   ⚠ imgRect·imgClip·imgDim 은 «본래 크기» 기준이다. 찍을 크기로 적으면 역시 잘린다.
   ⚠ treatAsChar="1" 로 글자처럼 붙이면 뒤로 밀려 «종이 밖으로» 나간다. 0 으로 두고 겹친다. */
(function (root) {

  var HU_PER_INCH = 7200, DPI = 96;
  function PX_TO_HU(px) { return Math.round(Number(px || 0) / DPI * HU_PER_INCH); }

  function picXml(o) {
    o = o || {};
    var ORG = PX_TO_HU(o.orgPx || 300);         /* 그림 본래 크기 */
    var S = Math.round(o.showHU || 3400);        /* 찍을 크기 — 기본 지름 약 12mm */
    var ox = Math.round(o.offX || 0), oy = Math.round(o.offY || 0);
    return '<hp:pic id="' + (o.picId || 1) + '" zOrder="1" numberingType="PICTURE"'
      + ' textWrap="IN_FRONT_OF_TEXT" textFlow="BOTH_SIDES" lock="0" dropcapstyle="None"'
      + ' href="" groupLevel="0" instid="' + (o.picId || 1) + '" reverse="0">'
      + '<hp:offset x="0" y="0"/>'
      + '<hp:orgSz width="' + ORG + '" height="' + ORG + '"/>'
      + '<hp:curSz width="' + S + '" height="' + S + '"/>'
      + '<hp:flip horizontal="0" vertical="0"/>'
      + '<hp:rotationInfo angle="0" centerX="' + Math.round(S / 2) + '" centerY="' + Math.round(S / 2) + '" rotateimage="1"/>'
      + '<hp:renderingInfo>'
      + '<hc:transMatrix e1="1" e2="0" e3="0" e4="0" e5="1" e6="0"/>'
      + '<hc:scaMatrix e1="1" e2="0" e3="0" e4="0" e5="1" e6="0"/>'
      + '<hc:rotMatrix e1="1" e2="0" e3="0" e4="0" e5="1" e6="0"/>'
      + '</hp:renderingInfo>'
      + '<hp:imgRect><hc:pt0 x="0" y="0"/><hc:pt1 x="' + ORG + '" y="0"/>'
      + '<hc:pt2 x="' + ORG + '" y="' + ORG + '"/><hc:pt3 x="0" y="' + ORG + '"/></hp:imgRect>'
      + '<hp:imgClip left="0" right="' + ORG + '" top="0" bottom="' + ORG + '"/>'
      + '<hp:inMargin left="0" right="0" top="0" bottom="0"/>'
      + '<hp:imgDim dimwidth="' + ORG + '" dimheight="' + ORG + '"/>'
      + '<hp:img binaryItemIDRef="' + (o.id || 'image1') + '" bright="0" contrast="0" effect="REAL_PIC" alpha="0"/>'
      + '<hp:sz width="' + S + '" height="' + S + '" widthRelTo="ABSOLUTE" heightRelTo="ABSOLUTE" protect="0"/>'
      + '<hp:pos treatAsChar="0" affectLSpacing="0" flowWithText="1" allowOverlap="1"'
      + ' holdAnchorAndSO="0" vertRelTo="PARA" horzRelTo="PARA" vertAlign="TOP" horzAlign="LEFT"'
      + ' vertOffset="' + oy + '" horzOffset="' + ox + '"/>'
      + '<hp:outMargin left="0" right="0" top="0" bottom="0"/>'
      + '</hp:pic>';
  }

  /* 도장 자리 — 「(인)」「(서명)」「서명 또는 인」「印」. 못 찾으면 null 이다.
     ⚠ 아무 데나 찍지 않는다. 못 찾으면 화면에서 사람에게 묻는다. */
  var MARKS = /[（(]\s*(인|서명)\s*[)）]|서명\s*또는\s*인|印/;
  function findSpot(sectionXml) {
    var s = String(sectionXml || '');
    var m = MARKS.exec(s);
    if (!m) return null;
    var end = s.indexOf('</hp:run>', m.index);
    if (end < 0) return null;
    return { index: end + '</hp:run>'.length };
  }

  function insertPic(sectionXml, pic, at) {
    if (!at) return String(sectionXml || '');
    var s = String(sectionXml || '');
    return s.slice(0, at.index) + '<hp:run charPrIDRef="0">' + pic + '</hp:run>' + s.slice(at.index);
  }

  var api = { PX_TO_HU: PX_TO_HU, picXml: picXml, findSpot: findSpot, insertPic: insertPic };
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else root.KcareerHwpStamp = api;
})(typeof globalThis !== 'undefined' ? globalThis : window);
```

- [ ] **Step 4: 검사를 돌려 통과를 확인한다**

Run: `node --test tests/kcareer-hwpstamp.test.js`
Expected: PASS — 9개

- [ ] **Step 5: 화면에 「🖋 도장 찍기」를 붙인다**

`kcareer.html` — 모듈을 읽는다(`kcareer-formmap.js` 다음 줄):

```html
<script src="js/kcareer-hwpstamp.js?v=1"></script>
```

편집기 머리 단추 줄(`rhOpenBigPopup` 단추 앞)에 더한다:

```html
                <button class="btn" style="border-color:#D9B9B9;color:var(--red)" onclick="rhStampDoc()" title="★기본 도장을 「(인)」 자리에 찍습니다 — 글자를 다 확인한 뒤 마지막에">🖋 도장 찍기</button>
```

`rhMemoryLoad` 아래에 함수를 더한다:

```js
/* ── 도장 찍기 ──
   글자 채우기와 «따로» 둔다 — 도장을 안 찍는 서류가 있고,
   내용을 다 확인한 뒤 마지막에 날인하는 것이 순서다(대표 결정 2026-08-29). */
async function rhStampDoc(){
  if(!_rhDoc||!_rhDoc.bytes){ toast('먼저 서식을 올려주세요'); return; }
  if(!window.KcareerHwpStamp){ toast('도장 모듈을 못 불러왔습니다'); return; }
  var st=(typeof getDefaultStamp==='function')?getDefaultStamp():null;
  if(!st){ toast('먼저 환경설정 › 도장(직인) 보관함에 도장을 올려주세요'); return; }
  var f=await getFileAsync(st.id);
  if(!f||!f.base64){ toast('도장 그림을 찾을 수 없습니다'); return; }
  try{ await _ensureJSZip(); }catch(e){ toast('모듈 로드 실패(인터넷 필요)'); return; }
  var src=await _rhToHwpx(_rhDoc.bytes,_rhDoc.name);
  if(!src){ toast('이 형식에는 도장을 찍을 수 없습니다'); return; }

  var png=String(f.base64).replace(/^data:[^,]*,/,'');
  var px=await new Promise(function(res){ var im=new Image();
    im.onload=function(){ res(im.naturalWidth||300); }; im.onerror=function(){ res(300); };
    im.src='data:image/png;base64,'+png; });

  var zip=await JSZip.loadAsync(src);
  zip.file('BinData/pustamp.png', png, {base64:true});
  var hpf=await zip.file('Contents/content.hpf').async('string');
  if(hpf.indexOf('id="pustamp"')<0){
    zip.file('Contents/content.hpf', hpf.replace('</opf:manifest>',
      '<opf:item id="pustamp" href="BinData/pustamp.png" media-type="image/png" isEmbeded="1"/></opf:manifest>'));
  }
  var names=Object.keys(zip.files).filter(function(n){ return /Contents\/section\d+\.xml$/i.test(n); }).sort();
  var done=0;
  for(const n of names){
    var xml=await zip.file(n).async('string');
    var at=KcareerHwpStamp.findSpot(xml);
    if(!at) continue;
    var pic=KcareerHwpStamp.picXml({ id:'pustamp', orgPx:px, showHU:3400, offX:-2600, offY:-700 });
    zip.file(n, KcareerHwpStamp.insertPic(xml, pic, at));
    done++; break;   /* 도장은 한 번만 — 여러 장에 찍히면 되돌리기가 어렵다 */
  }
  if(!done){ toast('도장 자리(「(인)」·「(서명)」)를 못 찾았습니다 — 한글에서 직접 넣어 주세요'); return; }
  var bytes=new Uint8Array(await zip.generateAsync({type:'arraybuffer'}));
  _rhDoc={ name:String(_rhDoc.name).replace(/\.(hwp|hwpx)$/i,'')+'_날인.hwpx', ext:'hwpx', bytes:bytes };
  rhDraftSave();
  await mountEditor(bytes.buffer.slice(0), _rhDoc.name);
  toast('🖋 도장을 찍었습니다 — 자리가 어긋나면 한글에서 옮길 수 있습니다');
}
```

- [ ] **Step 6: 화면 검사를 더하고 돌린다**

`tests/kcareer-formmap-ui.test.js` 에 더한다:

```js
test('도장은 «누를 때만» 찍힌다 — 채우기와 한 단추로 묶지 않는다', () => {
  assert.match(bare, /function rhStampDoc/);
  const fill = bare.slice(bare.indexOf('function rhFillByMap'), bare.indexOf('function rhFillByMap') + 2500);
  assert.doesNotMatch(fill, /rhStampDoc|KcareerHwpStamp/,
    '채우기가 도장까지 찍으면 안 찍을 서류에도 찍힙니다');
});

test('도장 모듈을 읽어 들인다 — 캐시 번호를 붙여서', () => {
  assert.match(source, /js\/kcareer-hwpstamp\.js\?v=\d+/);
});
```

Run: `node --test tests/kcareer-hwpstamp.test.js tests/kcareer-formmap-ui.test.js`
Expected: PASS

- [ ] **Step 7: 커밋**

```bash
git add js/kcareer-hwpstamp.js kcareer.html tests/kcareer-hwpstamp.test.js tests/kcareer-formmap-ui.test.js
git commit -m "feat(경력관리): 「(인)」 자리에 도장 찍기 — 누를 때만, 실측한 크기 규칙 그대로"
```

---

### Task 9: 실제 서식으로 끝까지 확인

검사는 XML 만 본다. **엔진이 실제로 그리는지는 브라우저에서만 안다.**

**Files:**
- 없음 (확인만). 어긋나면 해당 Task 로 돌아간다.

- [ ] **Step 1: 전체 검사 — 종료코드로 판정**

```bash
node --test tests/*.test.js; echo "EXIT=$?"
```
Expected: `EXIT=0`. 출력을 골라 읽지 않는다.

- [ ] **Step 2: 화면을 띄운다**

Browser 도구 `preview_start` 로 `static`(8799) 을 켜고 `http://localhost:8799/kcareer.html` 를 연다.
로그인 가림막은 다음으로 감춘다:

```js
[...document.querySelectorAll('body > *')]
  .filter(el=>el.textContent.includes('통합시스템 로그인')).forEach(el=>el.style.display='none');
```

- [ ] **Step 3: 대표 서식 모양을 만들어 올린다**

```js
const t=(rows)=>HWPX.tablePara(rows,HWPX.cols(rows[0].map(()=>1/rows[0].length)));
const body=HWPX.para('□ 지원 직위명 : 위촉직이사',HWPX.CP.title)
 +t([['성  명','(한글)','생년월일','']])
 +t([['현 주 소','']])
 +t([['전화번호','자택:_______  직장:_______','휴대폰','','E-mail','']])
 +t([['현 근무처','기관명 : 부서명 : 직위 :']])
 +HWPX.spacer()+HWPX.para('성명 :  권형하        (인)',HWPX.CP.body,{paraPr:HWPX.PP.right});
await importTemplateFile(new File([HWPX.build(body)],'위촉직이사 지원서류.hwp'),'resume');
```

⚠ 이름을 일부러 `.hwp` 로 둔다 — 이름과 속이 다른 실제 상황이다.

- [ ] **Step 4: 눈으로 확인한다**

- 칸 지도에 **「(한글)」 자리가 「성명」으로** 떠 있는가
- 「채울 곳 N군데 — 알아본 것 M」 이 보이는가
- **「이대로 채우기」** 를 누르면 편집기 안 문서에 이름·주소·전화·기관명·부서명·**직위**가 들어가는가
- **「🖋 도장 찍기」** 를 누르면 「(인)」 옆에 도장이 **온전히**(잘리지 않고) 그려지는가
- 서랍 접기(⇥)로 문서가 화면을 다 쓰는가

- [ ] **Step 5: 같은 서식을 다시 올려 «기억»을 확인한다**

같은 서식을 한 번 더 올린다. 「전에 정한 대로 채울 곳을 맞춰 두었습니다」 알림이 뜨고
🟨 골라 줄 것이 **줄어드는지** 본다.

- [ ] **Step 6: 상태 파일에 적고 커밋·PR**

`fund-erp/STATUS.md` 끝에 한 줄 적는다(무엇이 바뀌었는지 · 실측값 · 못 하는 것).

```bash
git add fund-erp/STATUS.md
git commit -m "docs(STATUS): 경력관리 칸 지도 서식 채움 완료"
git push -u origin fix/kcareer-hwp-form
gh pr create --base main --title "feat(경력관리): 칸 지도 — 어떤 서식이든 채울 자리를 다 찾는다" --body-file -
```

⚠ PR 을 열면 곧 자동으로 합쳐진다. **커밋을 전부 밀어 넣은 뒤에** 연다.

---

## 자기 점검

**설계서 훑기 — 빠진 것이 있는가**

| 설계서 | 어느 Task |
|---|---|
| §1 「직위」 결함 | Task 1 |
| §3.1 `scan` 세 갈래 | Task 2 |
| §3.1 목록 표 `lists` | Task 3 |
| §5 글상자·중첩표 알리기 | Task 3(모듈) · Task 6(화면) |
| §3.2 짐작 다섯 고침 ①②⑤ | Task 4 |
| §3.2 ③ 목록 묶기 | Task 3 |
| §3.2 ④ 순수 빈 칸 접기 | Task 6 (`shown`/`hidden`) |
| §3.1 `apply` | Task 5 |
| §3.3 서식 지문·기억 | Task 5(지문) · Task 7(기억) |
| §3.4 도장 | Task 8 |
| §4 화면·서랍·「내 정보」 | Task 6 |
| §6 안전장치 | Task 2(안 덮음) · Task 4(rrn) · Task 5(비워 둠) · Task 8(따로) |
| §7 검사 | Task 1~8 각 검사 |

빠진 것 없음.

**이름 맞춰 보기**

- `scan`/`guess`/`apply`/`fingerprint` — Task 2·4·5 에서 정의, Task 6·7 에서 그 이름 그대로 사용 ✔
- `slot.id` 모양 `t0r1c2` — Task 2 정의, Task 5 `apply` 의 `picks` 열쇠, Task 6 `_rhPicks` 열쇠 ✔
- `KcareerHwpStamp.picXml/findSpot/insertPic/PX_TO_HU` — Task 8 정의·사용 ✔
- `rhMemorySave/rhMemoryLoad` — Task 6 에서 부르고 Task 7 에서 정의. **Task 6·7 은 반드시 이 순서로 이어서** 한다(Task 6 만 하고 멈추면 화면이 터진다). Task 6 Step 9 검사는 통과하지만 브라우저에서는 Task 7 전까지 지도 그리기가 실패한다 — 알고 넘어간다.
- `_cvFillData`·`_rhToHwpx`·`_ensureJSZip`·`getDefaultStamp`·`getFileAsync`·`mountEditor`·`_jsAttr` — 모두 `kcareer.html` 에 이미 있다 ✔
