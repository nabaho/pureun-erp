# 업무시트 (work.html) — 상태 · 인수인계

직원 주간 업무기록 + 인수인계 관리. 엑셀 `2026업무시트.xlsx`(사람별 시트 8개)를 대체.
설계안: https://claude.ai/code/artifact/1a330202-fe13-4564-b474-878d59a14b0f

---

## 1. 사용 시작 전 필요한 것 (관리자 1회)

### ① Firebase 규칙에 `work_erp` 블록 추가 — **필수, 이것 없으면 화면이 안 열림**

Firebase 콘솔 → 프로젝트 `pureun-erp` → Realtime Database → 규칙. 기존 `fund_erp` 블록 옆에 추가:

```json
"work_erp": {
  ".read": "auth != null",
  ".write": "auth != null"
}
```

`fund_erp`와 같은 방식(로그인한 사람만 읽기·쓰기)입니다. 게시(Publish)하면 즉시 적용됩니다.

### ② 엑셀 이관 데이터 가져오기 — 163건

1. 포털 → 업무시트 → 좌측 하단 **📥 이관 가져오기**
2. `업무시트_이관_163건.json` 선택 (Downloads 폴더, 원본은 `fund-erp/backups/work_items_export.json`)
3. 같은 파일을 다시 넣어도 중복 등록되지 않습니다(이관 키 `xk` 기준)

| 담당 | 건수 | 비고 |
|---|---:|---|
| 박재원 | 61 | 재직 |
| 박한별 | 36 | 재직 |
| 김동현 | 28 | 재직 |
| 김혜민 | 23 | 재직 |
| **임혜미** | **15** | **퇴사 — 후임 지정 필요** |

**임혜미 15건**은 퇴사했지만 2026년 5월까지 진행 기록이 살아 있어 함께 이관합니다.
재직자 명단에 없으므로 가져오기 직후 **🔄 인수인계 → "⚠ 재직자 명단에 없는 담당자의 잔여 업무"**
섹션에 자동으로 모입니다. 거기서 건별로 후임을 지정하면 그 사람의 내 업무로 넘어가고,
이미 종결된 건은 드로어에서 완료 처리하면 보관함으로 갑니다.

제외: 김동근·박지호(퇴사, 최근 기록 없음), 박성수(휴직, 시트 기록 2025.10 중단).

재추출: `python fund-erp/backups/extract_work_items.py`

> ⚠️ **이관 키 중복 주의 (2026-07-26 실제 사고)** — 엑셀 시트는 `번호`를 재사용합니다
> (연도·구역별로 1번부터 다시 시작. 예: 김혜민 시트에 `번호 10`이 9개).
> 처음엔 이관 키를 `담당-번호`로 만들어서 **163건 중 71건이 서로 덮어써 92건만 들어갔습니다.**
> 지금은 키에 행 번호까지 넣어(`박한별-1-r5`) 유일성을 확보했고, 가져오기 화면이
> 키 중복을 사전 검사해 중복이 있으면 아예 중단합니다.
> 잘못 들어간 이관분은 가져오기 화면의 **"엑셀 이관분 N건을 지우고 새로 넣기"** 로 정리합니다
> (업무·주별 기록·건별 기록·노트까지 삭제. 직접 등록분과 푸른이알피 유입분은 건드리지 않음).

### ③ AI 기능(선택)

푸른이알피 설정의 AI 프록시 주소(`setting_ai_proxy_url`)가 등록되어 있으면 인수인계 노트에
**✨ AI 초안** / **🎤 AI 인터뷰** 버튼이 자동으로 나타납니다. 없으면 버튼이 숨겨지고 나머지 기능은 정상 동작합니다.
첫 사용 시 "기록이 외부 AI로 전송된다"는 동의 창이 한 번 뜹니다.

---

## 2. 화면

| 화면 | 내용 |
|---|---|
| 📝 내 업무 | 오늘 포커스(기한 임박·이번 주 미기록·다음 할 일·인수 대기) + 주간 표(이번 주 칸 인라인 입력, Enter 저장→다음 행) |
| 타임라인 드로어 | 행 클릭 → 건별 전체 이력(주 구분·보완입력 병기·인수인계 마커) + 인수인계 노트 |
| 주 피커 캘린더 | 기간 라벨 클릭 → 일별 기록 도트·기한 표시, 주 클릭으로 과거 열람 |
| 👥 팀 전체 | 담당별 그룹·기록률·미기록 필터 (전 직원 상호 공개) |
| 🔄 인수인계 | 부재 예정 등록(대표만)·체크아웃 표·확인 지연 경고·퇴직자 잔여 업무·문서철 |
| 🗄 보관함 | 완료 건·복구 |

단축키: `/` 빠른 기록, `N` 새 업무, `Esc`/`Backspace` 뒤로.

---

## 2-2. 푸른이알피 `나의 업무`와의 역할 분담

두 화면은 강점이 반대라서 **경계를 나누고 목록만 공유**합니다.

| | 푸른이알피 · 나의 업무 | 업무시트 |
|---|---|---|
| 담당 | 계약·수임 단위의 *무엇이 있나* (마스터·계약기간·금액·청구) | *무엇을 했나* (주별 경과·인수인계) |
| 기록 | 진행률 % · 상태 (덮어씀) | 주별 문장 (쌓임) |

**목록 공유** — 업무시트 좌측 하단 **🔗 푸른이알피 업무 가져오기**:

- `계약·사건·컨설팅·기금·기타` 5종 마스터에서 **진행 중**인 건만 담당자별로 가져옵니다
  (종료 판정은 pu-erp `isItemClosed`와 동일 기준)
- **관리번호는 푸른이알피 번호를 그대로 사용** (`계약-2026-027`) — 번호가 두 벌로 갈리지 않음
- 유형명은 표준 구분 13종으로 자동 정규화 (`일터혁신컨설팅` → 컨설팅, `임금체불` → 체불 …)
- **중복 처리**: 이미 가져온 건은 자동 제외(`src:'puerp'` + `ref`), 기존 업무와 기업명이 겹치는 건은
  **기본 체크 해제 + 어떤 건과 겹치는지 표시** → 담당자가 건별로 판단 (같은 건이면 두고, 다른 건이면 체크)
- 새 계약이 생기면 버튼을 다시 누르면 됩니다 (기존 건은 건드리지 않음)
- 가져온 건의 드로어에 **[🔗 푸른이알피]** 버튼 — 해당 메뉴로 이동(`#menu=biz/...` 딥링크)

**데이터 출처**: pu-erp가 `localStorage`(`pureun_v6_*`)에 캐시한 마스터를 같은 출처에서 읽습니다.
그 브라우저에서 푸른이알피를 한 번도 안 열었으면 안내 문구와 [푸른이알피 열기] 버튼이 나옵니다.

**되돌려 보여주기 (3단계, 완료)** — 푸른이알피 `나의 업무` → `내 프로젝트` 표에 **최근 기록** 컬럼:

- `work_erp/items/{연도}`를 1회 읽어 `ref(type|id)`로 매칭 → 마지막 기록 1줄 표시
- 셀 클릭 → `work.html#item=<건ID>` 새 탭 (해당 업무 타임라인 바로 열림)
- **읽기 전용** — pu-erp 데이터에 쓰지 않으므로 동시 편집 충돌이 없습니다.
  Firebase 미연결·데이터 없음·읽기 실패 시 조용히 `—` 표시(기존 동작 무영향)
- 진행률 %는 건드리지 않았습니다. 퍼센트보다 실제 기록 한 줄이 상태를 정확히 보여줍니다.

> pu-erp.html 수정 시 검증법: 인라인 `<script>` 9블록을 추출해 각각 `node --check`.
> 편집 전후 블록 수와 통과 여부가 같아야 합니다.

## 2-3. 코덱스 리뷰 반영 (2026-07-26)

Codex CLI 0.144.6으로 전체 리뷰 → 20건 지적(P1 11 / P2 9). 그중 **실제 위험 5건 수정**:

| # | 문제 | 수정 |
|---|---|---|
| 1 | `items`가 현재 연도 파티션에 고정 → **2027-01-01에 전 데이터가 화면에서 사라짐** | `work_erp/items/{id}` 평면 구조로 변경 (pu-erp 읽기 경로도 함께). `logs`는 주차 분할 유지 |
| 2 | `patchItem`이 쓰기 실패를 삼키고 성공으로 resolve → "저장 실패"와 "✅ 완료"가 동시에 뜸 | 성공 여부 boolean 반환, **호출부 11곳 전부 `if(!ok)return;` 가드**. 특히 담당 변경 실패 시 감사 로그를 남기지 않음 |
| 4 | `esc()`가 작은따옴표를 막지 않아 이름·키에 `'`가 있으면 인라인 핸들러가 깨짐 | `escJ()`(JS 이스케이프→HTML 이스케이프) 추가. 키 생성은 `safeKey()`로 통일해 근원에서 차단 |
| 5 | 중복 검사를 **변환 전** 원본 키로 수행 → `a.b`와 `a#b`가 같은 키로 붕괴 | `safeKey()` 적용 **후**의 키로 검사. 제어문자·따옴표·공백도 제거. 키 없음/사용불가도 중단 사유 |
| 6 | 교체 가져오기가 **삭제를 먼저 커밋**한 뒤 업로드 → 업로드 실패 시 기존 데이터 영구 소실 | `excelWipePaths()`가 삭제 경로만 반환하고, 삭제(null)+등록을 **한 번의 `update()`**로 커밋. 삭제 직전 `itemlogs`를 서버에서 재조회(캐시 오래됨 방지) |

**보류한 15건** — 3·7·8·11은 동시 편집 경합(8명 사무실에서 같은 노트를 초 단위로 동시 편집할 확률이 낮고, 고치려면 트랜잭션 구조 전환 필요). 9·10·13·17은 다음 차례(빈 SID 삭제 권한, 부재/고아 중복 표시, IME `isComposing`, AI 요청 타임아웃). 12·14·15·16·18·19·20은 실사용 영향 미미.

> 검증: `verify_fixes.js` 32항목 전부 PASS + work.html/pu-erp.html 문법 검사.
> 작업 중 파일에 NUL 바이트가 들어간 사고 1회 — 바이트 단위로 복구함. `work.html`
> 편집 시 정규식 문자클래스에 제어문자를 넣지 말고 `\uXXXX` 이스케이프를 쓸 것.

## 3. 확정된 정책 (2026-07-26, 사용자 결정)

1. **이관 범위** — 진행 중인 건만 (완료 건은 엑셀 원본 보관)
2. **공개 범위** — 전 직원 상호 공개
3. **구분 표준** — 업무분류기준 11종 + 컨설팅 + 기타로 시작, **이관 데이터 확인 후 재검토 예정**
4. **인수인계 적용 범위** — 퇴사·휴직·연차 등 담당 부재 전부
5. **노트 작성 시점** — 평상시에도 작성(드로어에 상시 노출)
6. **인수 확인 지연** — 5일 경과 시 대표 배지
7. **공동담당** — 부담당이 승계 1순위 기본값, 노트는 그래도 작성
8. **인수인계서** — 문서철에 스냅샷 보관(퇴사자가 떠난 뒤에도 재인쇄 가능)
9. **부재 예정 등록 권한** — 대표만 (`isAdmin()` = 직위에 '대표' 포함)

---

## 4. 데이터 구조 (Firebase `work_erp/*`)

```
work_erp/
├─ items/{연도}/{건ID}          업무건 (cat·company·client·officer·title·no·due·status·mgr_main·mgr_subs·last·ho·ho_note·state)
├─ logs/{연도}/{주차}/{건ID}/   주별 기록 — 주차 노드만 로드 (화면이 필요한 조각만)
├─ itemlogs/{건ID}/            건별 기록 사본 — 타임라인 드로어용
├─ leaving/{사번}              부재 예정 (name·type·date·done)
└─ handover_docs/{키}          인수인계서 스냅샷
```

기록은 **일 단위**로 날짜 저장, 주는 묶음 단위. 연간 약 1.7MB, 화면 1회 로딩 50~100KB —
10년 쌓여도 로딩량 동일(분할 저장). 읽기전용 참조: pu-erp `user_accounts`(재직자), `companies`(업체 자동완성).

---

## 5. 남은 작업

- 이관 데이터 확인 후 **구분 표준 재검토** (원본 표기는 `cat_raw`에 보존됨)
- 회의 모드(팀 보드 → 사람별 큰 글씨 발표 화면)
- 통계 화면 (구분·사람별 집계)
- 모바일 카드형 레이아웃 (현재는 반응형 표)
- 금요일 미기록 알림
- AI 3단계: 후임용 Q&A ("이 건 감독관 연락처가 뭐였지?" → 타임라인 근거 답변)

## 6. 주의

- `pureunall` 저장소는 여러 세션이 동시 작업 — 커밋은 **내 파일만 `git add`**, push 전 `git pull --rebase --autostash`. `git stash pop`은 `git stash list` 확인 후.
- `work_items_export.json`은 의뢰인 연락처 포함 — `.gitignore`된 `fund-erp/backups/`에만 두고 저장소에 올리지 말 것.
