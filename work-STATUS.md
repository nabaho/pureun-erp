# 업무시트 (work.html) — 상태 · 인수인계

직원 주간 업무기록 + 인수인계 관리. 엑셀 `2026업무시트.xlsx`(사람별 시트 8개)를 대체.
설계안: https://claude.ai/code/artifact/1a330202-fe13-4564-b474-878d59a14b0f

---

## 1. 사용 시작 전 필요한 것 (관리자 1회)

### ① Firebase 규칙에 `work_erp` 블록 추가 — **필수, 이것 없으면 화면이 안 열림**

Firebase 콘솔 → 프로젝트 `pureun-erp` → Realtime Database → 규칙. 기존 `fund_erp` 블록 옆에 추가:

```json
"work_erp": {
  ".read": "auth != null",
  ".write": "auth != null"
}
```

`fund_erp`와 같은 방식(로그인한 사람만 읽기·쓰기)입니다. 게시(Publish)하면 즉시 적용됩니다.

### ② 엑셀 이관 데이터 가져오기

1. 포털 → 업무시트 → 좌측 하단 **📥 이관 가져오기**
2. `work_items_export.json` 선택 (Downloads 폴더 또는 `fund-erp/backups/`)
3. 같은 파일을 다시 넣어도 중복 등록되지 않습니다(이관 키 `xk` 기준)

재추출: `python fund-erp/backups/extract_work_items.py`

### ③ AI 기능(선택)

푸른이알피 설정의 AI 프록시 주소(`setting_ai_proxy_url`)가 등록되어 있으면 인수인계 노트에
**✨ AI 초안** / **🎤 AI 인터뷰** 버튼이 자동으로 나타납니다. 없으면 버튼이 숨겨지고 나머지 기능은 정상 동작합니다.
첫 사용 시 "기록이 외부 AI로 전송된다"는 동의 창이 한 번 뜹니다.

---

## 2. 화면

| 화면 | 내용 |
|---|---|
| 📝 내 업무 | 오늘 포커스(기한 임박·이번 주 미기록·다음 할 일·인수 대기) + 주간 표(이번 주 칸 인라인 입력, Enter 저장→다음 행) |
| 타임라인 드로어 | 행 클릭 → 건별 전체 이력(주 구분·보완입력 병기·인수인계 마커) + 인수인계 노트 |
| 주 피커 캘린더 | 기간 라벨 클릭 → 일별 기록 도트·기한 표시, 주 클릭으로 과거 열람 |
| 👥 팀 전체 | 담당별 그룹·기록률·미기록 필터 (전 직원 상호 공개) |
| 🔄 인수인계 | 부재 예정 등록(대표만)·체크아웃 표·확인 지연 경고·퇴직자 잔여 업무·문서철 |
| 🗄 보관함 | 완료 건·복구 |

단축키: `/` 빠른 기록, `N` 새 업무, `Esc`/`Backspace` 뒤로.

---

## 3. 확정된 정책 (2026-07-26, 사용자 결정)

1. **이관 범위** — 진행 중인 건만 (완료 건은 엑셀 원본 보관)
2. **공개 범위** — 전 직원 상호 공개
3. **구분 표준** — 업무분류기준 11종 + 컨설팅 + 기타로 시작, **이관 데이터 확인 후 재검토 예정**
4. **인수인계 적용 범위** — 퇴사·휴직·연차 등 담당 부재 전부
5. **노트 작성 시점** — 평상시에도 작성(드로어에 상시 노출)
6. **인수 확인 지연** — 5일 경과 시 대표 배지
7. **공동담당** — 부담당이 승계 1순위 기본값, 노트는 그래도 작성
8. **인수인계서** — 문서철에 스냅샷 보관(퇴사자가 떠난 뒤에도 재인쇄 가능)
9. **부재 예정 등록 권한** — 대표만 (`isAdmin()` = 직위에 '대표' 포함)

---

## 4. 데이터 구조 (Firebase `work_erp/*`)

```
work_erp/
├─ items/{연도}/{건ID}          업무건 (cat·company·client·officer·title·no·due·status·mgr_main·mgr_subs·last·ho·ho_note·state)
├─ logs/{연도}/{주차}/{건ID}/   주별 기록 — 주차 노드만 로드 (화면이 필요한 조각만)
├─ itemlogs/{건ID}/            건별 기록 사본 — 타임라인 드로어용
├─ leaving/{사번}              부재 예정 (name·type·date·done)
└─ handover_docs/{키}          인수인계서 스냅샷
```

기록은 **일 단위**로 날짜 저장, 주는 묶음 단위. 연간 약 1.7MB, 화면 1회 로딩 50~100KB —
10년 쌓여도 로딩량 동일(분할 저장). 읽기전용 참조: pu-erp `user_accounts`(재직자), `companies`(업체 자동완성).

---

## 5. 남은 작업

- 이관 데이터 확인 후 **구분 표준 재검토** (원본 표기는 `cat_raw`에 보존됨)
- 회의 모드(팀 보드 → 사람별 큰 글씨 발표 화면)
- 통계 화면 (구분·사람별 집계)
- 모바일 카드형 레이아웃 (현재는 반응형 표)
- 금요일 미기록 알림
- AI 3단계: 후임용 Q&A ("이 건 감독관 연락처가 뭐였지?" → 타임라인 근거 답변)

## 6. 주의

- `pureunall` 저장소는 여러 세션이 동시 작업 — 커밋은 **내 파일만 `git add`**, push 전 `git pull --rebase --autostash`. `git stash pop`은 `git stash list` 확인 후.
- `work_items_export.json`은 의뢰인 연락처 포함 — `.gitignore`된 `fund-erp/backups/`에만 두고 저장소에 올리지 말 것.
