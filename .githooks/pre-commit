#!/bin/sh
# 커밋 전 점검 — 고친 .js 를 쓰는 쪽의 ?v= 를 안 올렸는지 본다.
# 켜려면 한 번만:  git config core.hooksPath .githooks
# 건너뛰려면:      git commit --no-verify
command -v node >/dev/null 2>&1 || exit 0
node "$(git rev-parse --show-toplevel)/scripts/check-cache-version.js" || exit 1
